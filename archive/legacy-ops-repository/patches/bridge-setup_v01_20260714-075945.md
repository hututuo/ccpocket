# ccPocket Bridge setup v01

Status: active

## Scope

- Requested command: `npx @ccpocket/bridge@latest setup`
- Global target: `/Users/huyiyang/Library/LaunchAgents/com.ccpocket.bridge.plist`
- Service label: `com.ccpocket.bridge`
- Purpose: refresh the ccPocket Bridge LaunchAgent registration and restart the service.

## Backup

- Pre-setup plist: `../backups/20260714-075945_bridge-setup/com.ccpocket.bridge.before.plist`
- Setup and verification evidence: `../runs/20260714-075945_bridge-setup/`

## Result

- `npx @ccpocket/bridge@latest setup` resolved the latest package as version `1.65.0`.
- The command wrote the LaunchAgent plist and reached `Registering service...`.
- Because the active Codex session was connected through this same Bridge, `launchctl unload` terminated the connection and the setup process before it could reload the service.
- Recovery loaded the validated plist and explicitly started `com.ccpocket.bridge`.
- The pre-setup and post-setup plist files are byte-identical with SHA-256 `58d348362330a0d2cc70e33ae2c809b8b0f77d553558a0071b9659348d533d4f`.
- Final verification: LaunchAgent state `running`; Node listener active on `*:8765`; startup log reports `Ready. Listening on http://0.0.0.0:8765`; local HTTP probe reaches the server and returns the expected root-path `404`.

## Rollback

1. Run `launchctl unload "$HOME/Library/LaunchAgents/com.ccpocket.bridge.plist"`.
2. Restore the backup plist to `$HOME/Library/LaunchAgents/com.ccpocket.bridge.plist`.
3. Run `plutil -lint "$HOME/Library/LaunchAgents/com.ccpocket.bridge.plist"`.
4. Run `launchctl load "$HOME/Library/LaunchAgents/com.ccpocket.bridge.plist"`.
