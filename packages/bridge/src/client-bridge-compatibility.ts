/**
 * Local CC Pocket Mobile/Bridge release-train revision.
 *
 * This is deliberately independent from the public npm package version. Bump
 * it only when a locally distributed IPA and Bridge should prompt when paired
 * with an older counterpart. Bridge-only fixes that remain compatible do not
 * need to change it.
 */
export const CLIENT_BRIDGE_COMPATIBILITY_REVISION = 1;
